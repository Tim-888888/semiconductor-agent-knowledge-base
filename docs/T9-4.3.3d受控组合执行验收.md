# T9-4.3.3d 受控组合执行验收

## 1. 当前结论

状态：**已完成**。

预定义组合执行、逐任务终态、路由专用证据校验、`task_status` SSE、按消息持久化和前端展示已经实现。
本地质量门禁、目标 ECS 真实服务调用、MongoDB 四方对账、桌面与 `390 x 844` 浏览器真实点击、
刷新恢复、截图、控制台和布局检查均已通过。T9-4.3.4 仍未开始，必须取得用户新的开发确认。

## 2. 实现范围

- 单轮最多 3 个 `task_items`，只允许既定 RAG、Tool、RAG+Tool、RAG+Web、历史处理+新任务组合。
- `task_decisions` 记录执行前策略；`task_results` 记录执行后的
  `completed/clarify/refused/deferred/failed`，两者语义分离。
- 安全任务可继续；同句 Recipe 写入单独拒绝，未启用的报告任务单独延后，不静默漏单。
- `chat_direct` 不携带检索证据；RAG、Tool、RAG+Tool、RAG+Web 分别验证所需的内部证据、工具事实和
  外部证据，降级结果留下稳定告警。
- SSE 增加 `task_status`，前端按消息显示最终路由、进行中任务和逐项终态；展示模式仍由服务端决定，
  RAG/Tool 使用结构化卡片，直接聊天、澄清和拒绝使用气泡。
- `agent_message_requests.task_results`、`result_payload.task_results` 和助手消息
  `presentation.task_results` 使用向后兼容的可选字段，无破坏性迁移。

## 3. 自动验收

- 后端全量：`226 passed`；唯一告警是既有 Starlette/httpx 弃用提示。
- Ruff：通过。
- 前端 ESLint、TypeScript 和 Vite production build：通过。
- `git diff --check`：通过，仅有 Windows 行尾提示。
- 变更文件凭据扫描：零命中；根目录 `.env` 继续被 Git 忽略。
- ECS 真实组合报告：`docs/evidence/t9-4-3-3d/live-controlled-task-report.json`。

真实组合覆盖：

1. `tool_only`：安全 FDC 查询完成；Recipe 写入拒绝；报告生成延后。
2. `rag_and_tool`：三个任务均完成，同时具有受控内部证据和制造数据工具事实。
3. 历史处理+RAG：历史任务走 `chat_direct` 且证据 ID 为空；新 SOP 查询走 `internal_rag`。

每个案例都已对账服务返回、MongoDB 请求台账、`result_payload` 和助手消息展示字段，结果完全一致；
临时线程、Checkpoint、请求台账、审计和 Trace 在验证结束后按测试用户范围清理。

## 4. ECS 与部署保护

最终应用容器：API `7575c3874b78`、Worker `c14722a17fe6`、Web `1a1b7442aa5c`；API 健康检查为
`ok`，Worker `pong`，Web 健康。

MongoDB `e6bd110ad320`、Milvus `2f6827ca640e`、Milvus MinIO `6c9133e777d2`、业务 MinIO
`54dab754141e`、etcd `9deab8ba1805`、Redis `ada0a643b6a6` 在部署前后 ID 完全一致。

本次曾因未显式指定既有 Compose 项目名和生产文件，短暂创建一组隔离的应用容器、空网络和空 Redis
卷；没有修改或重启数据服务。随后按容器标签记录的
`docker compose -p semikb -f docker-compose.prod.yml` 恢复应用，并删除确认无容器使用的误建资源。
后续部署命令必须同时固定项目名和生产 Compose 文件。

## 5. 浏览器与 MongoDB 验收

- “安全查询+危险写入+报告”最终显示 `completed/refused/deferred`，安全任务继续执行。
- RAG+Tool 的三个任务在 SSE 中真实显示 `running`，最终全部完成；证据抽屉包含 5 条内部证据和
  1 条制造数据事实。
- 历史+RAG 在 SSE 中先显示 `chat_direct=running`、`internal_rag=queued`，最终两项完成且准确引用上一条
  业务问题。
- 刷新后保持相同线程 ID，逐任务结果和历史原句完全恢复；MongoDB 台账、终态载荷和助手展示字段一致。
- 桌面和精确 `390 x 844` 截图均无横向溢出、文本重叠或状态错位；控制台 0 error、0 warning。
- 结构化卡片用于 Tool/RAG 及其组合；3.3c 已验收的直接聊天、澄清和拒绝仍使用普通气泡。

证据：

- `docs/evidence/t9-4-3-3d/browser-and-mongo-acceptance.json`
- `docs/evidence/t9-4-3-3d/desktop-rag-tool-streaming.png`
- `docs/evidence/t9-4-3-3d/desktop-partial-success.png`
- `docs/evidence/t9-4-3-3d/desktop-history-refresh.png`
- `docs/evidence/t9-4-3-3d/mobile-390x844-history-task-results.png`

## 6. 阶段边界

T9-4.3.3d 已完成，只解除 T9-4.3.4 的开发门禁。自动显示第一张最终授权图片、完整 Markdown/PDF 入库
浏览器闭环、全量意图回归和异步评估仍属于 3.3.4，本阶段没有提前实现或宣称完成。
