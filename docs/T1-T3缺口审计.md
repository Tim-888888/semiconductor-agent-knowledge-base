# T1-T3 缺口审计

审计日期：2026-08-11。审计方法：逐项对照原始 Task 的输入、输出和验收标准，检查
仓库产物、自动测试以及真实服务状态。状态只依据已验证证据，不依据历史开发记录中的结论。

## T1 工程骨架

状态：**已完成**。

| 验收项 | 证据 | 结论 |
| --- | --- | --- |
| 独立仓库 | 本地 Git 仓库及 GitHub `Tim-888888/semiconductor-agent-knowledge-base` | 通过 |
| Python/Node 工程 | `pyproject.toml`、`src/`、`tests/`、`web/package.json`、lock 文件 | 通过 |
| 配置隔离 | `.env` 被 `.gitignore` 命中且未进入 tracked files；`.env.example` 无真实密钥 | 通过 |
| API 启动 | TestClient 请求 `/api/v1/health` 返回 `200` 和 `ok` | 通过 |
| Web 构建 | `npm run build` 成功 | 通过 |
| 测试入口 | `pytest` 9 项通过，存在 1 条第三方弃用告警 | 通过 |

## T2 存储契约

状态：**部分完成，不能标记完成**。

### 已完成证据

- Pydantic 领域契约和 MongoDB、Milvus、MinIO 职责边界已经定义。
- MongoDB、Milvus、MinIO、Redis 真实连接预检均通过。
- `semikb-raw`、`semikb-derived` 存在且无公开 Bucket Policy。
- `semikb_chunks_v1`、Dense HNSW、Sparse Inverted Index 已存在。
- `semikb-redis` 健康，密码认证、AOF 和持久化策略已验收。

### 未完成项

| ID | 缺口 | 完成标准 |
| --- | --- | --- |
| T2-G1 | 未配置服务返回 `configured=false`，但 `detail` 仍显示 `URI/API configured` | 返回明确的 `not configured` 与缺失变量名称，并有自动测试 |
| T2-G2 | Preflight/Provisioning 内部重复直接创建客户端，没有统一客户端工厂和超时/关闭约定 | 提供低耦合客户端工厂或上下文管理接口，并完成单元测试 |
| T2-G3 | 只有连通性检查，没有可重复的 Mongo 索引、MinIO 私有策略、Milvus schema/index/alias、Redis 配置核验命令 | 新增只读 verifier，输出结构化差异并在不一致时失败 |
| T2-G4 | Mongo 索引列表被一次性创建为复合索引；如 `ingestion_jobs(job_id,idempotency_key)` 不能保证 `idempotency_key` 单独唯一 | 先设计迁移方案，经用户审批后仅修改 `semikb` 项目索引并复验 |
| T2-G5 | `DocumentRevision/Chunk` 允许 `None` 的过滤字段，与 Milvus 默认非空 VARCHAR/INT64 字段不一致 | 明确 nullable/default 方案，并用最小实体插入/读取测试验证 |
| T2-G6 | 没有 T2 专项自动测试 | 覆盖缺失配置、异常脱敏、schema contract、索引定义和 verifier |

### 2026-08-11 修复进展

- T2-G1、T2-G2、T2-G3、T2-G5、T2-G6 已完成代码实现并通过 8 项专项测试。
- 真实 `preflight` 对 MongoDB、Milvus、MinIO、Redis 全部通过。
- 只读 verifier 对 MinIO、Milvus、Redis 全部通过，并按预期以退出码 `1` 报告 MongoDB 索引差异。
- T2-G4 迁移方案见 `docs/T2-MongoDB索引迁移方案.md`，尚未执行，因此 T2 继续保持“部分完成”。

说明：修复 T2-G4 可能涉及删除或重建 `semikb` 数据库中的项目索引，实施前必须单独获得用户审批；
不得修改 MongoDB、Milvus、MinIO、Attu 的容器、网络或服务配置。

## T3 演示数据

状态：**部分完成，不能标记完成**。

### 已有内容

- SOP R1 历史版和 R2 现行版。
- 一个 Edge-ring 缺陷 Case。
- 一条 Pressure FDC 处置规则。
- 一条晶圆图图注/OCR/检测摘要元数据。
- 3 条正向检索黄金问题。

### 未完成项

| ID | 缺口 | 完成标准 |
| --- | --- | --- |
| T3-G1 | 无独立 Recipe 文档 | 至少提供同一 Recipe 的历史版与现行版、变更关系、审批和有效期 |
| T3-G2 | 无真实晶圆图二进制文件 | 提供合成 PNG/TIFF 资产，字节 SHA-256 与对象引用一致，并能实际打开 |
| T3-G3 | 无 SPC 数据 | 提供合成 CSV/Parquet 时序数据，包含时间、Wafer/Lot、Tool/Chamber、指标、UCL/LCL 和异常标签 |
| T3-G4 | 来源与权限依赖默认值 | 每条文档显式包含 synthetic source、许可、版本、审批、`access_scope_key` 和适用范围 |
| T3-G5 | 黄金集覆盖不足 | 增加 Recipe、SPC、图片、过期版本、无权限、无答案等 Case，显式保存 Scope、预期证据和失败标签 |
| T3-G6 | 缺少数据质量专项测试 | 自动验证 ID 唯一、版本链、文件存在、哈希一致、字段完整及黄金集引用有效 |

## 开发门禁

1. 先完成 T2-G1、T2-G2、T2-G3、T2-G5、T2-G6；T2-G4 在审批后实施。
2. T3 可与不涉及真实存储变更的 T2 修复并行，但必须完成全部 T3-G1 至 T3-G6。
3. T2、T3 均通过专项测试后，才能恢复 T4 真实入库开发。
