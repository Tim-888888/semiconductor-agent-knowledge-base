# 分阶段开发计划

## 执行原则

每个 Task 完成后必须更新 `开发记录.md`，运行对应测试，并以英文 Git commit
提交。真实服务配置、密钥和生产数据不进入 Git。

| Task | 目标 | 输入 | 输出 | 验收 |
| --- | --- | --- | --- | --- |
| T1 | 工程骨架 | 已确认技术选型 | 独立仓库、配置、文档、测试入口 | `.env` 已忽略，API/Web 骨架可启动 |
| T2 | 存储契约 | `.env` 服务参数 | Collection/Bucket/客户端烟雾检查 | 不泄露密钥，连接失败可诊断 |
| T3 | 演示数据 | 半导体模拟场景 | SOP、Recipe、Case、图片、黄金集 | 每条数据可追溯且带版本/权限 |
| T4 | 入库 | 原始文档和元数据 | Job、Chunk、资产、暂存发布 | 幂等重试，失败不发布 |
| T5 | 检索 | 已发布 Chunk | 混合召回、精排、图片引用 | 无权/过期内容不可返回 |
| T6 | 对话 | 检索与模拟工具 | 连续线程、追问、证据账本 | 同一线程可恢复，缺信息不猜测 |
| T7 | 运营能力 | Trace 与黄金集 | 入库中心、评估运行、基线对比 | 过程可重现 |
| T8 | Web UI | 稳定 API | 工作台、证据、Trace、评估页 | 四项产品亮点可操作 |
| T9 | 部署验收 | 已填 `.env` 和服务版本 | Docker、兼容矩阵、演示脚本 | 端到端 Demo 可重复 |

`T2` 与 `T3` 可并行。`T4 -> T5 -> T6` 为主链，`T7` 在 T4/T5 后并行，T8
等待 API 契约稳定后实施。

## 当前执行状态（2026-08-12）

| Task | 状态 | 说明 |
| --- | --- | --- |
| T1 | 已完成 | 独立仓库、环境模板、测试和前后端骨架已建立。 |
| T2 | 已完成 | 四类存储连接与结构验证通过；MongoDB T2-G4 索引迁移已受控执行，完整 verifier 通过，重复 dry-run 为 0 个动作。 |
| T3 | 已完成 | 已提供 SOP/Recipe 多版本、Case/FDC、两张真实合成晶圆图、120 行 SPC 数据、显式来源/许可/Scope、`demo-v2` 八类黄金 Case 和数据质量测试。 |
| T4 | 已完成 | 已实现 Celery 异步入口、MinIO 原件/解析件/图片、MongoDB Job/Event/Catalog、在线 Qwen 原生 Dense/Sparse、Milvus 暂存发布、Alias 切换、补偿与跨进程幂等重试；v4 新文档真实入库与精确清理通过。 |
| T5 | 已完成 | 在线 `qwen3.7-text-embedding` 原生 Dense/Sparse、Milvus ACL/版本过滤、条件 HyDE、RRF、`qwen3-rerank`、动态截断、MongoDB Trace 与 MinIO 图片闭环已在 v4 影子和发布后复验。 |
| T6 | 已完成 | 真实 `interrupt/resume`、MongoDB Checkpointer、证据账本、只读模拟工具、Luna/Qwen 答案生成与校验、显式批准长期记忆已通过跨服务实例恢复验收。 |
| T7 | 已完成 | 已实现不可变数据集快照、MongoDB 运行真相、Celery 异步执行、四档检索基线、指标差异、失败 Case、管理员权限和真实服务验收；证据见 `docs/T7离线评估验收.md`。 |
| T8 | 已完成 | 已按 T5/T6/T7 最终契约重建四个运营页面；真实 API/Worker 点击、文件上传、评估运行、Trace/图片下钻及桌面/移动截图验收全部通过，证据见 `docs/T8浏览器验收.md`。 |
| T9 | 未完成（部署验收待执行） | 已有 Docker Compose、兼容矩阵和演示脚本草案；仍需阿里云镜像版本核对、容器部署、端到端复现、容量与运维验收。 |

T9 前置变更（2026-08-12）：因目标阿里云服务器无 GPU，已把本地 BGE-M3 替换为在线
`qwen3.7-text-embedding`。确认其支持 `dense&sparse` 后，活动索引从临时过渡的 v3 迁移到
原生 Sparse 的 v4；v3、v2 均保留回滚。v4 已重新通过 T4-T8，不改变 T9 尚未完成的状态。

审计门禁：T2、T3 的全部缺口与验收测试已经完成，T4 真实入库开发门禁已解除。逐项证据与
完成标准见 `docs/T1-T3缺口审计.md`；真实验收证据见 `docs/T4真实入库验收.md`、
`docs/T5真实检索验收.md`、`docs/T6真实Agent验收.md`、`docs/T7离线评估验收.md` 和
`docs/T8浏览器验收.md`。
