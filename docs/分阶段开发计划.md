# 分阶段开发计划

## 执行原则

每个 Task 完成后必须更新 `开发记录.md`，运行对应测试，并以英文 Git commit
提交。真实服务配置、密钥和生产数据不进入 Git。

| Task | 目标 | 输入 | 输出 | 验收 |
| --- | --- | --- | --- | --- |
| T1 | 工程骨架 | 已确认技术选型 | 独立仓库、配置、文档、测试入口 | `.env` 已忽略，API/Web 骨架可启动 |
| T2 | 存储契约 | `.env` 服务参数 | Collection/Bucket/客户端烟雾检查 | 不泄露密钥，连接失败可诊断 |
| T3 | 演示数据 | 半导体模拟场景 | SOP、Recipe、Case、图片、黄金集 | 每条数据可追溯且带版本/权限 |
| T4 | 入库 | 原始文档和元数据 | Job、Chunk、资产、暂存发布 | 幂等重试，失败不发布 |
| T5 | 检索 | 已发布 Chunk | 混合召回、精排、图片引用 | 无权/过期内容不可返回 |
| T6 | 对话 | 检索与模拟工具 | 可恢复线程、追问、证据账本 | 澄清流程可跨实例恢复，缺信息不猜测 |
| T7 | 运营能力 | Trace 与黄金集 | 入库中心、评估运行、基线对比 | 过程可重现 |
| T8 | Web UI | 稳定 API | 工作台、证据、Trace、评估页 | 四项产品亮点可操作 |
| T9 | 部署验收 | 已填 `.env` 和服务版本 | Docker、兼容矩阵、演示脚本 | 端到端 Demo 可重复 |

`T2` 与 `T3` 可并行。`T4 -> T5 -> T6` 为主链，`T7` 在 T4/T5 后并行，T8
等待 API 契约稳定后实施。

## 当前执行状态（2026-08-30）

| Task | 状态 | 说明 |
| --- | --- | --- |
| T1 | 已完成 | 独立仓库、环境模板、测试和前后端骨架已建立。 |
| T2 | 已完成 | 四类存储连接与结构验证通过；MongoDB T2-G4 索引迁移已受控执行，完整 verifier 通过，重复 dry-run 为 0 个动作。 |
| T3 | 已完成 | 已提供 SOP/Recipe 多版本、Case/FDC、两张真实合成晶圆图、120 行 SPC 数据、显式来源/许可/Scope、`demo-v2` 八类黄金 Case 和数据质量测试。 |
| T4 | 已完成 | 已实现 Celery 异步入口、MinIO 原件/解析件/图片、MongoDB Job/Event/Catalog、在线 Qwen 原生 Dense/Sparse、Milvus 暂存发布、Alias 切换、补偿与跨进程幂等重试；v4 新文档真实入库与精确清理通过。 |
| T5 | 已完成 | 在线 `qwen3.7-text-embedding` 原生 Dense/Sparse、Milvus ACL/版本过滤、条件 HyDE、RRF、`qwen3-rerank`、动态截断、MongoDB Trace 与 MinIO 图片闭环已在 v4 影子和发布后复验。 |
| T6 | 已完成 | 真实 `interrupt/resume`、MongoDB Checkpointer、证据账本、只读模拟工具、Luna/Qwen 答案生成与校验、显式批准长期记忆已通过跨服务实例恢复验收。这里的“恢复”特指澄清中断与图状态恢复，不代表通用历史装配和按需路由已实现。 |
| T7 | 已完成 | 已实现不可变数据集快照、MongoDB 运行真相、Celery 异步执行、四档检索基线、指标差异、失败 Case、管理员权限和真实服务验收；证据见 `docs/T7离线评估验收.md`。 |
| T8 | 已完成 | 已按 T5/T6/T7 最终契约重建四个运营页面；真实 API/Worker 点击、文件上传、评估运行、Trace/图片下钻及桌面/移动截图验收全部通过，证据见 `docs/T8浏览器验收.md`。 |
| T9 | 已完成 | T9-1~T9-4.9 已按门禁顺序完成；后续 T9-4.10~T9-4.12 体验、语义接地和公共知识 Web 闭环修复也已独立验收。最终一键验收、桌面/移动浏览器点击、截图、迁移包和风险边界均已归档。证据见 `docs/T9-4端到端验收报告.md`、`docs/T9-4.10工作台视口与输入器交互优化验收.md`、`docs/T9-4.10.1输入器按钮布局修复验收.md`、`docs/T9-4.11意图语义接地与知识证据降级验收.md` 和 `docs/T9-4.12公共知识检索与Web回退闭环验收.md`。 |

### T9 子阶段门禁

| 子阶段 | 状态 | 输出与验收 |
| --- | --- | --- |
| T9-1 部署前审计 | 已完成 | 只读盘点当前 CentOS/Docker/服务版本；给出两档 ECS 规格；审计前后容器快照一致；不修改基础设施 |
| T9-2 部署产物加固 | 已完成 | 全栈 Compose、精确镜像、鉴权、资源限制、健康检查、日志、内部网络、配置生成、冷备恢复和静态验收已完成；证据见 `docs/T9-2生产部署产物加固验收.md` |
| T9-3 阿里云部署 | 已完成 | 新 ECS 已安装 Moby/Compose，完成镜像构建、服务器配置、空存储初始化、7 份合成语料发布、全栈启动和公网工作台烟雾验收；证据见 `docs/T9-3阿里云真机部署验收.md` |
| T9-4 端到端验收 | 已完成 | 9 个最终演示场景、SSE、Trace、首图、运营页面、安全黑盒、离线迁移、存储不变量、Worker、公网桌面/移动点击与 Console 均通过。证据见 `docs/T9-4端到端验收报告.md` 和 `docs/evidence/t9-4/`。 |

### T9-4 执行顺序

`T9-4` 严格按以下顺序执行，前一项未验收不得跳到后一项：

1. `T9-4.1` 已完成：Luna/Qwen 已在本地和 ECS 返回真实 Delta；SSE 事件、Provider 终止、错误、取消、幂等和最终持久化语义已冻结。
2. `T9-4.2` 已完成：FastAPI `StreamingResponse`、LangGraph 阶段事件、LLM Token Delta、
   `fetch + ReadableStream`、Nginx 禁止缓冲、取消/刷新断连和旧非流式接口回归均已验收。
3. `T9-4.3` 已完成：通用会话、按需路由与完整业务闭环严格按以下串行子任务验收：
   - `T9-4.3.1` 已完成：精确消息、旧历史摘要、活动上下文、线程恢复、同线程顺序控制、槽位来源和失效传播基础结构均已实现并通过 ECS 真 Mongo 验收。
   - `T9-4.3.2` 已完成：L0 快速通道，任务/非任务/反馈/控制/混合识别，一级意图，最多 3 个受控任务，情绪解耦，槽位设置/继承/纠正/清除，取消范围，一次结构化 LLM 和确定性 `RoutePolicy`；`semikb-intent-v1` 及 ECS 真实路由调用计数均通过。
   - `T9-4.3.3a` 已完成：建立版本化意图定义卡、易混淆对、独立示例库和冻结
     `semikb-intent-v3`；一期把全部 `active` 卡注入一次结构化 LLM，不做候选卡预筛；补齐逐类混淆、
     P/R/F1、多任务、对象动作、槽位、依赖及卡数/Token 容量指标。证据见
     `docs/T9-4.3.3a意图治理与评测验收.md`。
   - `T9-4.3.3b` 已完成：同一冻结 v3 对照当前 Prompt、全量卡、全量卡+固定 Few-shot、全量卡+动态
     Few-shot；示例库与评估集交叉为 0，质量、卡数、Token、P50/P95 延迟、调用和参考成本均已归档。
     C 因意图卡 Macro-F1 回退被否决；D 未在多任务和关键混淆对上严格优于 C，也被否决；线上保持 B。
     证据见 `docs/T9-4.3.3b Few-shot影子实验验收.md`。
   - `T9-4.3.3c` 已完成：历史回顾迁移为 `chat_direct` 的历史约束生成，服务端固定准确消息引用，
     LLM 只负责自然表达；同时自然化普通聊天、澄清和能力边界拒绝，保留确定性失败回退。展示方式由
     服务端按消息和最终路由固定：RAG/证据复用/工具路由显示结构化卡片，直接聊天/澄清/拒绝显示气泡；
     后续气泡和刷新均不得覆盖历史卡片。旧 `history_direct` 只作持久化记录兼容。证据见
     `docs/T9-4.3.3c自然直接回答验收.md`。
   - `T9-4.3.3d` 已完成：只允许预定义 RAG/Tool/Web/历史组合，逐任务记录执行、澄清、拒绝、延后和
     失败，不静默漏单；前端展示真实路由和任务状态。本地/ECS/Mongo 自动门禁、桌面/移动浏览器点击、
     刷新恢复、截图和控制台检查均通过。证据见 `docs/T9-4.3.3d受控组合执行验收.md`。
   - `T9-4.3.4` 已完成：从用户上传 Markdown/PDF 开始，真实验证入库、检索、连续问答、混合任务、
     图片、Trace、异步评估和意图回归。最终授权证据包含图片时自动显示第一张，多图可切换，新结果无图
     时清除旧预览；本地 `230 passed`，ECS 跨存储及桌面/移动浏览器闭环通过。证据见
     `docs/T9-4.3.4完整业务闭环验收.md`。
4. `T9-4.4` Knowhere 式多格式入库与语料治理：前五个子阶段完成可替换的 `semikb_ingest`、九格式真实闭环；新增 `T9-4.4.6`，先实现知识文档受控下架/恢复，再完成通用化加固、公开半导体语料标准化、分层评测和密封盲测，禁止针对已知来源或演示问题建立专用处理分支。
5. `T9-4.5` 已完成：Embedding、Reranker、LLM、MinerU、OCR/VLM 和 Web MCP 已统一接入有界重试、稳定失败分类与脱敏尝试审计；本地故障矩阵、ECS 六类真实烟雾、受保护容器不变和公网 Trace 页面均通过。证据见 `docs/T9-4.5Provider韧性验收.md`。
6. `T9-4.5.1` 已完成：建立版本化通用澄清 Frame、恢复前关系判定、结构化回答解析、确定性缺失条件推导、每轮进展约束、取消/换题/普通插话退出机制、旧 Checkpoint 兼容和脱敏审计；新任务独立路由并重置执行态，旧 Frame 不污染或复活。后端全量 `399 passed`，独立 8 场景/12 轮评估的完成率、关系准确率和路由准确率均为 `1.0`，新任务误吸收、取消失败、跨 Frame 污染、重复澄清、内部泄露和未补齐误调用均为 `0`；ECS 桌面/移动点击、刷新恢复和开关回滚通过。证据见 `docs/T9-4.5.1通用澄清状态机验收.md`。
7. `T9-4.6` 已完成：按 `1 -> 2 -> 3` 执行有界在线并发，同时运行 1 个入库和 1 个 Full 评估；
   6/6 在线请求完成且 0 错误/超时/5xx，最低可用内存约 5.06 GiB，无 OOM 或意外重启。九个服务完成
   两轮逐项受控重启，MongoDB、Milvus Strong 逻辑计数、MinIO 和 Redis 前后对账一致；公网 SSE、
   刷新恢复及桌面/移动浏览器通过。证据见 `docs/T9-4.6容量与运行韧性验收.md`。
8. `T9-4.7` 已完成：生成带 SHA-256 和同期环境副本的停写冷备，恢复到独立空目录和独立 Compose
   Project；MongoDB 17 个业务集合、Milvus 149 行、MinIO 56/91 个对象、Redis 和 `demo-v2` 检索烟雾
   与备份点一致，清理后线上指纹无差异。证据见 `docs/T9-4.7冷备恢复验收.md`。
9. `T9-4.8` 已完成：公网仅有 22/80，JWT/RBAC、服务端固定 Demo Scope、访问码限流、约 300 秒
   图片链接、日志查询串脱敏和非 root/只读应用容器均通过；固定 10 个镜像，生成 3,813,758,976 bytes
   离线包并完成删除专用别名后的真实重载，数据容器 ID 和跨存储指纹不变。证据见
   `docs/T9-4.8安全与可迁移部署验收.md`。
10. `T9-4.9` 已完成：一键脚本的 6 类总门禁和 9 个演示场景全部通过；亲自在桌面/移动浏览器完成
    聊天、RAG、证据复用、历史、澄清、图片、多任务、Trace、任务中心和评估页点击，形成最终报告与截图。
11. `T9-4.10` 已完成：作为 T9 最终验收后的独立体验优化，工作台改为动态视口布局，澄清期间仅保留
    黄色持久状态，消息区独立滚动，输入区始终留在可视范围；浏览器原生缩放改为可访问的展开/收起
    按钮。桌面和 `390 x 844` 浏览器均完成真实点击、尺寸断言、截图和 Console 检查，且只重建 Web，
    六个受保护数据容器 ID 不变。证据见 `docs/T9-4.10工作台视口与输入器交互优化验收.md`。
12. `T9-4.10.1` 已完成：展开/收起按钮与发送按钮改由同一个右下角横向操作区管理，统一为
    `32 x 32px` 并固定 `6px` 间距；textarea 为操作区预留 `86px`，收起和展开状态不再依赖垂直定位。
    桌面 `1440 x 900`、用户实际 Chrome 视口和移动 `390 x 844` 的按钮矩形均不相交，输入内容保持、
    Console 0 error/warning。ECS 只重建 Web，六个受保护数据容器 ID 不变。证据见
    `docs/T9-4.10.1输入器按钮布局修复验收.md`。
13. `T9-4.11` 已完成：新增意图语义接地、任务形态化条件槽位和知识证据充分性闭环。业务关键词只作为
    弱语义信号，不能单独决定路由；每个 LLM 任务必须由当前消息或受控历史原文支撑。知识查询先执行
    内部 RAG，再按公开通用、内部受控、混合知识边界决定是否进行一次受控 Web 降级。冻结 v1/v2/v3
    意图集和旧 MongoDB/Checkpoint/Trace 保持兼容，新增未见最小差异对评测。开发与在线验收只使用
    香港 ECS `47.76.218.205`，深圳 ECS 未连接或修改。后端全量 `462 passed`，132 条 v4 冻结评审集、
    API/SSE、Trace、桌面/移动浏览器、刷新恢复和六个数据容器不变量均通过。详细结果见
    `docs/T9-4.11意图语义接地与知识证据降级验收.md`。
14. `T9-4.12` 已完成：公开通用知识新增独立回答可用性门禁，综合绝对分数、相对比例、断崖差值、
    术语覆盖和问题覆盖度；低价值 Chunk 保留在 Trace 但不进入答案。内部证据全部不合格时触发有界
    Web 回退，解析 MCP 结构化结果、JSON 和文本链接，不按域名硬过滤；有效 Web 结果使用自然回答和
    可点击来源，内部 SOP 继续使用结构化调查卡且不被 Web 覆盖。5 条未使用报告原句的冻结校准 Case、
    后端全量 `470 passed`、香港 ECS、桌面/移动浏览器、来源点击、刷新恢复和数据容器不变量均通过。
    证据见 `docs/T9-4.12公共知识检索与Web回退闭环验收.md`。
15. `香港 ECS 迁移` 的应用、数据迁移和 24 小时 HTTP 稳定性观察已完成：应用和四类存储已完成冷备、
    独立恢复、内容级对账和生产提升；香港 HTTP 域名入口、9 场景自动验收、桌面/移动点击、SSE、首图
    自动预览和运营页面均通过。HTTPS 代码已准备，但 Let’s Encrypt 二次 DNS 校验仍超时，因此 TLS
    与 443 尚未完成。详见 `docs/SemiAtlas香港ECS迁移验收报告.md`。该阻塞不回退已完成的 T9 功能状态。

流式实现固定使用 `SSE + fetch ReadableStream`。服务端事件至少包括 `accepted`、`stage`、
`evidence`、`answer_delta`、`completed`、`error` 和 `heartbeat`；只输出可公开的处理阶段与答案，
不得输出隐藏推理、系统提示词或密钥。原有非流式消息接口保留用于兼容和回归测试。

T9-1/T9-2 完成不代表 T9 完成。标准门禁要求安全组新增 `80/tcp`、将 `22/tcp` 限制为用户公网 IP、
删除公网 `3389`，并保持所有数据库和存储端口不开放。当前测试 ECS 已新增 `80/tcp` 并删除公网
`3389`；考虑用户需要从不同办公地点连接，用户明确批准测试期暂时保留 `22/tcp` 对 `0.0.0.0/0`
开放。该项记录为限时风险例外，不视为生产安全标准已满足；正式发布前必须改为密钥登录并限制来源，
或接入受控运维通道。T9-3 完成不代表 T9 完成；T9-4 未通过前不得把部署烟雾验证写成完整端到端验收。

T9 前置变更（2026-08-12）：因目标阿里云服务器无 GPU，已把本地 BGE-M3 替换为在线
`qwen3.7-text-embedding`。确认其支持 `dense&sparse` 后，活动索引从临时过渡的 v3 迁移到
原生 Sparse 的 v4；v3、v2 均保留回滚。v4 已重新通过 T4-T8，不改变 T9 尚未完成的状态。

T9-4.3 设计补充（2026-08-13）：采用“上下文装配 + 1 个前置闸门 + 3 层逻辑识别 + 确定性路由 +
路由专用校验”。明显请求走规则，复杂请求只做一次结构化 LLM 理解，LLM 不拥有最终路由权；
历史回顾和普通会话不得默认触发 RAG。详细契约、存储影响和验收 Case 见
`docs/T9-4.3通用会话记忆与按需路由设计.md`。本文档更新不等于实现完成。

T9-4.3 受控意图增强（2026-08-13）：吸收任务/非任务/混合识别、受控多任务、情绪解耦、槽位纠正与
取消、L0 快速通道、严格结构化输出和半导体意图评估 7 项能力。为控制复杂度，单轮最多 3 个任务，
只支持预定义组合；不增加独立向量意图模型、第二次意图 LLM 调用、自由任务 DAG、固定通用置信度阈值
或语义低置信度 session 熔断。开发量预计比原 T9-4.3 增加约 `25%~35%`，不新增基础设施。

T9-4.3.3 计划补充（2026-08-14）：用户确认增加意图定义卡、单意图/多任务评测升级、固定与动态
Few-shot 离线对照，以及带历史的自然 `chat_direct/clarify/refuse`。历史回顾的新目标行为为
`chat_direct + 服务端精确消息引用 + LLM 自然表达 + 确定性回退`，不调用 RAG；T9-4.3.3c 已完成
迁移，新请求不会再产生旧 `history_direct`。T9-4.3.3 严格拆为 a~d，当前 3.3d 已实现并处于浏览器
收尾验收中，本文档更新不等于 3.3d 或后续阶段完成。

T9-4.3.3 意图卡策略补充（2026-08-14）：项目初期活动卡较少，用户确认复杂输入一次性加载全部
`active` 意图卡以优先保证覆盖率，不增加候选卡预筛。固定 Few-shot 曾是实验前默认候选，动态检索
只补充案例而不筛选意图卡；3.3b 实测后两者均未获得上线资格，当前生产保持全量卡 B。达到卡数、
Prompt Token、P95 延迟或混淆门禁时只告警并重新评审，不能自动改变策略。

T9-4.3.3a 完成记录（2026-08-14）：已发布 15 张版本化卡（13 active）、15 条隔离示例和 108 条
`semikb-intent-v3`；70 个在线 LLM Case 的全量卡注入率为 1.0，请求台账可追溯目录 Hash、卡数、
Token 与时延。在线 P95 `7562.108 ms` 超过 5000 ms 门禁且多任务精确匹配为 `0.181818`，均作为
T9-4.3.3b 基线保留，不改变全量卡策略，也不代表模型效果达到生产标准。

T9-4.3.3b 完成记录（2026-08-14）：A/B/C/D 四臂各运行 108 条 v3，其中 L0 38、LLM 70；C 固定
Few-shot 的意图卡 Macro-F1 相对 B 回退，D 的多任务精确匹配和关键混淆对准确率未严格高于 C，故
两者均未获得上线候选资格。生产默认继续使用 B，0 个 Few-shot、0 次意图示例 Embedding；下一步只能
在用户确认后进入 T9-4.3.3c。

T9-4.3.3c 完成记录（2026-08-14）：默认目录切换到 `semikb-intent-catalog-v2`，历史回顾、历史变换、
普通交流、澄清和拒绝均通过受控自然生成；服务端校验完整 JSON 文本单元后才发送 SSE Delta。冻结
v1/v2/v3 通过版本化覆盖层复跑，路由准确率与上下文引用准确率均为 1.0，危险执行漏拒率和新
`history_direct` 产生率均为 0。公网浏览器完成真实流式、连续历史回顾、历史简化、澄清恢复、能力边界、
危险写入、刷新恢复和 390x844 验收；MongoDB 已验证 `direct_reply_audit` 真持久化。下一步只能在用户
确认后进入 T9-4.3.3d，不得提前进入 T9-4.3.4 或 T9-4.4。

T9-4.3.3c 展示热修复记录（2026-08-14）：`ChatMessage.presentation` 按消息保存展示模式、路由、结构化
答案和 Trace；旧线程通过请求台账兼容补齐。SSE 完成响应与线程读取共用补齐逻辑，修复新气泡覆盖旧
结构化卡片。公网已点击验证 `internal_rag` 卡片、`chat_direct/clarify/refuse` 气泡、刷新恢复和移动端。
该修复仍属于 3.3c，不改变下一步只能进入 3.3d 的门禁。

T9-4.3.3c 收尾记录（2026-08-14）：修复槽位纠正把否定侧旧 Chamber 当作新值的问题，服务端现在按
槽位类型解析肯定侧新值并校验 Grounding。公网 MongoDB 账本确认 `correct chamber=A`、
`chat_direct/control`、`trace_id=null`，刷新后回复和历史结构化卡片均恢复；同时真实点击补齐
`tool_only/reuse_evidence/rag_and_web` 的结构化卡片验收。后端全量 `215 passed`，数据服务容器未重启。
该收尾不改变下一步只能在用户确认后进入 T9-4.3.3d 的门禁。

T9-4.3.3d 完成记录（2026-08-14）：预定义组合、逐任务终态、路由专用校验、SSE `task_status`、
MongoDB 持久化和前端展示已经实现；后端 `226 passed`，ECS 三组真实组合及响应/台账/终态载荷/助手
消息四方对账通过。桌面和精确 `390 x 844` 浏览器点击、刷新、截图及控制台检查通过。下一步只能在
用户确认后进入 T9-4.3.4。

T9-4.3.4 完成记录（2026-08-14）：服务端持久化最终授权图片顺序，前端自动预览第一张并支持多图切换、
无图清空和刷新恢复；默认意图目录升级到 v3，MinerU 预签名 PUT 协议修复后真实 PDF 发布成功。
Markdown/PDF 入库、跨存储对账、连续问答、三任务 `rag_and_tool`、Trace、8 Case Full 评估和冻结
v1/v2/v3 意图回归均完成。后端 `230 passed`，公网桌面/移动点击和控制台检查通过；T9-4/T9 仍未完成，
下一步只能在用户确认后进入 T9-4.4。

T9-4.4 计划补充（2026-08-15）：用户确认把 Knowhere 式多格式路由编排进后续开发。该阶段先于
Provider 韧性执行，因为新增的格式解析器和 OCR/VLM/MinerU Provider 也必须进入后续故障注入，不能只
验证当前“Markdown/TXT 直读、其他格式统一 MinerU”的简化链路。T9-4.4 严格拆为以下串行子任务：

1. `T9-4.4.1`（已完成）上游与契约审计：重新核对 Knowhere 当前源码、固定复用 Commit 和 Apache-2.0
   义务；列出直接复用、改写参考和自主实现的文件及依赖；冻结本项目自己的 `ParsedDocument`、
   `ChunkDraft`、`ImageAssetDraft`、`TableAssetDraft` 和稳定错误码。不得把 Knowhere 的 Worker、任务
   状态、数据库或对象存储模型泄漏到业务层。
2. `T9-4.4.2`（已完成）包边界与格式路由：在 `semikb_ingest` 内建立 `parsers`、`providers`、`structure`、
   `assets`、`chunking` 和 `models`；按 MIME、扩展名和文件签名选择显式适配器。未支持格式必须明确
   拒绝或进入隔离队列，禁止静默落入“全部交给 MinerU”的万能分支。
3. `T9-4.4.3`（已完成）首期专用适配器：Markdown/TXT/HTML 走结构化文本解析；PDF 走 MinerU PDF 适配器并
   保留页码和页面资产；DOCX 走标题/段落/表格/图片专用解析；XLSX/CSV 走 Sheet、表头、单位、合并
   单元格和表格区域解析；JPG/JPEG/PNG 走真实图片保存、OCR/VLM 描述和 `image_text`；PPTX 走页面、
   文本框、表格和图片解析。Provider 回退必须由每种格式单独声明，不能跨格式盲目回退。
4. `T9-4.4.4`（已完成）入库集成与兼容：`rag_ingestion` 只依赖上述统一契约，继续复用现有上传、哈希去重、
   Job/Event、质量门禁、MinIO/MongoDB/Milvus 暂存发布和幂等补偿；记录 `parser_name`、
   `parser_version`、`upstream_commit`、`chunker_version` 和资产来源。现有 Markdown/PDF 路径必须
   保持兼容，跨存储主键和 API 不做无迁移方案的破坏性修改。
5. `T9-4.4.5`（已完成）逐格式验收：为每种格式建立可公开展示的黄金文件和预期章节、表格、图片、Chunk 及
   检索证据；运行单元、契约、幂等、质量门禁、权限/版本过滤和检索回归；再在 ECS 工作台逐一上传，
   检查任务阶段、原件/派生资产、真实问答、图片自动预览、Trace、刷新恢复、桌面和 `390 x 844`
   移动视口。该子阶段证明格式闭环，不替代真实公开语料治理与受控下架验收。
6. `T9-4.4.6`（进行中，`4.4.6a~b` 已完成）真实公开语料治理与受控下架：严格按
   `4.4.6a -> 4.4.6b -> 4.4.6c -> 4.4.6d` 串行执行。只有四项全部验收，才能进入 T9-4.5
   Provider 韧性。

允许直接复用或参考 Knowhere 源码，但提交前必须更新 `THIRD_PARTY_NOTICES.md`，保留许可证与来源
Commit，并记录本地改动。该计划确认不等于代码已实现；开始 T9-4.4.1 前仍需用户明确发出开发指令。

T9-4.4.1 完成记录（2026-08-15）：已刷新 Knowhere 官方仓库并固定 Commit
`2e4eb5846249d273b11902ee00f26db949e45b38`，冻结 License/NOTICE 哈希和关键 Blob；确认 README 与
代码在 CSV/HTML/JSON 支持声明上存在差异，因此以后以固定源码和本项目验收为准。已输出上游复用
分类、九格式能力矩阵、`ParsedDocument + ChunkDraft + ImageAssetDraft + TableAssetDraft` JSON Schema
和 16 个稳定错误码；专用测试 `4 passed`，后端全量测试 `234 passed`，Ruff 通过。本阶段未
Vendoring Knowhere 源码，未修改运行时依赖、镜像、ECS 或存储。详细证据见
`docs/T9-4.4.1Knowhere上游与契约审计.md` 和
`docs/evidence/t9-4-4-1/`。下一步只能在用户明确确认后进入 T9-4.4.2，不得提前开发专用格式适配器、
部署或 T9-4.5 Provider 韧性。

T9-4.4.2 完成记录（2026-08-15）：已建立独立顶层包 `semikb_ingest`，包含冻结 Pydantic 输出模型、
Parser/Provider 注册表、结构块、进程内资产句柄、Chunking 协议和唯一 `IngestDispatcher`；九格式路由
联合校验扩展名、MIME、Magic Bytes 与 OOXML 成员，并对文件上限、ZIP Bomb、路径穿越、重复路径和
格式冲突使用稳定错误码失败。只有 PDF 声明 MinerU，未知格式及未注册 Parser/Provider 无默认回退。
本阶段零新增依赖、零 Knowhere Vendored source、零 API/存储/ECS 变更；Wheel 仅增加 11,979 bytes。
定向测试 `32 passed`，后端全量测试 `266 passed, 1 warning`，Ruff、Schema 输出验证、Wheel 和凭据扫描
均通过。证据见 `docs/T9-4.4.2包边界与格式路由验收.md`。下一步只能在用户明确确认后进入
T9-4.4.3，不得提前接入发布链路、部署或执行 T9-4.5。

T9-4.4.3 完成记录（2026-08-15）：九格式适配器已在 `semikb_ingest` 内实现。Markdown/TXT/HTML
恢复结构化文本；PDF 使用独立 MinerU Provider 并解析页码、表格和图片；DOCX 保持块顺序并抽取标题、
列表、表格和图片；XLSX/CSV 保留 Sheet、表头、单位、合并单元格与表格区域且不执行公式；PPTX 保留
Slide、文本框、表格、图片、备注和位置；图片使用 `qwen3.7-plus` 生成受控图注/OCR/检测摘要和
`image_text`。真实 Qwen 图片冒烟、冻结 Schema、Linux 哈希锁、Wheel、凭据扫描、定向 `17 passed`
和后端全量 `283 passed, 1 warning` 均通过。未修改现有发布链路、API、存储或 ECS；详细证据见
`docs/T9-4.4.3首期专用格式适配器验收.md`。下一步只能在用户明确确认后进入 T9-4.4.4，不得提前
部署或标记 T9-4.4/T9 完成。

T9-4.4.4 完成记录（2026-08-15）：`rag_ingestion` 已只通过 `SemikbIngestAdapter` 消费冻结的
`semikb-ingest-v1` 输出，将 Draft 映射为受治理的 `DocumentRevision`、`Chunk`、`ImageAsset` 和
`TableAsset`。九格式继续复用原件上传、哈希幂等、Job/Event、质量门禁、MinIO 派生资产、MongoDB
目录、Milvus Chunk 投影、暂存发布和失败补偿。Parser/Provider/上游 Commit、Chunker、警告和指标可
追溯；旧字段使用向后兼容默认值。新增 `table_assets` 采用独立 dry-run/apply/rollback 迁移，已有业务
记录时拒绝危险回滚。定向测试 `61 passed`，后端全量 `302 passed, 1 warning`，Ruff、前端构建、Wheel
和凭据扫描通过。本阶段未部署 ECS、未创建逐格式黄金文件，也未做浏览器闭环；详细证据见
`docs/T9-4.4.4受控入库集成验收.md`。下一步只能在用户明确确认后进入 T9-4.4.5，不得提前进入
T9-4.5 Provider 韧性或标记 T9-4/T9 完成。

T9-4.4.5 完成记录（2026-08-15）：九种 CC0 合成黄金文件已完成确定性 Hash、预期 Chunk/图片/表格
和检索问题冻结。真实 ECS API/Worker 发布九格式，MongoDB 9 Document/26 Chunk/5 Image/9 Table、
MinIO 32 对象和 Milvus v4 26 行对账一致；9/9 检索命中，ACL 0 泄露，R1/R2 版本过滤和伪装 PDF 拒绝
通过。公网任务中心逐一显示九任务 100% 发布，工作台验证结构化回答、Trace、首图自动预览、多图切换、
无图清空及刷新恢复。桌面与精确 `390 x 844` 无横向溢出，Console 0 error/0 warning；最终后端
`313 passed, 1 warning`，Ruff、构建、Wheel 和凭据扫描通过。证据见
`docs/T9-4.4.5多格式真实闭环验收.md`。该完成记录当时只解除 T9-4.5 的原门禁；用户随后确认新增
T9-4.4.6，因此新增顺序从 T9-4.4.6a 开始；当前 4.4.6a 已完成，但仍不得标记 T9-4/T9 完成。

T9-4.4.6 计划补充（2026-08-15）：用户确认扩充真实公开半导体语料，并在批量入库前补齐可恢复的
知识文档下架能力。该阶段不把“文件已下载”视为“知识已可用”，也不把物理删除作为一期默认操作。

1. `T9-4.4.6a`（已完成）来源登记、生命周期契约与迁移设计：
   - 建立版本化 Source Manifest，至少保存 `source_id/title/source_type/source_url/doi_or_repo`、
     `retrieved_at/source_hash/real_or_synthetic/license/license_status/redistributable/ingestion_policy`、
     Parser 提示、预期资产和数据集版本。
   - 在兼容旧记录的前提下增加 `withdrawn` 生命周期；冻结文档清单、revision、下架、恢复、审计事件和
     跨存储补偿契约。先输出 MongoDB/Milvus/MinIO 影响、dry-run、回滚和旧数据迁移方案，再实施结构变化。
   - 大型表格数据不逐行向量化：MinIO 保存原始数据，MongoDB 保存目录与治理信息，RAG 只索引数据字典、
     数据画像和受控分析报告；明细通过只读分析工具访问。
2. `T9-4.4.6b`（已完成）受控下架、恢复与知识文档管理：
   - 增加知识文档及 revision 清单，管理员可填写原因后下架或恢复指定 revision；普通用户无权操作。
   - 下架先在 MongoDB 把 revision/Chunk/图片/表格标记为 `withdrawn`，使检索和资产访问立即拒绝，再精确
     删除活动 Milvus 投影；向量清理失败时保留不可检索状态并进入补偿任务，不得重新暴露内容。
   - 一期保留 MinIO 原件和派生资产用于审计与恢复，不提供误触式硬删除；未来硬清除需单独审批、延迟窗口
     和不可逆确认。恢复时重新校验审批、有效期、权限、Hash 和索引版本后再发布。
   - API 规划为文档列表、revision 列表、`withdraw` 和 `restore`；每次操作保存 actor、reason、时间、
     影响对象数、前后状态、补偿状态和 Trace/Job 关联。前端在入库任务中心增加“知识文档”视图。
   - Worker 通过 MongoDB 原子执行占用阻止同一操作被重复并发执行；占用 ID 是 Repository 私有字段，
     不得泄漏到公共操作契约、OpenAPI、Trace 或前端。
3. `T9-4.4.6c` 通用化加固、真实公开语料获取与标准化：
   - `T9-4.4.6c-1` 先修通用性基线。公共上传、Actor、Document、Chunk、Image/Table 和 Source Manifest
     契约不得静默继承 `demo_engineering/FAB-01/P-ALPHA/ETCH-03/approved/published`；Demo 值迁移到显式
     Demo Factory。未知治理字段允许为空或 `unknown`，新上传默认 `staged + draft`（界面显示“待复核”），缺少发布
     必需字段时进入澄清/隔离/人工复核，不得由模型猜测。
   - 首次发布和恢复使用同一 Source Manifest 门禁：校验 Manifest 存在且已批准、来源 Hash、Scope、许可/
     再分发状态及 `ingestion_policy` 与实际产物一致。来源卡只是通用契约实例，不能通过文件名、来源名或
     Document ID 触发专用业务逻辑。
   - 将受保护证据从 `SOP-` ID 前缀迁移到受治理的 `document_type/retrieval_policy`；HyDE、图片查询等
     启发式使用任务类型或版本化配置，并以未见过的中英文改写验证，禁止围绕演示问题持续追加关键词。
   - `T9-4.4.6c-2` 建立通用 `CorpusStandardizationService`：不可变原始快照、安全文件清单、格式/语料类型
     识别、统一 Parser Dispatcher、声明式 Sidecar/Profile、数据字典、数据画像、客观图注、跨文件关系、
     质量警告和暂存复核。文档、表格、图片集、多文件混合语料使用同一接口；ZIP/目录/标签文件关系必须
     通过通用批次或语料快照契约处理，不允许手工绕过 API/Worker。
   - 首批候选包括 UCI SECOM、ST-AWFD、Carinthia SEM Defect、WM-811K 抽样、DeepPCB 辅助资料、
     SemiKong 论文/仓库/ontology，以及 10~20 篇 Etch、FDC、Yield、Wafer Map、SEM 检测相关论文。
     这些来源只作为通用流水线的开发样本，不得出现 `if source == ...`、专用 Chunk、预置答案或检索结果。
   - 许可证不作为面试 Demo 的唯一筛除条件，但来源、作者/机构、许可状态和使用限制必须保留；原始语料只
     放私有 MinIO，不把许可证不明确或限制再分发的原文件提交到公开 GitHub。图像保存真实字节、来源标签
     和客观 `image_text`，不得从缺陷外观虚构 Recipe、机台或根因。
   - 冻结评测治理契约：数据集必须标识 `development/calibration/regression/holdout`，记录密封、首次打开、
     来源快照和泄漏审计状态。既有 `demo-v1/v2`、意图 v1/v2/v3 和九格式黄金文件保留为演示/回归证据，
     不再表述为未知文件或未知问题的泛化证明。
   - 验收：通用契约测试、未知元数据、未支持格式、危险压缩包、跨文件标签、来源更名、未见文件结构和未见
     问法均不能触发演示默认值或来源特化；九格式、权限、版本、下架/恢复和既有 API 保持兼容。
4. `T9-4.4.6d` 批量入库、密封盲测与演示验收：
   - 只使用 `T9-4.4.6c` 的通用 API/Worker 批量发布，核对 MongoDB 文档/Chunk/资产、MinIO 对象和 Milvus
     活动投影；重复任务幂等，失败、低质量或待复核文档不发布。
   - 建立开发集、校准集、回归集和密封盲测集。阈值、Top-K、Chunk/Prompt 等只能使用开发/校准集调整；
     `demo-public-v1` 与演示问题集只承担演示/回归，不作为无偏泛化指标。
   - 在参数与代码版本冻结后才打开密封文件和密封问题；记录 Parser/Chunker/Embedding/Reranker/索引/
     Prompt/检索配置及数据集 Hash。若查看盲测结果后修改实现或参数，必须创建新的盲测版本，不能在原集上
     继续调优并宣称测试提升。
   - 盲测覆盖未参与开发的文档结构、表格、图片集、多文件关系、问题改写、无证据、权限、版本和来源更名；
     单独报告解析、检索、回答、路由、权限以及数据泄漏检查结果。
   - 执行 `docs/半导体Agent智库演示问题集.md` 中标记为“完成 T9-4.4.6 后”的问题，验证文本、表格、
     图片、多文档综合、无证据、权限、版本、下架后不可召回和恢复后重新可用，但与盲测指标分开展示。
   - 在 ECS 工作台、任务中心、知识文档清单、Trace 和离线评估页完成真实点击、刷新、桌面与
     `390 x 844` 移动截图，并确认浏览器控制台无错误。

T9-4.4.6a 完成记录（2026-08-15）：已冻结 `semikb-source-manifest-v1`、来源许可/再分发/入库策略、
文档 revision 清单、`withdrawn`、下架/恢复请求、跨存储影响计数和补偿状态契约；旧入库记录通过可选
字段继续兼容。新增不可变 `source_manifests` 注册表和仅新增索引的版本化迁移。ECS 预演只包含 6 个
`create` 动作，正式应用后再次预演为 0；已有 35 条文档目录和 141 条审计事件未改写，六个受保护数据
容器 ID 不变。大型表格策略在模型层禁止 `raw_row_vectorization=true`。证据见
`docs/T9-4.4.6a来源与生命周期契约及迁移验收.md`。本阶段没有实现文档管理 REST API、RBAC 操作、
Milvus 清理、MinIO 恢复或前端知识文档页；下一步只能在用户明确确认后进入 T9-4.4.6b。

T9-4.4.6b 完成记录（2026-08-16）：已实现知识文档/revision 管理、管理员 RBAC、下架/恢复 REST API、
MongoDB 先阻断、Milvus 精确清理与读回确认、MinIO 原件保留、补偿重试、恢复 Hash/审批/有效期/权限/
索引版本校验和前端知识文档视图。ECS 迁移预演只有 4 个 `create`，应用后再次预演为 0；真实 5 Chunk/
2 图片文档完成 `published -> withdrawn -> published`，下架期间检索不可见且图片 HTTP 403，恢复后重新
可用，原件 Hash 不变。重复 Worker 投递不能并发发布，六个受保护数据容器 ID 不变，后端全量
`337 passed, 1 warning`。证据见
`docs/T9-4.4.6b受控下架恢复与知识文档管理验收.md`。2026-08-16 通用性与数据泄漏审计确认：下一步仍
属于 `T9-4.4.6c`，但必须先执行 `4.4.6c-1` 通用化加固，再执行 `4.4.6c-2` 公开语料标准化；不得先为
候选来源编写特化处理，也不得跳到批量入库、密封盲测或 T9-4.5。

T9-4.4.6c-1 完成记录（2026-08-16）：公共 Actor、上传、Document、Chunk、图片和表格契约已移除演示
Scope、Fab、Product、审批和发布默认值；新上传默认 `draft + staged`，Demo 值由显式 Demo Factory 注入。
首次发布与恢复现在共享 Source Manifest 门禁，并校验审批、Scope、许可/再分发、Hash、数据集版本和实际
索引产物。受保护证据使用 `retrieval_policy`，HyDE/图片加权使用结构化 `retrieval_mode`，不再依赖文档 ID
前缀或演示问法。评测数据新增用途、密封、首次打开、来源快照和泄漏状态，运行冻结对应快照；旧 Demo /
意图数据 Hash 保持不变。ECS 的未知元数据文件经真实 API/Worker 进入 MongoDB/MinIO 暂存但未进入活动
Milvus 投影，既有六个受保护数据容器 ID 不变。证据见 `docs/T9-4.4.6c-1通用化基线验收.md`。下一步只能
在用户明确发出开发指令后进入 `T9-4.4.6c-2`，不得下载后为候选来源编写特化分支，也不得提前进入
`T9-4.4.6d` 或 `T9-4.5`。

T9-4.4.6c-2 完成记录（2026-08-16）：已建立通用 `CorpusStandardizationService`、严格 Corpus/
Sidecar/Profile/Manifest 契约、不可变 MinIO 快照、ZIP 安全展开、统一 Parser Dispatcher、有界表格画像、
图片客观 `image_text`、显式跨文件关系、质量警告和 MongoDB 暂存审核真相。管理员 API、Celery Worker
和任务中心“语料标准化”页面均已接通。UCI SECOM ZIP、arXiv 论文 PDF 和浏览器临时 Markdown+CSV
通过相同入口，未出现来源专用分支。真实两条公开资料任务使 `corpus_standardization_jobs` 从 0 增至 2，
但 `document_catalog=36`、`chunk_catalog=87`、Milvus 活动行 `123` 均不变；公开原件未提交 Git。
迁移首次只有四个批准索引，应用后复验为 0 个动作，六个受保护数据容器 ID 不变。证据见
`docs/T9-4.4.6c-2通用语料标准化验收.md`。下一步只能在用户明确发出开发指令后进入
`T9-4.4.6d`，不得提前发布公开语料、打开密封盲测或进入 `T9-4.5`。

T9-4.4.6d 完成记录（2026-08-16）：已新增受治理 Corpus 批量发布 API/Worker、跨存储对账与补偿、
development/calibration/regression/holdout 四类数据和不可变发布冻结。UCI SECOM 发布 2 项/18 Chunk，
arXiv 2411.11029 发布 1 项/37 Chunk，私有混合 holdout 发布 4 项/11 Chunk；三批均使用 4.4.6c 的通用
标准化和发布路径。冻结后首次打开的有效 v2 holdout 通过率为 5/6，唯一失败是无证据问题误命中表格
画像；该结果保留为真实缺口，没有在已打开数据上继续调参。首次 v1 因字符编码损坏和 Actor Scope
不匹配被标记为无效运行。公网任务中心、离线评估、内部受控公开语料检索、首图自动预览、Trace、刷新
恢复及桌面/移动点击通过。浏览器验收发现并通用修复公开语料路由和“不要使用 Web”否定语义，默认
意图目录升级为 v4，未加入来源名或演示问题特例。六个受保护数据容器 ID 不变。证据见
`docs/T9-4.4.6d批量发布密封盲测与演示验收.md`。T9-4/T9 仍未完成；下一步只有在用户明确发出开发
指令后才能进入 `T9-4.5` Provider 韧性。

T9-4.7 完成记录（2026-08-23）：真实冷备归档 190,703,528 bytes，源数据 521 个文件/
633,361,069 bytes，从停写到全栈健康恢复 67 秒。恢复副本使用独立数据根和 `semikb-restore-t947`
Compose Project，不发布端口；MongoDB 完整 BSON、Milvus Strong 行数与元数据、MinIO 全对象内容 Hash、
Redis 队列和当前 `demo-v2` 检索烟雾均与备份点一致。篡改校验和、非空目标和线上目标三类保护均实测
拒绝。清理后恢复容器/网络/数据根/环境均为 0 或不存在，线上六个数据容器 ID 与跨存储指纹不变。
证据见 `docs/T9-4.7冷备恢复验收.md`。T9-4/T9 仍未完成；下一步只有在用户明确发出开发指令后才能
进入 `T9-4.8` 安全与可迁移部署。

T9-4.8 完成记录（2026-08-23）：生产 Demo Token 改为服务端固定角色/Scope，生产 seed 入口关闭；
JWT、RBAC、跨用户线程隔离、访问码限流、短时图片链接和凭据精确扫描通过。Nginx 不再记录 query string
或 Referer，API/Worker/Web 均为非 root、只读根文件系统、`cap_drop=ALL` 和
`no-new-privileges=true`。固定提交 `0f4b57806fc584867229edbe116d0e5b4bdc8e0f` 的 10 个镜像生成
3,813,758,976 bytes 离线包，删除 `semikb-offline/*` 后仅凭归档重载并逐项匹配 Image ID。六个数据
容器 ID、MongoDB/Milvus/MinIO/Redis 指纹和检索烟雾不变。证据见
`docs/T9-4.8安全与可迁移部署验收.md`。T9-4/T9 仍未完成；下一步只有在用户明确发出开发指令后才能
进入 `T9-4.9` 最终演示验收。

T9-4.9 完成记录（2026-08-23）：新增一键最终验收入口、9 场景 SSE/路由/证据/图片/多任务验收、
安全与离线包聚合结论及操作手册。最终处理中通用修复了受控语料有界预览被表述成完整总量，以及
证据追问进入 L0 后丢失上一轮 SOP 任务语义的问题；冻结数据和 Hash 未修改。后端全量
`445 passed, 1 warning`，Ruff、前端 ESLint/TypeScript/Vite build 均通过。ECS 最终总门禁的
`final_demo/security/offline_bundle/storage_invariants/worker/health` 全部通过，存储差异为空；公网
桌面和 `390 x 844` 移动点击、首图自动预览及 Console 0 error/warning 通过。离线包固定提交
`1d1d1b604b926709c865cea54465faa58e93cd3b`，含 10 个唯一镜像，`images.tar` 为
3,825,176,576 bytes。证据见 `docs/T9-4端到端验收报告.md` 和 `docs/evidence/t9-4/`。T9-4 与 T9
已达到可重复面试 Demo 的验收标准；HTTP、公网 SSH 密码登录和 MongoDB 单节点仍是正式生产阻塞项。

T9-4.10 完成记录（2026-08-23）：修复页面级通知与固定工作台高度叠加导致输入区超出视口的问题；
澄清状态不再重复显示绿色通知，黄色栏只在 `waiting_for_clarification` 期间存在。工作台在桌面使用
`100dvh` 剩余空间，消息区独立滚动；窄屏保持工作区与证据区可访问。输入框禁用浏览器右下角原生
缩放点，新增展开/收起图标按钮，展开时保留输入内容并向消息区获取空间。本地前端 lint/build 通过；
公网桌面 `1440 x 900` 和移动 `390 x 844` 实际点击、DOM 尺寸、截图及 Console 均通过。ECS 只重建
Web，MongoDB、Milvus、双 MinIO、etcd 和 Redis 容器 ID 全部不变。证据见
`docs/T9-4.10工作台视口与输入器交互优化验收.md` 和 `docs/evidence/t9-4-10/browser-acceptance.json`。

T9-4.10.1 完成记录（2026-08-23）：修复收起状态下展开按钮与发送按钮在部分浏览器缩放和可用高度下
重叠的问题。两个按钮改为单一横向操作组，统一 `32 x 32px`、间距 `6px`，textarea 右侧预留
`86px`。本地 lint/build/diff 检查通过；公网桌面、用户实际 Chrome 视口和 `390 x 844` 移动端完成
真实点击、DOM 矩形、截图和 Console 验收。ECS 只重建 Web，六个受保护数据容器 ID 不变。证据见
`docs/T9-4.10.1输入器按钮布局修复验收.md` 和 `docs/evidence/t9-4-10-1/browser-acceptance.json`。

香港迁移收尾状态（2026-08-26）：最终离线包已形成香港、深圳和本机三份副本，本机四项 Hash 复验
通过；`semikb-hk-observation.service` 已从 23:13:50 开始执行每 5 分钟一次的 24 小时采样。HTTPS
受 Let’s Encrypt 二次 CAA 查询超时阻塞，超过 DNS 六小时窗口后的受控重试仍复现；下一步先增加显式
CAA 授权或使用受控替代签发路径，再验证 443、双域名跳转、SSE 和续期。完整 24 小时样本汇总前不得
标记最终 Go，深圳 ECS 不由项目自动释放。

香港迁移观察完成记录（2026-08-27）：24 小时观察覆盖 86,589 秒、286 个五分钟样本，本机/域名 HTTP
Health 失败、异常/不健康容器状态、OOM 和容器重启均为 0；根盘峰值 28%，最低可用内存约 5.20 GiB。
因此香港 ECS 的 HTTP 稳定性门禁已通过。全部样本均未监听 443，HTTPS 仍因外部 DNS/CA 校验阻塞而
保持 No-Go；下一步只处理证书签发及 443、双域名跳转、TLS、HSTS、SSE 和续期验收。深圳 ECS 未释放、
未修改，仍由用户自行决定后续释放时间。

审计门禁：T2、T3 的全部缺口与验收测试已经完成，T4 真实入库开发门禁已解除。逐项证据与
完成标准见 `docs/T1-T3缺口审计.md`；真实验收证据见 `docs/T4真实入库验收.md`、
`docs/T5真实检索验收.md`、`docs/T6真实Agent验收.md`、`docs/T7离线评估验收.md` 和
`docs/T8浏览器验收.md`。
