# T9-4.11 意图语义接地与知识证据降级验收

## 1. 验收结论

T9-4.11 已完成。一般知识、动态数据和内部受控知识不再由单个关键词决定路由；结构化 LLM 输出必须
通过原文接地、任务形态和条件槽位校验。知识请求先走内部 RAG，再由证据充分性和治理范围决定是否允许
一次受控 Web 降级。

香港 ECS 只重建 API、Worker、Web；MongoDB、Milvus、双 MinIO、etcd 和 Redis 的容器 ID 均未改变。
深圳 ECS 未连接、未读取、未修改。

## 2. 实现范围

1. 新增理解语义框架：时间属性、期望输出、知识范围、任务形态、分组维度和可核验原文片段。
2. 新增服务端语义接地：没有当前消息或合法历史原文支撑的任务不会进入执行计划；完整首轮问题保留原文。
3. 新增意图目录 v5：概念解释、指定实体、聚合排行、事件列表、趋势和调查使用不同条件槽位。
4. 新增证据充分性：输出 `sufficient/partial/insufficient`、稳定原因码、覆盖率、来源和是否允许 Web。
5. 调整检索图：内部混合检索与 Rerank 完成后评估证据；公开通用知识不足时最多搜索一次 Web，内部受控
   SOP/Recipe 不允许被外部资料替代。
6. 新增 132 条冻结评审集 `semikb-intent-v4`，并保留 v1/v2/v3 内容和 Hash；旧路由只通过版本化覆盖层
   迁移，不修改历史文件。

## 3. 本地质量门禁

- 后端全量：`462 passed, 1 warning in 1123.19s`。
- Ruff、compileall：通过。
- 前端 ESLint、TypeScript、Vite build：通过，构建 1583 modules。
- v4 冻结评审集：132 条、无重复，确定性精确基线通过，`history_direct` 新产生数和危险执行漏拒数均为 0。
- v1/v2/v3：内容和 Hash 未修改；结构性路由差异由 v4 迁移覆盖层解释。

这里的 v4 满分是“生成后冻结的评审集精确回归”，不是独立人工盲测准确率，不能作为生产效果宣传。

## 4. 香港 ECS 真实验收

### 4.1 路由与槽位

| 问题 | 实际结果 | 结论 |
| --- | --- | --- |
| 半导体检测流程一般包含什么呢？ | `knowledge_query / concept_explanation`，无澄清 | 不再索要 Product、时间和设备 |
| 最近有哪些产品良率低？ | `clarify`，仅缺 `time_range` | Product 是分组维度，不是过滤槽位 |
| 最近24小时有哪些产品良率低？ | `tool_only`，执行聚合工具 | 有边界数据问题不走知识查询 |
| ETCH-03 Chamber B 清腔后首片异常，当前 SOP 怎么要求？ | `internal_rag` | 受控 SOP 不走 Web |

浏览器中对无边界良率问题补充“最近24小时”后，原任务正常恢复并执行工具，没有再次索要 Product 或设备。

### 4.2 证据充分性与 Web

一般检测流程 Case 的内部证据被判定为 `insufficient`，治理范围为 `public_general`，因此触发一次
`web_fallback`。阿里云 Web MCP 的 Provider 尝试返回 `succeeded`，但网关处理后可用外部证据为 0；
系统保留内部证据、未知项和下一步，没有把空外部结果包装成引用。

内部 SOP Case 被判定为 `sufficient/internal_controlled`，`web_fallback_allowed=false`，Trace 路由中没有
`web_fallback`，外部证据数为 0。

### 4.3 SSE 与浏览器

- SSE 依次出现 `analyzing_request`、`routing_request`、`retrieving_evidence`、`reranking_evidence`、
  `assessing_evidence`、`searching_external`、`generating_answer`、`verifying_answer` 和
  `persisting_result`；收到 4 个 Delta、1 个 completed、0 个 error。
- 桌面 `1440 x 900` 完成知识、澄清恢复、内部 SOP 和 Trace 点击。
- 移动 `390 x 844` 的 `scrollWidth=390`，无横向溢出，输入器完整可见。
- 刷新后问题、澄清和工具结果均恢复；Console 0 error/warning。

## 5. 数据与运行不变量

- 受保护容器 ID：六项全部保持不变。
- 知识内容：MongoDB 44 个文档、154 个 Chunk、15 个图片资产、19 个表格资产未变化。
- Milvus：Active Collection `semikb_chunks_v4`，Strong 149 行未变化。
- MinIO：raw 56、derived 91 个对象未变化。
- Redis：Ping 成功，Celery 队列为 0；Worker 返回 `pong`。
- 验收只新增线程、请求、Checkpoint 和 Trace 等审计记录。
- API/Worker 日志在验收窗口内没有 Traceback、Exception 或 Error；HTTP Health 为 `ok`。

## 6. 已知边界

1. Web Provider 成功不代表一定得到可引用资料；本次公开知识 Case 的可用外部证据为 0，系统已正确降级。
2. 当前制造数据工具是只读模拟工具，聚合结果证明了路由和调用闭环，不代表已接入真实 MES/YMS/FDC 数据。
3. HTTPS 仍是香港迁移任务的独立阻塞，不属于 T9-4.11；当前验收入口为 HTTP。
4. 当前结果证明面试 Demo 和回归边界，不代表生产准确率或正式生产上线。

完整脱敏证据位于 `docs/evidence/t9-4-11/`。
