# 开发记录

## 2026-08-11 - T1 工程初始化

- 目标：创建独立仓库、根级环境配置约定、基础文档和测试入口。
- 变更：新增 Python/React 目录结构、`.env`、`.env.example`、`.gitignore`、项目说明和第三方声明。
- 数据结构：定义 Milvus、MongoDB、MinIO 方案文档作为后续实现契约。
- 测试：`.env` 已由 Git 忽略规则验证。
- 遗留：等待真实服务连接参数、镜像版本、MinerU/LLM/MCP Key。

## 2026-08-11 - T2/T3 存储契约与公开演示数据

- 目标：固定三类存储职责、连接前置检查与可公开展示的数据基线。
- 变更：新增 Pydantic 契约、Mongo/Milvus/MinIO/Redis 预检、资源初始化命令、SOP 历史/现行版、Case 图像元数据、FDC 规则与 `demo-v1` 黄金集。
- 数据结构：Milvus HNSW/Sparse 索引版本、MinIO 私有 Bucket、MongoDB Catalog/Trace/Thread/Checkpointer Collection 索引已文档化。
- 测试：模拟数据每条携带版本、审批、Scope、Fab/Product/Tool 范围；真实预检未执行，因为 `.env` 仍为空。
- 遗留：需在填写 `.env` 后运行 `python -m semikb.storage.preflight` 和 `python -m semikb.storage.provisioning`。

## 2026-08-11 - T4/T5 入库与检索 Demo

- 目标：实现可复现的受控入库、混合检索和文本召回图片路径。
- 变更：实现 SHA-256 幂等 Job、Markdown/二进制文件上传 API、Markdown 语义分块、质量门禁、MinerU Precision API 适配器、版本/权限过滤、Dense/Sparse/RRF、Rerank、动态截断和图片重鉴权。
- 数据结构：`RetrievalTrace` 绑定 `actor_user_id`，避免 Trace 跨用户可见；图片访问继承所属 Document 的 Fab/Product/Tool 范围。
- 测试：验证历史 SOP 不可检索、图文召回有效、Scope 不匹配不能访问图片、上传训练文档可缺省非必填工艺字段。
- 遗留：真实 BGE-M3、Reranker、Milvus 写入和 MinIO 预签名链接待外部服务配置。

## 2026-08-11 - T6/T7 对话与评估

- 目标：让 Agent 在信息不足时受控追问，并对检索质量可复现地评估。
- 变更：加入 LangGraph 约束分支图、同一 `thread_id` 连续消息、最多两轮澄清、模拟只读制造数据工具、受控 Web MCP 网关、Trace 和黄金集评估服务。
- 数据结构：Demo 使用 LangGraph 内存 Checkpointer；生产目标 MongoDB Collection 已预留为 `checkpoints` 和 `checkpoint_writes`。
- 测试：验证关键字段缺失不执行检索，同一线程补充字段后生成证据回答；`demo-v1` 输出 Recall@5/MRR。
- 遗留：MongoDB Checkpointer 适配和实际 MCP/API Key 连通性待真实环境验收。

## 2026-08-11 - T8/T9 前端、容器和验证

- 目标：完成四个产品亮点界面及可部署工程物。
- 变更：实现 React/Vite 工程师工作台、证据抽屉、可解释 Trace、入库任务中心、离线评估页；新增 Dockerfile、Nginx 反向代理、开发/生产 Compose、兼容矩阵和演示脚本。
- 测试：`pytest` 9 项通过；`ruff check src tests` 通过；`npm run build` 通过；`docker compose config` 通过；运行中的 API 和 Vite 前端均完成健康检查。
- 遗留：Docker 镜像实际构建暂被本机未启动的 Docker Desktop Linux daemon 阻塞；另需阿里云 `docker ps` 版本核对、真实存储预检以及空存储到完整问答的端到端验证。

## 2026-08-11 - T2 外部服务配置映射与预检

- 目标：复用旧知识库的可用基础设施配置，为真实存储适配阶段准备连接参数。
- 变更：将旧项目的 MongoDB、Milvus、MinIO、MinerU、DashScope LLM/MCP、BGE-M3 配置映射至根目录 `.env`；该文件持续被 Git 忽略。
- 设计决策：MongoDB 使用新项目独立数据库 `semikb`，MinIO 使用 `semikb-raw` 和 `semikb-derived`，不复用旧项目业务数据或 Bucket；MinerU Base URL 规范为主机地址，由适配器拼接 `/api/v4`；当前保留 `DEMO_MODE=true`，直到真实 Repository 适配器完成。
- 验证：MongoDB、Milvus、MinIO 连接均通过预检；BGE-M3 本地路径存在。
- 遗留：缺少 Redis URL；旧 Reranker 路径不存在；Web 搜索域名白名单未定义；LLM、MinerU、MCP 尚未做实际调用验收。

## 2026-08-11 - T2 MongoDB、Milvus、MinIO 真实资源初始化与验收

- 目标：完成 T2 中不依赖 Redis 的真实存储资源创建和客户端烟雾验收。
- 变更：在独立 MongoDB 数据库 `semikb` 创建 14 个业务 Collection 索引；创建私有 `semikb-raw`、`semikb-derived` Bucket；创建 `semikb_chunks_v1` Milvus Collection。
- 数据结构：Milvus 已确认包含 1024 维 `dense_vector`、`sparse_vector`、版本/权限/有效期/Fab/Product/Tool/Recipe 过滤字段，并建立 Dense HNSW 和 Sparse Inverted Index。尚未建立或切换 `semikb_chunks_active` Alias，避免空索引作为活动版本。
- 验证：MongoDB 14 个 Collection 均具有预期索引；两个 Bucket 均存在且没有公开 Bucket Policy；Milvus Collection 和两项索引存在；第二次执行初始化未新建 Bucket，Collection 状态为 `already_exists`。
- 遗留：Redis 客户端预检待 Redis 安装后补齐；实际业务 Repository 写入、向量写入和首个发布 Alias 切换属于后续 T4/T5 验收范围。

## 2026-08-11 - T2 Redis Docker 部署与验收

- 目标：在不改动既有 MongoDB、Milvus、MinIO、Attu 的前提下，为 Celery 部署 Redis broker/result backend，并补齐 T2 外部服务预检。
- 变更：新增 `deploy/redis/docker-compose.redis.yml` 和 `docs/Redis部署说明.md`；在 CentOS `/opt/semiconductor-agent-knowledge-base/` 创建独立 `semikb-redis` 容器、`semikb-network` 网络和 `semikb-redis-data` 数据卷；根目录 `.env` 已写入项目专属 `REDIS_URL`，且持续由 Git 忽略。
- 安全与边界：Redis 仅绑定 `192.168.10.100:6379`，启用随机专用密码、`protected-mode yes`、AOF、`appendfsync everysec` 和 `unless-stopped`；服务器 `.env` 权限为 `600`。未修改任何既有基础设施容器、网络、卷或配置。
- 验证：Redis 7.4.10（`redis:7.4-alpine`，镜像摘要 `sha256:e7723ff73d963f5cc6d9c4643ea3d989527a402a319239054e9472a7fb9219a2`）状态为 `healthy`；密码认证返回 `PONG`，未认证连接被拒绝；本机 `semikb.storage.preflight` 对 MongoDB、Milvus、MinIO、Redis 均验证连通；`pytest` 9 项、Ruff 与 Redis Compose 配置检查全部通过。
- 遗留：MinerU、LLM、阿里云 MCP 尚未进行真实调用验收；真实业务 Repository 写入、向量写入和首个发布 Alias 切换仍属于后续 T4/T5 验收范围。

## 2026-08-11 - T1/T2/T3 完成状态复审

- 目标：停止依赖历史状态标签，按原始要求、实际产物、自动测试和真实运行四层重新验收 T1/T2/T3。
- T1 结论：保持“已完成”。独立 Git 仓库和远程地址有效，`.env` 已忽略且未跟踪，受控密钥扫描无命中；API 健康检查返回 `200/ok`，前端生产构建成功，现有 9 项测试通过。
- T2 结论：由“已完成”降为“部分完成”。四类数据存储均真实可连接，两个 MinIO Bucket 私有，Milvus Collection/索引存在，Redis 7.4.10 健康；但缺失配置提示与状态矛盾，没有可重复的资源结构核验命令和专项测试，MongoDB 多个索引被错误地合并为复合索引，Milvus 非空字段与 Pydantic 可选字段尚未形成明确写入契约。
- T3 结论：由“已完成”降为“部分完成”。现有数据只包含 SOP R1/R2、一个 Case、一条 FDC 规则、图像元数据和 3 条正向黄金问题；没有独立 Recipe 文档、真实图片文件、SPC 数据，也缺少显式来源/许可/Scope 与负向评估覆盖。
- 文档纠正：新增 `docs/T1-T3缺口审计.md`；纠正 README 的真实图片表述、样例数据说明和测试函数命名。
- 决策：T2、T3 完成缺口和专项验收前暂停 T4；任何 MongoDB 索引迁移或现有服务配置变更必须先获得用户审批。

## 2026-08-11 - T2 存储契约加固（索引迁移前）

- 目标：完成不需要服务器写操作的 T2 缺口，建立可重复、可脱敏、可失败退出的存储验收能力。
- 变更：新增统一 `StorageClientFactory`、明确的缺失变量诊断、MongoDB 目标索引契约、只读 `semikb.storage.verifier` 和 Milvus 非空行规范；Provisioning 遇到既有索引差异时默认停止。
- 验证：新增 8 项 T2 专项测试并全部通过；真实 preflight 四类存储均通过；verifier 对 MinIO、Milvus、Redis 全部通过，并准确报告 MongoDB 索引差异。
- 安全边界：本轮未创建、删除或修改任何服务器索引、Collection、Bucket、Alias、容器或配置。14 个 `semikb` Collection 当前文档数量均为 0。
- 遗留：T2-G4 MongoDB 索引迁移等待用户审批；迁移方案已记录在 `docs/T2-MongoDB索引迁移方案.md`。

## 2026-08-11 - T3 演示数据补齐与复验

- 目标：补齐原计划中的独立 Recipe 多版本、真实合成晶圆图、SPC 数据、显式治理字段和完整黄金集，不再以图像元数据或代码默认值代替数据产物。
- 数据：语料扩展为 7 个版本化文档；新增 Recipe V2.2/V2.3 版本链、Edge-ring/正常对照两张 320×320 PNG、120 行 Pressure SPC CSV 和 5 个 OOC upward-shift 标签。
- 契约：DocumentRevision 显式保存 source kind/URI/license、Scope 和 supersedes；ImageAsset 保存受控 Demo 文件路径与真实字节哈希；EvaluationCase 支持 `evidence`/`no_evidence` 和失败标签。
- 产品路径：图片预览接口在完成权限复核后返回实际 PNG 文件；无本地资产的上传记录仍保留受控占位预览。
- 黄金集：保留 `demo-v1` 历史基线，新增并默认使用 8 条 `demo-v2`，覆盖 SOP、Recipe、SPC、图片、FDC、过期证据、产品越权和无答案路径。
- 验证：新增数据质量测试，验证来源/权限字段、ID 唯一、版本链、PNG 签名、SHA-256、SPC 字段/OOC 数量及黄金集 Chunk 引用；T3 针对性测试 13 项通过。
- 状态：T3 已达到原始验收要求并标记完成；T4 仍由 T2-G4 MongoDB 索引审批门禁阻塞。
- 本地运行说明：最终复验使用 Conda `dl` 环境（Python 3.12）；当前 PowerShell 默认 Python 3.13 未安装 LangGraph，不作为本项目验收解释器。

## 2026-08-11 - T2-G4 MongoDB 索引迁移

- 目标：在不修改服务器配置和其他项目资源的前提下，修正 `semikb` 早期初始化产生的复合索引与唯一性错误，完成 T2 存储契约门禁。
- 代码：新增 `semikb.storage.mongo_index_migration` 专用命令，默认 dry-run；强制校验数据库名、空 Collection 和已批准旧索引，迁移前保存本地 JSON 快照，异常时自动恢复旧索引。普通 provisioning 不再提供绕过迁移门禁的参数。
- 真实执行：迁移前 14 个目标 Collection 均为 0 条文档；在 10 个有差异的 Collection 上删除 8 个旧索引并创建 16 个目标索引，未触碰另外 4 个正确的 Collection。
- 验证：迁移后完整 verifier 对 MongoDB、MinIO、Milvus、Redis 全部通过；二次 dry-run 为 0 个动作；14 个 Collection 文档数仍全部为 0；T2 存储契约专项测试 16 项、全量后端测试 32 项、Ruff 和前端生产构建均通过。仅保留 1 条 Starlette TestClient 第三方弃用告警。
- 安全边界：只修改 `semikb` 索引元数据；未修改 MongoDB/Milvus/MinIO/Attu/Redis 的容器、端口、网络、数据卷或配置，未访问其他 MongoDB 数据库。
- 状态：T2-G1 至 T2-G6 全部完成，T2 标记为已完成，T4 真实入库开发门禁解除。

## 2026-08-11 - T4 真实知识入库闭环

- 目标：把原有同步 Demo 入库扩展为可重放、可补偿、可审计的真实异步入库链路，并完成 MongoDB、MinIO、Milvus、Redis/Celery 和本地 BGE-M3 的闭环验收。
- 代码：新增 `IngestionStore` 边界及 MongoDB、MinIO、Milvus 生产 Repository；API 在真实模式只提交任务，Celery Worker 处理解析、语义分块、图文资产、BGE-M3 Dense/Sparse、暂存和发布；DemoStore 同步实现相同协议。Celery App 显式加载 `semikb.workers.tasks`，避免 Worker 启动后无法识别任务。
- 幂等与恢复：原件先按 SHA-256 稳定路径保存，Job 通过唯一幂等键原子创建，可重放 payload、原件和解析件均持久化；重启服务后仍能从失败阶段发起第 2 次尝试，事件按 `event_id` 幂等镜像。
- 质量与补偿：MinerU ZIP 只提取 `full.md` 和被 Markdown 引用的图片；分块和图注进入质量门禁；任何异常都会隔离本 Revision 并精确删除对应向量。Celery 投递失败返回 503，并把 Job 置为可重试失败态，不会永久停在 queued。
- 索引版本：真实写入发现早期 `semikb_chunks_v1` 缺少 `lifecycle` 字段，无法表达 staged/published 隔离。未删除或修改 `v1`，而是创建 `semikb_chunks_v2`，完成读回校验后将 `semikb_chunks_active` 切到 `v2`。
- 真实验收：两条 CC0 合成资料产生 2 个 Document、5 个 Chunk、1 个 Image、2 个 Job、20 条一致事件和 5 个 MinIO 对象；5 条 Milvus 记录全部为 `published`。注入一次 Embedding 失败后跨服务实例第 2 次成功，连续重复执行脚本未增加 Job、Chunk 或对象。
- 自动验证：Ruff 通过；新增生产入库、队列失败和 Celery 任务注册测试；真实 verifier 对 MongoDB、MinIO、Milvus、Redis 全部通过。另启动独立 `solo` Worker，经真实 Redis 消费一条合成 SOP，任务第 2 次尝试成功发布；验证后已精确清理该临时 Job、Chunk 和对象并停止 Worker。完整命令和证据见 `docs/T4真实入库验收.md`。
- 安全边界：只写入 `semikb` 数据库、`semikb-raw`/`semikb-derived` 和 `semikb_chunks_v2` 项目资源；没有改动任何基础设施容器、网络、端口、卷或其他项目数据。
- 遗留：MinerU 外部 API 的真实 PDF 调用与 T5 真实 Reranker/混合检索仍按后续 Task 独立验收，不计入 T4 完成范围。

## 2026-08-11 - T5/T6 LLM 主备 Provider 前置能力

- 目标：在 T5 HyDE 与 T6 Agent 生成能力接入前，固定文本模型选择和故障降级边界。
- 代码：新增 OpenAI 兼容 LLM 网关，配置 `gpt-5.6-luna` 为 CloseAI 主模型、`qwen-flash` 为 Qwen 降级模型；Provider 分别生成兼容的 Token 和推理参数，主模型失败时按显式配置降级，异常不暴露 Key 或 Endpoint。
- 配置：旧项目 CloseAI 配置已迁移到根目录受 Git 忽略的 `.env`；原有通义千问配置保留并映射到 `QWEN_*`。`.env.example` 仅保存无密钥模板，原 `LLM_*` 变量只作为 Qwen 兼容别名。
- 真实验证：项目网关调用 CloseAI 返回 HTTP 200、有效 JSON 和上游报告模型 `gpt-5.6-luna-2026-07-09`；Qwen 降级端独立调用返回 `qwen-flash`。已确认 Luna 支持 `reasoning_effort=none`、`verbosity=low`、`response_format=json_object` 和 `max_completion_tokens`，不发送其拒绝的 `temperature=0` 与 `max_tokens`。
- 安全边界：第三方代理当前仅允许处理模拟数据和公开资料；真实工厂知识接入前必须完成数据留存、日志、模型映射、地域与 SLA 审查。
- 遗留：本次不改变现有 Demo 对话和检索结果；T5 后续将单独接入真实 Dense/Sparse/RRF/Reranker/HyDE 并做黄金集基线对比，T6 再把网关接入 Agent 答案生成。

## 2026-08-12 - T5 真实混合检索闭环

- 目标：把非 Demo 模式从“真实入库但检索仍读取空 DemoStore”升级为真实生产检索链，并冻结 T6 可依赖的证据与 Trace 契约。
- 代码：新增生产 Retrieval Repository 与 Service；在 Milvus 搜索前执行批准状态、生命周期、生效期、ACL 和结构化工艺约束过滤，使用本地 BGE-M3 Dense/Sparse、条件 Luna HyDE、RRF、qwen3-rerank、最低相关度、断崖检测和受保护证据选择；MongoDB 回填正文并进行二次权限校验。
- 图文闭环：`image_text` 命中后只在 Trace 保存稳定 `image_id`；资产 API 同时校验 Document 与 Image 生命周期和权限，再签发五分钟 MinIO URL。真实 PNG 返回 200，无权产品返回 403。
- 工程修复：统一 PyMongo 无时区 UTC 与业务带时区时间，避免真实二次权限校验抛出比较异常；生产 API/Worker 共用单个 BGE-M3 实例，避免重复加载权重。
- 评估：新增 `t5-live-v1` 七条合成 Case 和四组真实基线。Dense/Sparse/Reranker 达到 Recall@5=1.0、MRR=0.7143、无证据识别=1.0、平均 954.21 ms；强制 HyDE 指标不变、平均 5314.08 ms，因此保留条件启用。
- 验证：55 项测试、Ruff、四类存储 verifier、真实 FastAPI 检索/Trace/图片权限及在线黄金集全部通过。完整证据见 `docs/T5真实检索验收.md`。
- 安全边界：没有修改任何基础设施服务配置或其他项目数据；在线指标仅来自 7 条合成 Case，不包装为真实 Fab 效果。
- 下一步：重新阅读架构、产品、计划与开发记录，以 T5 已冻结接口设计 T6；T6 未验收前不改变本条完成结论。

## 2026-08-12 - T6 真实 Agent 对话闭环

- 目标：把三节点内存 Demo 升级为可持久化暂停/恢复、可追问、可审计且受证据约束的真实 LangGraph 调查流程。
- 图与线程：新增 `CaseState` 受限主图，使用真实 `interrupt()` 和 `Command(resume=...)`；最多两轮、每轮不超过 3 个问题，关键字段仍不足或 Product/Tool 越权时不执行 T5 检索和制造工具。`agent_threads` 管业务目录，官方 `MongoDBSaver` 独占 `checkpoints`/`checkpoint_writes`。
- 证据与回答：T6 只通过 T5 Service 获取受控 Chunk/Trace；统一建立内部 Chunk、模拟工具、白名单外部资料的证据账本。Luna 主模型、Qwen 降级生成结构化事实/假设/未知项/下一步，确定性校验移除无引用事实和在存在内部证据时仅由外部资料支持的事实，并禁止把假设表述成已确认根因。
- 工具与记忆：模拟 Yield/FDC/Recipe 工具使用 Pydantic `extra=forbid` 参数并保持只读，每次调用独立审计。长期记忆使用 LangGraph MongoDB Store；只允许显式批准写入，工程师仅可写偏好，稳定规则/Case 摘要需管理员，记忆不作为事实来源。
- 数据结构：新增 T6 专用索引迁移，默认 dry-run，只允许项目库 4 个空 Collection。真实执行删除 4 个旧占位索引并创建 4 个 Checkpointer/Store/审计索引；迁移前保存快照，完整 MongoDB/Milvus/MinIO/Redis verifier 通过，未修改任何容器或其他数据库。
- 真实验收：模糊问题先暂停；重建 ConversationService 后同线程恢复，产生 14 个 Checkpoint，命中 4 个内部 Chunk、1 张真实图片和 3 个只读模拟工具；约束抽取与答案生成均由 `gpt-5.6-luna-2026-07-09` 完成，引用全部闭合到证据账本，审计和长期记忆持久化成功。
- 自动验证：66 项后端测试、Ruff 和真实四类存储 verifier 通过；在线验收产生的线程、Checkpoint、Trace、审计和记忆均按临时 ID 清理为 0。完整证据见 `docs/T6真实Agent验收.md`。
- 安全边界：真实第三方模型只处理合成/公开资料；制造工具为模拟只读实现。本结果不代表真实 Fab 接入、MongoDB 高可用、OIDC 或生产 SLA 已完成。

## 2026-08-12 - T7/T8 完成状态重审

- 目标：纠正早期 Demo 被误标为生产完成的问题，恢复严格的 `T7 -> 文档复核 -> T8` 串行门禁。
- T7 缺口：现有评估服务仍以进程内 `DemoStore` 保存运行结果，API 同步阻塞执行；`baseline_run_id` 没有形成真实指标差异，缺少冻结的组件版本、失败 Case 下钻、生产权限契约和真实服务验收。
- T8 缺口：现有前端仍依赖早期 Demo 响应结构，尚未完整接入 T6 澄清/证据账本和 T7 基线对比/失败 Case；原有健康检查与构建通过不能代替真实浏览器交互验收。
- 决策：先完成 T7 的代码、测试、真实评估、文档、英文提交与推送；之后重新阅读计划、开发记录、架构设计和产品方案，再开始 T8。T8 必须亲自在浏览器中点击完整流程，并保存和检查桌面端、移动端截图。

## 2026-08-12 - T7 真实离线评估闭环

- 目标：把进程内同步 Demo 评估升级为可持久、可重现、可比较、可运营的生产评估能力。
- 数据与状态：新增不可变 `EvaluationDataset` 快照；同版本不同 Hash 拒绝覆盖。生产运行写入 MongoDB `evaluation_runs`，冻结数据集 Hash、检索配置、组件版本、Case 结果、失败标签、任务所有权和时间线，服务重建后仍可读取。
- 异步执行：API 在生产模式创建 `queued` 运行并向 Redis/Celery 投递，Worker 原子认领；同一 Celery task id 可恢复重投，其他任务不能并发认领。失败运行保留安全摘要并支持管理员重试。
- 评估内容：固定 `dense`、`hybrid`、`reranked`、`full` 四档，输出 Recall@5、MRR、nDCG@5、无证据准确率、图片 Recall@5、通过率、平均/P95 延迟；baseline 保存逐指标方向、差值和 improved/regressed/unchanged，并给 Case 标记 fixed/regressed/unchanged。
- 可解释性：每个 Case 保存预期/实际/缺失证据、候选路由/排名/RRF/Rerank、截断原因、告警和 `trace_id`；真实 Trace 使用 `evaluation:<run_id>:<case_id>` 关联并已核对 7/7 存在。
- 权限与 API：数据集、运行列表/详情、创建和重试全部要求 `knowledge_admin` 或 `admin`；真实 API 验收确认管理员得到 `202 queued`，工程师读取返回 403，Worker 完成后可持久读取。
- 真实结果：`t5-live-v1` 四档串行通过。`reranked` 与 `full` 的 Recall@5/MRR/nDCG@5/无证据准确率/图片召回/通过率均为 1.0；`full` 条件 HyDE 未改善这组 7 Case 的质量，平均延迟约 1618.74 ms，高于 `reranked` 的 933.87 ms，应保留条件启用策略。
- 验证：70 项后端测试通过，Ruff 通过，MongoDB/Milvus/MinIO/Redis verifier 全通过；真实 Celery/Redis、BGE-M3、Milvus、qwen3-rerank、条件 Luna HyDE、MongoDB 恢复和 API 验收通过。完整命令与运行 ID 见 `docs/T7离线评估验收.md`。
- 安全边界：未修改任何基础设施容器、端口、网络、数据卷、服务配置或其他数据库；只写入项目 `semikb` 的评估与 Trace 数据。指标仅来自 7 条合成 Case，不代表真实 Fab 效果。
- 下一步：重新阅读计划、开发记录、架构设计和产品方案，按 T5/T6/T7 最终契约确认 T8，再开始前端开发和强制浏览器点击/截图验收。

## 2026-08-12 - T8 四页面真实浏览器闭环

- 目标：严格在 T7 完成并复核计划、架构和产品文档后，把早期 Demo UI 升级为接入 T4/T5/T6/T7 最终契约的可操作工作台。
- 前端：重建共享 TypeScript 契约和 API Client；工程师工作台支持线程恢复、结构化追问、事实/未知项/下一步、证据账本和图片；Trace 支持多路召回、候选筛选、分数、排除原因、截断和耗时；入库中心支持真实 multipart 上传、阶段事件和重试；评估页支持数据集/Profile/Baseline、异步状态、指标 Delta、失败 Case 和专用 Trace 下钻。
- 交互修复：页面切换主动回到顶部并清理上一页提示；排队评估区分“运行中”与“没有失败 Case”；桌面侧边导航和移动端紧凑导航均使用同一功能入口。
- 真实浏览器验收：亲自完成模糊问题追问与同线程恢复、完整 Trace 筛选、MinIO 真实晶圆图打开、Markdown 文件选择与发布、Full 评估创建与失败 Case 下钻。移动端使用 390 x 844 视口复验，最终控制台 0 条 warning/error。
- 运行证据：浏览器上传任务 `ing_6c49f3cd027f4a8096c51116b5329f5d` 发布 3 个 Chunk；评估运行 `eval_d679b0f4c9ae42fab3e8319f8cd5db2f` 完成 7 个 Case。新增知识触发一个无证据负例回退，页面正确展示并可下钻，未覆盖 T7 冻结基线。
- 自动验证：前端生产构建通过；70 项后端测试通过；Ruff 通过；API/Worker/Vite 日志无异常。9 张桌面/移动截图和完整步骤见 `docs/T8浏览器验收.md`。
- 安全边界：只写入本项目资源和合成验收资料，未修改任何基础设施配置或其他项目数据。
- 状态：T8 已完成。下一阶段严格按计划进入 T9；开始前需重新审阅 T9 的部署输入、现有 Docker 产物、阿里云目标环境和剩余验收缺口，不直接沿用早期“Demo 完成”标签。

## 2026-08-12 - T9 前置：在线 Embedding 与 v3 索引迁移

- 目标：解决阿里云无 GPU 节点无法运行本地 BGE-M3 的部署阻塞，同时保持 Dense + Sparse + RRF 两阶段检索能力。
- 模型验证：使用既有 Reranker Key 调用 `qwen3.7-text-embedding` 原生 DashScope Endpoint，双文本返回 2 个 1024 维有限值向量；项目客户端进一步执行数量、维度、有限值和 L2 归一化检查。在线模型不返回 Sparse，因此新增无模型 `lexical-hash-v1`，以稳定 SHA-256 特征空间编码英文/数字术语及中文一元、二元片段。
- 代码：新增在线 Hybrid Encoder 工厂、独立 Embedding 配置与安全 Key 复用；移除 FlagEmbedding 和未启用的本地 BGE Reranker 分支；API、入库、检索、Trace、评估统一记录实际 Embedding/Sparse 版本。新增默认 dry-run 的索引迁移命令，执行全量重编码、Milvus 读回、自检索、Alias 切换、MongoDB 版本同步和异常回滚。
- 迁移：创建并校验 `semikb_chunks_v3`，重编码 3 个已发布 Document 的 8 个 Chunk；`semikb_chunks_active` 已切换到 v3，MongoDB Catalog 与 `index_releases` 已同步。`semikb_chunks_v2` 未修改、未删除，作为回滚投影保留。
- 缺陷修复：真实发布发现 PyMilvus `list_aliases()` 返回字典而旧代码按列表判断，导致错误调用 `create_alias`；新增跨版本响应归一化及回归测试后成功切换。T5 失败时脚本现在先打印 Case 明细再返回非零，保留可诊断证据。
- 质量门禁：T8 临时入库文档在迁移前后都使 CMP 无证据负例得到相同 Reranker 分数 `0.35609`；正向期望证据最低分为 `0.746104`。据冻结黄金集将最低阈值从 `0.35` 校准为 `0.40`，未删除测试文档。T5 Full 达到 Recall@5=1.0、无证据准确率=1.0；T7 Full Run `eval_822491ba04ac4145862b5edd320f5ac0` 的 Recall/MRR/nDCG/无证据准确率/图片召回/通过率全部通过。
- 资源审计：当前生产路径不再加载 Torch、FlagEmbedding 或任何本地权重。Embedding、Reranker、主备 LLM、MinerU 和 Web MCP 均为远程服务；本地仅有词法哈希算法与测试/Demo 的确定性编码器，不属于推理模型。
- 验证：存储 verifier 全通过；在线 T5 四档基线通过；真实 Redis/Celery Worker 完成 T7 四档评估并可由新 MongoDB Repository 恢复；全量自动测试、Ruff、前端构建和 Compose 配置见迁移验收文档。
- 状态：这是 T9 的部署前置变更，不将 T9 标记完成。下一步仍须按计划重新审计阿里云 Docker、镜像版本、生产 `.env`、网络出口、限流/SLA 和端到端部署验收。

## 2026-08-12 - T9 前置修正：Qwen 原生 Sparse 与 v4 受控发布

- 目标：修正首次 API 探测遗漏 `output_type=dense&sparse` 导致的错误结论，用 Qwen 原生 Sparse 替换 v3 词法 Sparse，同时保持无 GPU 部署和严格阶段门禁。
- Provider：SDK 示例和项目原生 HTTP 客户端均验证 Dense/Sparse 同时返回；新增数量、顺序、Dense 维度/有限值/归一化、Sparse 非空/索引/值/重复项校验。`dense` 模式仅保留 v3 回滚兼容。
- 迁移：把单步 `--apply` 拆为 dry-run、`--build` 候选构建和 `--publish` 独立发布。v4 构建后 Alias 仍指向 v3；T5/T7 影子验收通过才发布。当前 Alias 指向 v4，3 个 Document、8 个 Chunk 已同步；v3、v2 未删除。
- T4-T7 回归：真实 Worker 新文档产生 1 个 v4 Chunk 和 485 个非零 Sparse 项后精确清理；T5 Reranked/Full Recall@5 与无证据准确率均为 1.0；T6 连续线程、图片、工具、审计和 Checkpoint 通过；T7 Full Run `eval_8d3ed2f15ad046deafef8dd4f25d7e69` 的 Recall=1.0、MRR=0.9、nDCG=0.9302、负例/图片/通过率均为 1.0。
- T8 浏览器：重新点击结构化追问、连续调查、真实图片、Trace 排除项、任务中心和离线评估；390 x 844 工作台/Trace/评估无横向溢出。证据位于 `docs/evidence/qwen-sparse-v4/`。
- 缺陷修复：兼容结构化 `next_actions` 对象；修复 Trace 长组件键重叠；入库事件改为记录实际 Encoder 与 Sparse 版本；补齐迁移发布、物理 Collection、Provider 响应和事件文案测试。
- 工程加固：API 单测不再继承根目录生产 `.env`；补齐已有 `npm run lint` 缺失的 ESLint 工具链，并修复未使用函数和错误包装问题。首次全量测试误投递的 1 个临时 Document/Chunk/Job、4 个 Evaluation、12 个 Trace、2 个 MinIO 对象和 4 个 Redis Result 均按精确 ID 清理，最终恢复 3 个 Document、8 个 Chunk。
- 最终验证：87 项后端测试通过（仅 1 条 Starlette 第三方弃用告警）；Ruff、ESLint、TypeScript/Vite 构建、开发/生产 Compose 配置和四类存储 verifier 全部通过。浏览器验收结束后已停止本地 API、Worker 和 Vite 进程。
- 风险：一次 T6 重复调用未选择内部证据，随后相同检索和完整复跑正常；T9 必须增加远程 Reranker/Embedding 的重复性、限流、超时和降级验收。本次仍不将 T9 标记完成。

## 2026-08-12 - T9-1 部署前只读审计与 ECS 估算

- 目标：在购买阿里云 ECS 和修改生产部署产物前，固化当前 CentOS/Docker 服务事实、部署风险与 Demo 最小资源边界。
- 只读审计：通过已有 SSH 凭据读取 OS、CPU、内存、磁盘、Docker、容器、端口、网络和版本；审计前后 7 个容器 ID、镜像、状态和端口完全一致，未执行安装、停止、重启、拉取镜像或配置修改。
- 当前环境：CentOS 7、4 vCPU、约 4 GiB、约 35 GiB 根磁盘；Docker 26.1.4、Compose 2.27.1；MongoDB 8.2.6、Milvus 2.5.5、etcd 3.5.18、双 MinIO、Attu 2.5.10、Redis 7.4.10 均在运行。当前内存低于 Milvus 8 GiB 下限，只保留作内网开发验证。
- 风险：CentOS 7 EOL；MongoDB 使用可变 `latest`；多个存储/管理端口监听全部网卡；容器无资源上限；现有生产 Compose 的 Redis 鉴权、Worker 并发、应用健康检查、日志轮转、内部网络和 TLS 尚未加固。
- ECS 估算：最低可行单机 Demo 为 4 vCPU/8 GiB/80 GiB ESSD/5 Mbps、单用户和 Worker 并发 1；稳妥演示档为 4 vCPU/16 GiB/100 GiB ESSD/5-10 Mbps。全部模型在线调用，无 GPU 需求。
- 工具：新增 `scripts/audit_docker_host.sh`，兼容没有 Python 3 的 CentOS 7；只读取明确字段，不打印容器环境变量或密钥，并比较执行前后容器快照。新增测试锁定无基础设施变更命令、无环境变量泄露和 CentOS 7 `lsblk` 兼容性。
- 文档：新增 `docs/T9-1部署环境审计与ECS资源估算.md`，同步运行时兼容矩阵、部署基线和 T9 子阶段门禁。
- 状态：T9-1 已完成，T9 仍未完成。购买 ECS 前等待用户确认 8 GiB/16 GiB 档位、地域/计费方式和全栈单机/外部存储拓扑；确认后才进入 T9-2 生产 Compose 加固。

## 2026-08-12 - T9-2 全栈单机部署产物加固

- 目标：根据已确认的 4 vCPU/8 GiB/80 GiB Alibaba Cloud Linux 4 测试 ECS 和全栈单机拓扑，生成可迁移、可审计、默认不暴露存储端口的部署制品。
- Compose：固定 MongoDB 8.2.6、Milvus 2.5.5、etcd 3.5.18、双 MinIO 和 Redis 7.4.10；拆分 edge/data/assets 网络，仅 Web 发布 80；10 个服务均有健康检查、日志轮转和资源/PID 上限，Worker 并发降为 1，Nginx/API/临时目录统一 100 MiB 单文件限制。
- 安全：Milvus 开启鉴权并使用幂等一次性密码初始化；MongoDB 创建项目专属用户；Redis 开启密码、保护模式和 AOF；API/Worker 非 root、只读根文件系统并删除 capabilities；公网 Demo token 增加访问码和限流，Nginx 增加 CSP 等响应头。
- 图片闭环：新增 `MINIO_PUBLIC_BASE_URL=/objects`，把内部 MinIO 预签名 URL 改写为同源短时链接；Web 只通过独立 assets 网络代理业务 MinIO，不进入完整数据网络。
- 迁移与运维：新增服务器 `.env` 生成器，明确排除 SSH/DEPLOY 字段和旧存储地址；新增 Alibaba Cloud Linux 4 Moby 安装、主机预检、数据目录、受控部署、显式 CC0 合成语料种子、冷备和空目录恢复脚本。备份同时保存匹配 `.env` 与 SHA-256。
- 验证：101 个全量后端测试、Ruff、ESLint、TypeScript/Vite build 和临时服务器配置的 Compose 解析通过；解析结果只有 80 发布。Windows Docker Desktop Linux Engine 未运行，因此本阶段没有实际构建镜像。
- 安全组门禁：用户截图仍显示 SSH 22 与 RDP 3389 对 `0.0.0.0/0` 开放，且未见 80 规则；进入 T9-3 前必须删除公网 3389、把 22 限制为用户公网 IP、增加 80，并继续禁止数据库和存储端口。
- 状态：T9-2 已完成，T9 仍未完成；未连接或修改新 ECS，也未改动旧 CentOS。下一步严格进入 T9-3 真机部署，随后才是 T9-4 端到端、容量和备份恢复验收。

## 2026-08-12 - T9-3 阿里云真机部署启动

- 目标：在新购 Alibaba Cloud Linux 4 测试 ECS 上执行全栈单机部署，不修改旧 CentOS 及其 MongoDB、Milvus、MinIO、Attu 和 Redis。
- 安全组现状：`80/tcp` 已对公网开放；无用的 Windows RDP `3389/tcp` 已删除；数据库和存储端口未开放。删除后已重新建立 SSH 会话并验证连接正常。
- 测试期例外：服务器实际观察到的 SSH 来源是用户当前宽带出口，但用户需要在不同办公地点连接，因此明确批准暂时保留 `22/tcp` 对 `0.0.0.0/0` 开放。该例外只适用于当前测试 ECS，不代表生产安全门禁通过；正式发布前必须改为密钥登录并限制来源，或使用受控运维通道。
- 状态：T9-3 进行中。下一步先执行新 ECS 只读主机审计，符合基线后才安装 Moby/Compose、部署服务器专用 `.env` 并启动全栈。

## 2026-08-12 - T9-3 阿里云真机部署完成

- 目标：在不触碰旧 CentOS 的前提下，把 T9-2 全栈制品部署到新 Alibaba Cloud Linux 4 测试 ECS，并完成空存储初始化、受控合成语料发布和运行烟雾验收。
- 主机与运行时：新 ECS 为 4 vCPU、7.3 GiB 内存、79 GiB 根盘；安装 Moby 28.3.3、containerd 1.7.34、Compose 2.26.1 和 Git 2.47.3。项目位于 `/opt/semiconductor-agent-knowledge-base/`，数据与冷备使用独立 `/opt` 目录；服务器 `.env` 权限为 `600` 且未被 Git 跟踪。
- 网络与镜像：ECS 直连 Docker Hub 超时，账号专属 ACR 加速地址未覆盖全部精确标签；仅在镜像拉取和首次构建期间使用 ECS 回环绑定的临时 SSH 反向代理，完成后已删除代理、桥接脚本和 Docker 代理配置。应用 Python 依赖改用带 SHA-256 的 `requirements.lock`，镜像构建不再触发无界依赖回溯。
- 故障修复：首次 Milvus 启动因 3 行鉴权片段覆盖镜像完整 `milvus.yaml`，使 DataCoord 与 Proxy 同抢 `19530` 并退出 134。改为保留 v2.5.5 镜像完整配置、启动时只原位开启 `authorizationEnabled`，新增部署回归测试；修复提交为 `1aed815`，重建后第三次健康探测即通过。
- 全栈结果：9 个长期服务均运行，API、Web、MongoDB、Milvus、双 MinIO、etcd、Redis 均健康，Celery Worker 返回 `pong`；一次性 `milvus-init` 以 0 退出。宿主机只监听公网 22/80，数据库和存储无宿主机端口映射；公网 `/healthz`、`/api/v1/live` 和首页均返回 200，无访问码申请 Token 返回 401。
- 数据结果：MongoDB 已有 7 个 Document、18 个 Chunk、2 个 ImageAsset、7 个幂等 Job 和 49 个阶段事件；MinIO 私有 Bucket 中有 7 个 raw 与 9 个 derived 对象；Milvus v4 强一致查询返回 15 个 Published Chunk，Dense HNSW、Sparse Inverted Index 和活动 Alias 全部通过 verifier。3 个历史过期 Chunk 未进入活动向量结果。
- 检索与页面：公网 API 烟雾请求经 Qwen 原生 Dense/Sparse、Milvus、RRF 和 `qwen3-rerank` 返回现行 `SOP-ETCH-03-R2-001`，未返回历史 R1；Trace 为 `trace_9819a910c86c41e6868d2dcc578f064e`。浏览器使用访问码进入工程师工作台，页面导航和首屏正常，控制台 0 条错误，截图见 `docs/evidence/t9-3/ecs-workbench.png`。
- 安全与资源：Milvus 配置密码可用而默认 `root:Milvus` 被拒绝；Redis 密码、保护模式和 AOF 验证通过。API 镜像未安装 Torch 或 FlagEmbedding。采样时宿主机约 5.9 GiB 可用内存、根盘剩余 67 GiB；当前仍是最低规格测试节点，不代表容量验收通过。
- 风险与状态：测试期仍按用户批准保留 SSH 22 对 `0.0.0.0/0` 和密码登录，HTTP 尚无域名/HTTPS，MongoDB 仍为单机且服务器无 Swap，Docker Hub 直连不可用。这些均保留为 T9-4/正式发布前门禁。T9-3 已完成，T9 仍未完成；下一步只能进入 T9-4 端到端、容量、安全、备份恢复和演示脚本验收。

## 2026-08-12 - T9-4 范围补充：SSE 流式对话

- 目标：解决当前对话请求在约十秒等待后一次性显示完整答案的问题，并在开始 T9-4 前锁定剩余开发和验收范围。
- 决策：新增 POST SSE 消息接口，前端固定使用 `fetch + ReadableStream`；事件覆盖接收、处理阶段、证据、真实答案 Token、完成、错误和心跳。保留原非流式接口用于兼容和自动回归。
- 交互边界：用户消息立即乐观显示；阶段状态必须来自实际 Agent 执行过程；不得用伪打字机掩盖等待，也不得输出隐藏推理、系统提示词或密钥。最终完整结果仍以 MongoDB 线程和 Checkpoint 为准。
- 部署边界：Nginx 对 SSE 路由关闭缓冲并延长读取超时；补齐取消、断连、幂等请求、失败不保存半截答案和流式时序验证。
- T9-4 其余范围：完整上传/入库/连续问答/图片/Trace/评估点击闭环，远程 Provider 超时与限流降级，容量与重启测试，冷备恢复到独立空目录，安全复核、镜像离线迁移和最终演示脚本。
- 文档：新增 `docs/T9-4端到端验收计划.md`，同步 `docs/分阶段开发计划.md` 和 `docs/测试与验收计划.md`。
- 状态：本次仅冻结计划，没有开始业务代码或服务器变更；T9-4 仍为“未开始”，T9 仍为“未完成”。

## 2026-08-12 - T9-4.1 流式能力探测与契约冻结

- 目标：在开发 SSE 业务路由前，证明主备 LLM 能提供真实增量，并冻结事件、错误、取消、幂等和最终持久化语义。
- Provider 实测：本地与目标 ECS 均逐一调用 `gpt-5.6-luna` 和 `qwen-flash`，没有 fallback。Luna 返回 28 个可见 Delta，以 `finish_reason=stop + EOF` 结束；Qwen 返回 10 个，以 `[DONE]` 结束。ECS 首 Delta 分别约 1.74 秒和 0.38 秒，正文 Hash 一致。
- 代码：新增脱敏流式探测器和 `verify_llm_streaming.py`；支持网络 UTF-8 分片、`[DONE]`、`finish_reason_eof`，拒绝无终止残流；网络错误不暴露 Key/Endpoint，reasoning 只计数、不输出。
- 契约：新增 `StreamMessageRequest`、七类 SSE 事件、阶段/错误枚举、事件顺序校验、统一 `SendMessageResponse` 和 `AgentMessageRequestRecord`。`ChatMessage` 增加向后兼容的 `request_id/run_id`。
- 持久化决策：T9-4.2 新增 MongoDB `agent_message_requests`，唯一键为用户、线程和请求 ID；正文 Hash 冲突拒绝，运行中拒绝重复，完成可重放，失败/取消重试不重复写用户消息，半截助手答案不入库。
- 部署修复：首次增量更新误合并开发/生产 Compose，短暂暴露 8000，随即恢复纯生产 Compose 并确认仅 22/80。API 更换内部 IP 后 Nginx 旧 upstream 导致一次 502，重建 Web 后恢复；`deploy.sh` 已固定 API/Worker 后强制重建 Web 并补测试。未重启或修改任何存储服务。
- 验证：112 项全量后端测试、Ruff、ESLint、TypeScript/Vite build 和生产 Compose 解析通过；ECS 双 Provider 探测退出 0，API/Web 与存储健康、Worker `pong`、公网健康接口恢复 200，近十分钟应用日志无 Traceback/Exception/Critical。详细证据见 `docs/T9-4.1流式能力与契约验收.md`。
- 状态：T9-4.1 已完成，只解除 T9-4.2 门禁。T9-4 仍进行中，T9 仍未完成；下一步严格进入 SSE 全链路开发，不跳到业务闭环或容量阶段。

## 2026-08-13 - T9-4.2 SSE 全链路开发与真机验收

- 目标：把 T9-4.1 已证明的 Provider Delta 接入 FastAPI、LangGraph、Nginx 和 React 工作台，解决等待后一次性显示完整答案的问题。
- 后端：新增 POST SSE 消息接口、请求状态查询与取消接口；LangGraph 输出真实阶段、证据和答案 Delta；超过静默窗口发送心跳。MongoDB 新增 `agent_message_requests` 业务账本，完成重放、正文冲突、运行中冲突、失败/取消同 ID 重试和最终持久化门禁。
- 前端：使用 `fetch + ReadableStream + TextDecoder` 解析跨网络分片；用户消息乐观显示，生成卡片展示真实阶段、证据数量和增量答案；提供停止与同 ID 重试。HTTP 环境请求 ID 改用 `crypto.getRandomValues()`，页面卸载通过带鉴权 `keepalive` 取消。
- 断连与失败：显式停止会中断并等待旧 LangGraph 任务退出，旧流只能清理自己的控制句柄；服务端使用受保护清理任务处理 ASGI 取消。最终持久化失败不会发送 `completed` 或保存半截助手消息。
- 代理与部署：Nginx SSE 路由关闭缓冲、缓存和 gzip，延长读写超时并设置 `X-Accel-Buffering: no`。增量部署只重建 API、Worker、Web，没有修改存储配置或数据卷；线上最终版本为 `612d667`。
- 原始时序：公网首事件 94 ms，检索 2347 ms、重排 4044 ms、生成 4101 ms，4 个答案 Delta 在 5811/5999/6343/6435 ms 分批到达，6451 ms 在持久化后完成；线程最终只有 `user + assistant` 两条消息。
- 浏览器：桌面端约 305 ms 显示生成状态；停止约 280 ms，立即重试约 1.49 秒重新进入运行且无 409，账本最终 `attempt=2` 且消息不重复。刷新断线账本为 `cancelled` 且无助手消息。移动端 `390 x 844` 无横向溢出，桌面/移动控制台均无错误。
- 验证：后端全量 125 项、Ruff、ESLint、TypeScript/Vite build 全部通过；桌面生成中/停止/完成和移动生成中/完成截图已逐张检查。详细证据见 `docs/T9-4.2 SSE全链路验收.md`。
- 修复提交：`00d6e21`、`7c6baa7`、`7c1c088`、`b515477`、`1b02942`、`330c844`、`612d667`。
- 状态：T9-4.2 已完成，只解除 T9-4.3 门禁。T9-4 和 T9 仍未完成；下一步严格进入完整业务闭环，不提前执行 Provider 韧性、容量或恢复阶段。

## 2026-08-13 - T9-4.3 通用会话记忆与按需路由方案确认

- 现象：工作台同一线程先完成一次业务问答，再输入“我刚才问什么了”，系统仍进入知识检索并返回缺少正文的高置信度模板，未表现出通用会话记忆。
- 根因边界：`agent_threads.messages` 已保存精确消息，但当前图主要把本轮 `request` 交给约束提取和检索，没有在每轮运行前装配历史；MongoDB Checkpoint 解决图中断/恢复，不会自动把展示消息变成 LLM 会话上下文。
- T6 结论：T6 的真实 `interrupt/resume`、跨实例澄清恢复、证据账本和工具闭环仍保持已完成；“通用连续会话”是新增需求，不反向伪造或抹除 T6 验收结论。
- 架构决策：采用 `ContextAssembler + 1 个前置闸门 + 3 层逻辑识别 + 确定性 RoutePolicy + 路由专用校验`。明显请求走规则，复杂请求只调用一次结构化 LLM；LLM 只给建议路由，最终决策由可测试策略完成。
- 路由范围：固定 `history_direct`、`chat_direct`、`reuse_evidence`、`internal_rag`、`tool_only`、`rag_and_tool`、`rag_and_web`、`clarify`、`refuse`。历史回顾和普通会话不默认调用 Embedding/Reranker。
- 记忆决策：`agent_threads.messages` 保存精确历史，最近 `10~12` 轮直接装配，更早内容使用带边界摘要；`active_context` 保存当前对象、槽位来源和证据引用；Checkpoint 仍只由 LangGraph 管理。跨线程长期记忆继续要求显式批准。
- 数据影响：计划以可选字段扩展 `agent_threads` 和 `agent_message_requests`，并增加同线程顺序控制、证据复用再鉴权和路由审计；当前未执行 MongoDB 迁移。
- 开发顺序：拆为 T9-4.3.1 会话上下文、T9-4.3.2 三层识别与按需路由、T9-4.3.3 路由专用生成/校验、T9-4.3.4 完整业务浏览器闭环，四项必须串行验收。
- 文档：新增 `docs/T9-4.3通用会话记忆与按需路由设计.md`，并同步分阶段计划、T9-4 计划、API/数据契约、存储设计、测试计划、架构设计和产品方案。
- 验证与状态：本次仅修改文档并检查 Markdown/Git 差异，没有修改业务代码、运行测试、连接服务器或部署。方案已经用户采纳，但 T9-4.3.1 仍需用户再次确认后才能开发；T9-4 和 T9 保持未完成。

## 2026-08-13 - T9-4.3 受控意图识别增强方案确认

- 目标：在不把系统膨胀为通用 Agent 平台的前提下，提高半导体场景中的任务/非任务、混合表达、多任务、纠正和取消识别，让对话自然且不漏单。
- 吸收范围：增加 L0 确定性快速通道、交互模式、最多 3 个受控 `task_items`、情绪与任务解耦、槽位 `set/inherit/correct/clear`、明确取消范围、严格结构化输出和 `semikb-intent-v1` 半导体意图评估集。
- 复杂度控制：只允许 `RAG`、`Tool`、`RAG + Tool`、`RAG + Web`、`历史处理 + 新任务` 等预定义组合；不建立自由任务 DAG，不增加独立向量意图模型、第二次意图 LLM 调用或新基础设施。
- 安全边界：多意图识别不等于全部自动执行；安全任务可完成，Recipe 写操作或越权项单独拒绝/审批，并逐项记录执行、澄清、拒绝、延后或失败状态。情绪只影响表达，不改变权限和工具路由。
- 上下文边界：Tool 等槽位纠正会使依赖的 Chamber、Recipe 和证据引用失效，但保留无关上下文；取消区分当前生成、当前任务、指定子任务和待补充澄清，不删除线程历史。
- 模型边界：复杂输入仍只进行一次结构化 LLM 理解；使用 Schema 和 Pydantic 校验，最多一次结构化修复。置信度阈值按半导体评估集与风险校准，不采用通用固定阈值；语义低置信度进入澄清，不触发 session 级 LLM 熔断。
- 评估：T9-4.3.2 建设初版 80~120 条 `semikb-intent-v1`，覆盖相近半导体任务、任务/非任务/混合、情绪、多任务、指代、省略、纠正、取消、中英文实体、越权和 OOD；T9-4.3.4 全量回归。
- 开发编排：T9-4.3.1 补齐槽位来源/失效基础结构；T9-4.3.2 实现识别、路由和意图评估；T9-4.3.3 实现受控组合和逐任务校验；T9-4.3.4 完成浏览器与意图回归。预计相对原 T9-4.3 增加约 25%~35% 工作，复杂度约 7/10。
- 状态：本次仍只修改文档，没有修改代码、数据结构或服务器。受控增强方案已获用户确认，但开始 T9-4.3.1 仍需遵守现有开发确认门禁；T9-4 和 T9 保持未完成。
## 2026-08-13 T9-4.3.1 通用会话上下文

- 目标：在不提前实现意图路由的前提下，为连续对话建立可恢复、可审计且顺序稳定的上下文基础。
- 契约：消息新增 `turn_seq`；线程新增摘要边界、上下文版本、活动上下文、顺序游标和活动请求租约；
  请求账本新增用户/助手序号。
- 实现：新增 `ContextAssembler`，装配最近 12 轮精确历史、旧历史有界摘要、活动槽位和批准偏好；
  LangGraph 只继承带来源且有效的槽位。Tool 等父槽位变化会失效依赖 Chamber/Recipe 和旧证据。
- 并发：流式与兼容非流式消息统一经过幂等请求账本；MongoDB 和 DemoStore 都拒绝同线程不同请求并发，
  完成、失败、取消会释放线程租约。
- 迁移：新增 `migrate_t9431_context.py`，支持 dry-run、apply、失败自动恢复和显式 rollback；快照不保存正文。
- ECS：21 个旧线程迁移成功，53 条旧消息补序号，重复 dry-run 为 0；真 Mongo 的精确历史、摘要边界、
  重启恢复、隔离、顺序、回滚和清理全部通过。只替换 API/Worker，数据服务未重启。
- 韧性：线程租约默认 30 分钟；只允许相同 `request_id` 在租约和请求记录都陈旧后恢复，禁止新请求抢占；
  长会话摘要按 `summary_upto_message_id` 增量推进，避免每轮重建全部旧历史。
- 部署修正：API/Worker 替换后 Nginx 缓存旧 API IP 导致短时 502；仅重建 Web 容器刷新服务解析，
  数据容器未重启，随后公网健康和旧线程 HTTP 复验通过。
- HTTP：真实 JWT 读取 20 个旧线程返回 200，全部具备新上下文版本和消息序号。
- 质量门禁：`ruff check src scripts tests` 通过；全量后端 `137 passed`；React/TypeScript 生产构建通过。
- 边界：本任务没有实现 `history_direct`、任务/非任务识别或按需路由；下一步仍是 T9-4.3.2，须先复核
  文档和最终契约并获得用户确认。

## 2026-08-13 T9-4.3.2 受控意图与按需路由

- 目标：在 T9-4.3.1 通用上下文之上实现自然任务/非任务识别和按需路由，避免历史回顾、反馈、取消、
  纠错或越权请求误触发检索与工具。
- 契约：新增交互模式、一级意图、目标/动作、最多 3 个任务、情绪、槽位操作、取消范围、固定路由、
  逐任务策略决策和向后兼容响应字段；请求台账保存同一组脱敏路由审计信息。
- 理解：L0 使用模块级预编译规则；复杂输入调用一次严格 JSON Schema LLM，Pydantic 校验失败最多修复
  一次，随后确定性降级。服务端校正调查优先级、实体槽位类型、Recipe 写拒绝和报告延后。
- 路由：确定性 `RoutePolicy` 决定历史、聊天、证据复用、内部 RAG、制造工具、RAG+Tool、RAG+Web、
  澄清或拒绝；历史/反馈/纠错/取消/越权可在下游前终止，旧证据复用会重新校验 ACL 和版本有效性。
- 上下文：槽位设置/继承/纠正/清除均保留来源；纠正 Tool 会失效依赖 Chamber、Recipe 和证据引用，
  但保留无关 Product。
- 评估：新增 96 条 `semikb-intent-v1`，Hash 为
  `01caa8a4fa2180e2dea19df3ea04da2cb80512f10bf10076d47c42dbce53b228`。本地确定性与 ECS 在线模型均
  96/96 通过，所有主要准确率/宏平均指标为 1.0，六项安全错误率为 0.0；只作为合成冻结集回归门禁。
- 真实链路：ECS 14 项路由断言全部通过，8 条请求持久化路由审计；累计检索 1 次、工具 3 次、Web 0 次，
  所有应跳过场景调用计数未增加。
- 质量与部署：Ruff、后端全量 151 项、ESLint 和 Vite build 通过；只重建 API/Worker/Web，六个数据服务
  容器 ID 未变化；公网健康正常、Worker `pong`、最近日志无应用错误或 Checkpoint 类型警告。
- 状态：T9-4.3.2 已完成，只解除 T9-4.3.3 门禁。组合执行、路由专用生成/校验、前端路由标识和完整
  浏览器闭环尚未实现；下一步必须复核文档并获用户确认，不得跳到 T9-4.3.4 或 T9-4.4。

## 2026-08-13 T9-4.3.2 线上历史回忆缺陷热修复

- 触发：用户在公网工作台使用“我刚刚说什么?”和“我刚刚说了什么?”时，前者被误路由到内部 RAG，
  后者把本轮回忆指令自身当成要回忆的内容。原 T9-4.3.2 验收只覆盖“刚才”等表达，没有覆盖该真实失败链。
- 根因：L0 历史规则缺少“刚刚/刚在”等口语表达；结构化模型虽识别出
  `previous_user_message + recall`，但服务端仍允许错误的 `knowledge_query/internal_rag` 建议影响路由；历史目标
  简单取最近一条用户消息，没有跳过连续出现的回忆元问题。
- 修复：扩展模块级历史表达规则；`RoutePolicy` 固定让历史目标与回忆/变换动作优先于模型意图和建议路由；
  历史目标从最近一条非回忆元问题的用户消息中确定，并用服务端选择覆盖不可靠的模型消息引用。
- 回归：保留 `semikb-intent-v1` 及原 Hash 不变，新增 99 条 `semikb-intent-v2`，Hash 为
  `ad13bb6b49ff604f5e25eb1b27e85c6db2a7b455ee7b1d24fbcd9d85641b95be`；新增
  `context_reference_accuracy`，同时验证路由与引用目标。Ruff 通过，后端全量 `159 passed`，v2 确定性
  99/99 通过。
- ECS：只重建 API/Worker/Web；MongoDB、Milvus、两个 MinIO、etcd、Redis 容器 ID 前后不变。真实服务
  14 项断言通过，连续两次回忆均返回原始 ETCH-03 SOP 问题，历史轮次没有增加 Retrieval/Tool/Web 调用。
- 浏览器：在公网工作台新建线程后依次发送业务问题、“我刚刚说什么?”、“我刚刚说了什么?”；两次回答
  均为原始业务问题，未自引用，证据栏未产生新的历史轮次检索结果。
- 状态：T9-4.3.2 在缺陷重新打开后已完成热修复和重新验收；T9-4.3.3 仍未开发，阶段顺序不变。

## 2026-08-14 T9-4.3.3 意图治理与自然会话计划补充

- 目标：吸收外部意图设计与评测中适合半导体智库的部分，并解决普通聊天、历史回顾、澄清和拒绝回复
  过于模板化的问题；本次只调整设计和开发编排，不修改代码或 ECS。
- 意图治理：计划新增版本化意图定义卡，记录 `handles/excludes/confused_with`、正例、困难反例、必要
  槽位、允许路由和风险级别；不建立“对象 x 动作”的意图笛卡尔积。
- 评测升级：保留 `semikb-intent-v1/v2` 原内容和 Hash，另建独立 `intent-example-bank-v1` 与冻结
  `semikb-intent-v3`。新增逐类混淆矩阵、P/R/F1、Macro/Micro-F1、高风险 F-beta、多任务集合完全匹配、
  漏/多任务、对象动作、槽位、依赖和最终路由计划指标；原始多意图输入必须端到端评估。
- 实验门禁：离线比较当前 Prompt、固定 Few-shot 和动态案例检索，示例库不得包含冻结评估样本；记录
  质量、P50/P95 延迟和成本。动态检索只有优于固定 Few-shot、安全指标不回退并再次获得用户确认后
  才能上线，否则不增加线上 Embedding 调用。
- 自然回答：历史回顾和历史回答变换计划迁移为 `chat_direct`。服务端仍负责准确选择
  `context_message_ids`、跳过连续历史元问题和线程权限校验，LLM 只负责自然表达；Provider 失败或校验
  不通过时精确回退。`history_direct` 暂时保留为旧台账、评估快照和 Trace 的兼容枚举。
- 对话边界：普通 `chat_direct` 使用有界最近历史；`clarify` 只围绕结构化缺项提问；`refuse` 使用稳定
  粗粒度原因、产品能力边界和合法替代用法自然说明。模型不拥有路由权，情绪不改变权限和执行状态。
- 开发编排：原 T9-4.3.3 拆为 `3.3a` 意图治理与评测、`3.3b` Few-shot 影子实验、`3.3c` 自然直接
  回答、`3.3d` 受控组合执行与前端标识，随后才进入 T9-4.3.4 完整浏览器闭环。a~d 严格串行。
- 文档：同步 `分阶段开发计划`、`T9-4端到端验收计划`、T9-4.3 设计、API 契约、测试计划、架构设计和
  产品方案。当前线上行为仍为 T9-4.3.2 热修复后的版本，计划更新不等于实现完成。
- 状态：下一步是复核 T9-4.3.3a 的数据契约、目录、评测报告格式和验收门禁，获得用户开发确认后再
  编码；不得跳到 3.3b、3.3c、3.3d、3.4 或 T9-4.4。

## 2026-08-14 T9-4.3.3 全量意图卡 Prompt 策略确认

- 决策：项目初期活动意图卡数量较少，所有未命中 L0 的复杂输入在一次结构化 LLM 调用中加载指定
  `catalog_version` 的全部 `status=active` 意图卡，不增加关键词、Embedding 或第二次 LLM 候选卡预筛。
- Prompt：每张活动卡提供精简但完整的处理范围、排除范围、混淆关系和少量代表性正反例；草稿和废弃卡
  不进入 Prompt。启动时完成 Schema、引用关系、枚举、版本和唯一性校验并缓存目录。
- 可观测性：理解审计记录意图目录版本/Hash、活动卡数量和输入 Token，不保存完整 Prompt 或敏感正文；
  同时统计 P50/P95 理解延迟和逐类混淆。
- 实验：T9-4.3.3b 调整为 A 当前 Prompt、B 全量卡、C 全量卡+固定 Few-shot、D 全量卡+动态 Few-shot。
  初期默认上线候选为 C；动态检索只补充案例，不筛选意图卡，仍需通过冻结 v3 和用户再次确认。
- 容量门禁：达到卡数、Prompt Token、P95 延迟或分类干扰阈值时只告警、暂停扩卡并重新评审，不能在
  未设计和评测的情况下自动切换候选卡模式。
- 文档：同步分阶段计划、T9-4 验收计划、T9-4.3 设计、API 契约、测试计划、总架构和产品方案。
- 状态：本次仍只修改计划文档，没有修改代码、数据或 ECS；下一步仍是取得确认后开发 T9-4.3.3a。

## 2026-08-14 T9-4.3.3a 意图治理与评测

- 目标：建立版本化半导体意图共同真相和可复现 v3 评测，证明每个非 L0 复杂输入在一次结构化 LLM
  调用中看到全部活动卡，并让目录版本、卡数、Token 和时延可审计。
- 治理：新增 `semikb-intent-catalog-v1` 15 张卡（13 active、1 draft、1 deprecated）、双向易混淆关系、
  严格启动校验和缓存；新增独立 `intent-example-bank-v1` 15 条，和冻结集文本交叉数为 0。
- 冻结集：保留 v1/v2 内容和 Hash，新增 108 条 `semikb-intent-v3`，Hash 为
  `e5f2f689655e0eccd945bb25eec49e167c2dc07db5436c6dc4d360fc38976679`，覆盖单意图、多任务、对象动作、
  依赖、槽位、混淆对和情绪输入。
- 运行时：非 L0 复杂请求固定注入全部 13 张 active 卡，无关键词/Embedding/二次 LLM 预筛；draft 和
  deprecated 不进入 Prompt。容量超限只告警，不自动改变策略。
- 审计：SSE 响应与 `agent_message_requests.understanding_audit` 保存目录版本/Hash、卡数、Token 来源、
  调用来源和理解时延，不保存 Prompt、卡片正文、用户正文、隐藏推理或密钥；字段可选，无破坏性迁移。
- 评测：确定性 v3 为 108/108；真实在线 70 个 LLM Case 全量卡注入率 1.0，一级意图 Macro-F1
  `0.991662`、意图卡 Macro-F1 `0.900559`、多任务精确匹配 `0.181818`，共有 34 条严格偏差。在线
  Token P50/P95 为 `4077.5/4181.55`，理解时延 P50/P95 为 `3673.554/7562.108 ms`，P95 超 5000 ms
  门禁并产生告警。这些偏差如实冻结为 3.3b 基线，不改写预期值。
- 质量：聚焦测试 18 项、全量后端 `164 passed`、Ruff 和补丁格式检查通过；凭据安全的三份本地/在线
  报告已归档。
- ECS：只替换 API/Worker/Web，六个数据服务容器 ID 不变；在线探针 13/13 卡、4072 Token、约 3 秒，
  真实 SSE 请求及 MongoDB 台账审计通过；API/Web 健康，Worker `pong`，最近日志无应用错误。
- Git：英文提交 `580ebba` 已推送。服务器此前存在已知源码同步漂移，本次逐文件确认与前一提交一致后
  只应用增量补丁，没有 reset、清理未知文件或覆盖 `.env`。
- 状态：T9-4.3.3a 已完成，T9-4 和 T9 仍未完成。下一步只能在用户确认后进入 T9-4.3.3b Few-shot
  影子实验，不得提前开发 3.3c、3.3d、3.4 或 T9-4.4。

## 2026-08-14 T9-4.3.3b Few-shot 影子实验

- 目标：在不改变线上 B 默认臂的前提下，用同一冻结 `semikb-intent-v3` 比较 A 旧 Prompt、B 全量
  活动卡、C 全量卡+固定 Few-shot、D 全量卡+动态 Few-shot，并冻结质量、安全、Token、延迟、调用和
  参考成本证据。
- 实现：新增显式实验 Profile 和隔离的内存示例选择器；固定臂使用 15 条独立示例，动态臂用现有
  `qwen3.7-text-embedding` 选择 Top-4，始终保留全部 13 张活动卡。L0 不调用 LLM/Embedding，生产
  构造函数继续默认 B。评测补齐输出/总 Token、Provider/Embedding 调用、示例选择时延和关键混淆对
  完全匹配率；动态门禁要求多任务与混淆对都严格优于 C。
- 在线影子结果：四组各 108 条，L0 38、LLM 70，严格失败 A/B/C/D 为 34/37/31/31。C 的意图卡
  Macro-F1 `0.882318` 低于 B 的 `0.899660`；D 虽达到 `0.907166`，但多任务精确匹配与混淆对准确率
  均只与 C 持平，故 C/D 均未获得上线候选资格。四组危险执行漏拒率均为 0。
- 容量与成本：Prompt P95 A/B/C/D 为 `1116.65/4183.65/5698.65/4613.0`，理解 P95 为
  `9723.903/7075.639/7253.003/8324.744 ms`，均保留时延告警。D 共 72 次 Embedding；参考成本和完整
  失败/混淆证据见 `docs/evidence/t9-4-3-3b/online-abcd-report.json`，Hash 为
  `2e19208b7d8fd6b792ec77e8e0c390379b905e1481740dc164d1baf62ad0bd67`。
- 质量：最终聚焦 `15 passed`、全量后端 `173 passed`，Ruff、补丁格式和报告一致性均通过；唯一警告
  来自 Starlette TestClient 的第三方弃用提示。
- ECS：确认服务器关键源码与父版本 Hash 一致后应用最小增量补丁，只替换 API/Worker。Web 因旧 API
  地址缓存短暂 502，仅重启进程后公网恢复 200；六个数据服务容器 ID 前后不变。真实 B 探针为 13/13
  卡、0 个示例、0 次 Embedding、1 次 LLM、严格失败 0，Worker `pong`，近期日志无应用错误。
- 门禁修正部署：严格 D 对 C 门禁补丁 SHA-256 为
  `406c3213ad2968897396f0a156731923205f2e7e0fbfd5c3f0189517c712c34d`，远端两份评估源码 Hash 与本地
  最终文件一致；再次只替换 API/Worker 并重启 Web 服务解析，六个数据服务容器 ID 仍逐项不变。最终
  API/Worker 容器 ID 分别为 `68c01adaaa0d...` 和 `101301a42d67...`，公网 API/首页均为 200。
- Git：实现提交 `fe2ef13`；最终门禁修正、报告回算和中文验收文档由后续英文提交记录。
- 状态：T9-4.3.3b 已完成，T9-4 和 T9 仍未完成。下一步只能在用户确认后进入 T9-4.3.3c；不得跳到
  3.3d、3.4 或 T9-4.4，也不得把 D 接入线上。

## 2026-08-14 T9-4.3.3c 自然直接回答

- 目标：把历史回顾、普通交流、历史变换、澄清和拒绝升级为受控自然流式回答；模型只负责表达，
  服务端继续决定意图、路由、权限、历史目标、缺失槽位和拒绝原因。
- 意图迁移：新增 `semikb-intent-catalog-v2` 16 张卡（14 active），历史请求只允许
  `chat_direct/clarify`；新增通用 `action.out_of_scope`，不按诗歌、笑话等主题穷举。生产保持 B，
  0 个 Few-shot、0 次意图示例 Embedding。
- 冻结兼容：保留 v1/v2/v3 原内容和 SHA-256，新增路由迁移覆盖层。三个冻结集 96/99/108 条的路由
  准确率和上下文引用准确率均为 1.0，危险执行漏拒率与新 `history_direct` 产生率均为 0。
- 自然生成：新增 `DirectReplyGenerator` 和分类型严格 JSON 文本单元校验；历史原文由服务端精确插入，
  历史变换禁止引入源消息不存在的设备编号、版本号和数值。首单元失败完整降级，部分失败追加安全结尾，
  浏览器文本与最终持久化文本一致。
- LangGraph：澄清拆为回复生成节点与 `interrupt` 节点，恢复 Checkpoint 不重复调用模型；拒绝改成普通
  会话气泡。`agent_message_requests` 增加可选、脱敏的 `direct_reply_audit`。
- 浏览器缺口 1：首次点击域外内容生成请求时，LLM 误标为普通交流。新增基于任务结构和产品能力域的
  服务端归一化门禁，即使 LLM 误标也必须进入 `action.out_of_scope`；没有恢复主题黑名单。
- 浏览器缺口 2：真实 MongoDB 终态更新漏写 `direct_reply_audit`。补齐 Mongo 映射和重试清空逻辑，
  增加 Repository 级测试；真机普通交流和历史回顾均已保存 `llm_stream` 审计，且未保存 Prompt/正文。
- 公网验收：在工作台点击问候/能力、连续历史回顾、历史简化、缺槽位澄清及恢复、域外拒绝、Recipe
  写入拒绝和刷新恢复。SSE 依次观察到阶段和多个已验证文本单元；1440x900、390x844 和控制台检查通过。
- ECS：只替换 API/Worker/Web。API 重建后 Nginx 保留旧 upstream 地址产生短暂 502，按约定强制重建
  Web 后恢复；MongoDB、Milvus、双 MinIO、etcd、Redis 容器 ID 与部署前一致，公网健康为 `ok`，
  Worker `pong`，最近应用日志无错误。
- 质量：全量后端 `190 passed`，Ruff、前端 ESLint、TypeScript/Vite build、凭据和补丁格式检查通过；
  唯一测试警告为既有 Starlette/httpx 弃用提示。
- 证据：新增 `docs/T9-4.3.3c自然直接回答验收.md`、冻结路由报告、真实直接生成报告、浏览器验收 JSON
  及桌面/历史/移动截图。
- 状态：T9-4.3.3c 已完成，T9-4 和 T9 仍未完成。下一步只能在用户确认后进入 T9-4.3.3d；不得提前
  开发 T9-4.3.4 或 T9-4.4。

## 2026-08-14 - T9-4.3.4 范围补充：检索图片自动预览

- 目标：当当前回答的最终授权证据包含一张或多张图片时，在证据抽屉自动显示第一张，减少用户额外点击。
- 编排：该需求归入 `T9-4.3.4` 完整业务浏览器闭环，不改变下一步 `T9-4.3.3d` 的受控组合执行范围，
  也不提前进入 T9-4.3.4 开发。
- 选择规则：只按 Rerank、动态截断、权限和版本校验后的最终证据顺序选择第一张，不从原始召回候选中
  取图；同一结果下用户手动切换后不再自动覆盖。
- 安全与状态：继续通过 `/api/assets/{image_id}/access` 获取短时 MinIO 链接，不持久化临时 URL；新结果
  无图、无权、版本失效或加载失败时不得残留旧预览，且不能阻断文本答案。
- 验收：覆盖单图、多图手动切换、无图、无权、版本失效、短时链接失败、刷新恢复，以及桌面和
  `390 x 844` 移动浏览器截图与控制台检查。
- 状态：本次仅更新开发与验收计划，没有修改业务代码、数据结构或 ECS；功能仍为“未开发”。

## 2026-08-14 - T9-4.3.3c 路由展示热修复

- 目标：修复历史检索回答的结构化调查卡片被后续普通聊天或拒绝气泡覆盖的问题，并明确不同路由的展示边界。
- 根因：前端只保存一个全局最新 `agentResult`，结构化卡片渲染在消息列表底部；后续直接路由没有
  `answer`，因此旧卡片消失。首次兼容修复只在读取线程时补齐旧消息；新 SSE 完成响应仍返回仓储原始
  线程，导致发出下一条消息后再次丢卡。
- 契约：新增服务端 `MessagePresentation`。`reuse_evidence/internal_rag/tool_only/rag_and_tool/rag_and_web`
  使用 `structured_card`；`chat_direct/clarify/refuse` 和旧 `history_direct` 使用 `bubble`。模型不能选择
  展示模式，前端改为在对应助手消息内部渲染卡片。
- 兼容：新消息直接保存 `presentation`；旧消息按 `request_id` 从完成请求台账补齐，不修改旧 MongoDB
  文档。线程读取、SSE 完成和幂等重放返回一致展示结果。
- 测试：新增全部路由映射、旧消息补齐、SSE 返回和“旧 RAG 卡片后追加聊天气泡”回归；后端全量
  `200 passed`，Ruff、前端 ESLint、TypeScript/Vite build、凭据和补丁格式检查通过。
- 公网验收：`internal_rag` 结构化卡片在后续 `chat_direct` 后仍为 1 张；新线程 `clarify/refuse` 卡片均为
  0；刷新恢复、桌面和 `390x844` 移动端通过，浏览器控制台错误数为 0。
- ECS：最终仅替换 API/Worker，Web 保持不变。一次验收命令误合并开发和生产 Compose，Worker 因正确的
  `.env` 600 权限而重启；已立即按既有规范只使用 `docker-compose.prod.yml` 恢复，最终 API/Worker
  无源码挂载、API 无 `--reload`、Worker `pong`。MongoDB、Milvus、双 MinIO、etcd、Redis 容器 ID
  始终未变。
- 状态：该修复归属 T9-4.3.3c，不进入 3.3d、3.4 或 T9-4.4。下一步仍只能在用户确认后进入
  T9-4.3.3d。

## 2026-08-14 - T9-4.3.3c 槽位纠正修复与收尾验收

- 目标：修复 `不是 Chamber B，是 Chamber A` 仍确认旧值 B 的问题，并完成 3.3c 剩余测试、线上账本、
  路由展示和运行状态验收。
- 根因：通用实体提取和 Grounding 只读取第一个 Chamber；纠正表达中的第一个实体恰好是被否定的旧值。
- 实现：增加带槽位类型的纠正解析，明确 `old_value/new_value`；覆盖 Tool、Chamber、Product、Recipe 版本、
  时间范围、Lot 和 Case。显式槽位、L0 操作和 Grounding 统一只接受肯定侧新值。
- 回归：新增三种 Chamber 说法、六类其他槽位提取、依赖失效、气泡展示、Mongo 审计类型和零下游调用
  测试；同时补齐 feedback、help、cancel、control ack 的直接生成与审计覆盖。
- 质量：后端全量 `215 passed`；Ruff、前端 ESLint、TypeScript/Vite build、凭据扫描全部通过，唯一告警
  仍为既有 Starlette/httpx 弃用提示。
- 公网：修复后请求账本为 `chat_direct/control`、`correct chamber=A`、`trace_id=null`，回复为
  `已确认，当前条件为 Chamber A。`，刷新后仍恢复且保留上一轮结构化卡片。Luna 的七类直接回复全部
  使用 `llm_stream`，无回退和告警。
- 路由展示：真实点击确认 `internal_rag/tool_only/rag_and_tool/reuse_evidence/rag_and_web` 使用结构化卡片，
  `chat_direct/clarify/refuse` 使用气泡；证据复用沿用原 Trace，没有重新检索。
- 移动与控制台：真实 `390x844` DOM 复核通过，Chamber A 回复和结构化卡片均可见；浏览器错误日志为空。
  本次移动截图接口超时，视觉证据继续引用同版本前端既有 `390x844` 截图，并在验收 JSON 中区分记录。
- ECS：API `9758ef466af3`、Worker `34a87267a064`、Web `e9c921b133ba`；三者无源码挂载，API 无
  `--reload`，健康为 `ok`，Worker 为 `pong`，最近日志无应用错误。MongoDB、Milvus、双 MinIO、etcd、
  Redis 容器 ID 与部署前一致。
- 证据：新增 `docs/evidence/t9-4-3-3c/closing-browser-and-mongo-acceptance.json` 和收尾桌面截图，更新
  `docs/T9-4.3.3c自然直接回答验收.md`。
- 状态：T9-4.3.3c 收尾完成，不进入 3.3d、3.4 或 T9-4.4。下一步仍只能在用户明确确认后进入
  T9-4.3.3d。

## 2026-08-14 - T9-4.3.3d 受控组合执行

- 目标：在不引入自由 DAG、第二次意图 LLM 或新基础设施的前提下，实现预定义 RAG/Tool/Web/历史
  组合、逐任务终态、路由专用证据校验和前端真实路由展示。
- 契约：新增 `TaskExecutionStatus/TaskExecutionResult`、SSE `task_status` 和可选 `task_results`；
  `task_decisions` 保留执行前策略语义，最终状态保存到请求台账、终态载荷及助手消息展示字段。
- 运行时：LangGraph 增加组合前置执行和终态汇总；安全任务在兄弟任务被拒绝或延后时继续执行；
  `chat_direct`、RAG、Tool、RAG+Tool 和 RAG+Web 各自执行证据隔离与完成校验。
- 前端：流式显示任务排队、运行和终态；完成后按消息显示实际路由标签和最多 3 项任务结果；结构化卡片
  与气泡边界继续沿用 3.3c 服务端展示决策。
- 质量：后端全量 `226 passed`，Ruff、前端 ESLint、TypeScript/Vite build、diff 检查和凭据扫描通过。
- 真实服务：三组 ECS 组合调用通过；响应、`agent_message_requests.task_results`、
  `result_payload.task_results` 与助手消息 `presentation.task_results` 四方一致。证据见
  `docs/evidence/t9-4-3-3d/live-controlled-task-report.json`。
- 部署：首次收尾同步误用默认 Compose，产生隔离应用容器、空网络和空 Redis 卷；数据服务未被修改或
  重启。已根据容器标签恢复为 `docker compose -p semikb -f docker-compose.prod.yml`，清理全部误建资源，
  API/Web 健康、Worker `pong`。六个数据容器 ID 始终未变。
- 浏览器：真实点击证明 partial success 为 `completed/refused/deferred`；RAG+Tool 三个任务在 SSE 中
  显示运行中并最终完成；历史+RAG 先显示直接任务运行、检索任务排队，最终准确引用上一条问题。
- 刷新与 MongoDB：同一线程刷新后逐任务结果和历史原句恢复；请求台账、终态载荷和助手展示字段一致。
  精确 `390 x 844` 截图无横向溢出或重叠，浏览器控制台 0 error、0 warning。
- 证据：`docs/T9-4.3.3d受控组合执行验收.md`、
  `docs/evidence/t9-4-3-3d/browser-and-mongo-acceptance.json` 及同目录桌面/移动截图。
- 状态：T9-4.3.3d 已完成，只解除 T9-4.3.4 的开发门禁；未取得用户新的开发确认前不得开始 3.3.4，
  也不得进入 T9-4.4。

## 2026-08-14 - T9-4.3.4 完整业务闭环

- 目标：从浏览器上传开始，闭合入库、检索、连续对话、组合任务、图片、Trace、评估和意图回归，并
  实现最终授权图片自动预览第一张。
- 契约：`MessagePresentation.image_asset_ids` 保存结构化回答最终证据的有序图片 ID；SSE 完成事件、
  请求台账、线程刷新和幂等重放保持一致，气泡路由不携带图片投影。
- 前端：有图自动打开第一张，多图可手动切换；同一结果保留用户选择，新结果无图或加载失败清空旧图；
  图片仍通过资产访问 API 重鉴权和短时链接加载。
- 入库修复：真实 PDF 暴露 MinerU 预签名 PUT 403；移除签名未包含的强制 `Content-Type` 后发布成功，
  并增加协议级回归测试。修复前失败 Job 保留为任务中心审计记录。
- 意图治理：新增 `semikb-intent-catalog-v3` 图片资产查询卡；冻结 v1/v2/v3 文件和 Hash 不变，通过
  `semikb-route-migration-history-to-chat-v2` 覆盖旧路由期望。三套冻结集 96/99/108 条的路由和上下文
  准确率均为 1.0，危险执行漏拒率与新 `history_direct` 产生率均为 0。
- 完整闭环：真实 Markdown/PDF 分别发布 2/1 个 Chunk；MongoDB 2 Document/3 Chunk、MinIO 4 Object、
  Milvus 3 Row 与 v4 Alias 对账通过；连续历史恢复、3 项 `rag_and_tool`、图片顺序、无图清空和 Trace
  闭合。Full 评估 8 Case 可运行和下钻。
- 浏览器：亲自点击桌面自动首图、两图切换、无图清空、Markdown/PDF 任务、Trace 和 Full 评估；
  `390 x 844` 无横向溢出，控制台 0 error、0 warning。
- 质量：后端全量 `230 passed`，Ruff、前端 ESLint、TypeScript/Vite build 通过。浏览器评估通过率为
  `0.75`，当前 SOP 和过期 Recipe 2 个 Case 仍需后续质量优化，不把运行闭环等同于全部质量通过。
- 部署：只重建 API/Worker/Web；API/Web healthy、Worker `pong`。Milvus、MongoDB、双 MinIO、Redis、
  etcd 六个受保护数据容器 ID 未变。
- 证据：`docs/T9-4.3.4完整业务闭环验收.md` 和 `docs/evidence/t9-4-3-4/`。
- 状态：T9-4.3.4 已完成，但 T9-4/T9 仍未完成；当时计划的下一步为 T9-4.4 Provider 韧性，该顺序
  已被下方 2026-08-15 的新增编排覆盖。

## 2026-08-15 - Knowhere 式多格式入库开发编排

- 目标：修正产品/架构中的多格式入库目标与当前“Markdown/TXT 直读、其他格式统一 MinerU”简化实现
  之间的缺口，将 Knowhere 的按格式解析、结构恢复、资产抽取和语义切分能力纳入后续开发。
- 顺序调整：新增 `T9-4.4 Knowhere 式多格式入库`；原 Provider 韧性顺延为 T9-4.5，容量与重启为
  T9-4.6，冷备恢复为 T9-4.7，安全迁移为 T9-4.8，最终演示为 T9-4.9。多格式实现必须先完成，之后
  的 Provider 故障注入才覆盖新增的 MinerU/OCR/VLM 和格式适配器。
- 代码边界：后续建立独立 `semikb_ingest` 包，内部拆分 parsers/providers/structure/assets/chunking/
  models，对外只输出 `ParsedDocument`、`ChunkDraft`、`ImageAssetDraft`、`TableAssetDraft`。不把 Knowhere
  的 Worker、数据库、对象存储、任务状态或内部 DataFrame 泄漏到 `rag_ingestion` 业务层。
- 首期格式：PDF、DOCX、XLSX/CSV、PPTX、JPG/JPEG/PNG、Markdown/TXT/HTML。每类使用专用适配器；
  未支持格式明确拒绝或隔离，禁止以 MinerU 作为所有未知格式的默认兜底。
- 复用规则：允许直接复用、改写或参考 Knowhere 必要源码；实施前重新核对上游，固定 Commit，记录
  直接复用/参考/自研边界、依赖和本地修改，并更新 `THIRD_PARTY_NOTICES.md` 与许可证声明。
- 验收：逐格式黄金文件、结构/表格/图片保真、解析版本审计、幂等与失败不发布、权限/版本过滤、
  MinIO/MongoDB/Milvus 对账、真实检索与图片回传；ECS 工作台必须逐类上传，并完成桌面和移动浏览器
  点击、刷新、截图和控制台检查。
- 影响：本次只更新计划、架构、产品和测试文档，没有修改业务代码、依赖、镜像、ECS 或存储数据。
  开始 T9-4.4.1 前仍需用户明确发出开发指令；不得跳到 T9-4.5 或后续阶段。

## 2026-08-15 - T9-4.4.1 Knowhere 上游与契约审计

- 上游：刷新官方 `https://github.com/Ontos-AI/knowhere.git`，固定 Commit
  `2e4eb5846249d273b11902ee00f26db949e45b38`；记录 Apache-2.0 License/NOTICE SHA-256、17 个关键
  Git Blob 和旧审计 Commit。`THIRD_PARTY_NOTICES.md` 已同步，当前 Vendored source 为 0。
- 事实审计：上游 README 声称 CSV 支持但源码路由缺失；README 把 HTML 标为 Coming Soon 但源码已有
  Adapter/测试；JSON Adapter 返回空结果。后续能力声明必须以固定源码和本项目验收为准。
- 复用边界：格式 Router、Office 容器校验、DOCX/XLSX/HTML/图片/PPTX 算法列为参考或局部改写候选；
  继续使用本项目现有 MinerU 客户端；拒绝上游 DataFrame、Worker、PostgreSQL/S3、任务状态和全局配置。
- 契约：新增九格式显式矩阵、`semikb-ingest-v1` JSON Schema 和 16 个稳定错误码。输出固定为
  `ParsedDocument`、`ChunkDraft`、`ImageAssetDraft`、`TableAssetDraft`，并强制 Parser/Provider/
  upstream Commit/源 Hash Provenance。未知格式和信号冲突失败，不存在通用 MinerU 兜底。
- 依赖影响：后续只评估 python-docx、openpyxl、python-pptx、Pillow、BeautifulSoup/markdownify 和
  lxml；明确排除 Pandas、PyMuPDF、Java、LibreOffice、Tabula 及上游服务依赖。本阶段未安装任何依赖。
- 测试：`tests/test_t9441_contract_artifacts.py` 共 4 项，通过来源锁、路由矩阵、Schema 和错误目录校验；
  固定源码工作树的 Commit、License/NOTICE SHA-256 和 17 个 Git Blob 已逐项复算一致；后端全量测试
  `234 passed, 1 warning`，Ruff `All checks passed`。唯一警告为既有 Starlette/httpx 弃用提示。
- 影响与状态：未修改业务代码、镜像、ECS 或存储；T9-4.4.1 已完成，T9-4/T9 仍未完成。下一步只能
  在用户明确确认后进入 T9-4.4.2 包边界与格式路由，不得提前开发格式适配器或 T9-4.5。

## 2026-08-15 - T9-4.4.2 包边界与格式路由

- 包边界：新增独立顶层包 `semikb_ingest`，拆分 models、routing、service、parsers、providers、
  structure、assets 和 chunking；`pyproject.toml` 已把该包加入 Wheel。新包不反向依赖 `semikb` 业务层。
- 契约：把 `semikb-ingest-v1` 落实为禁止额外字段的 Pydantic 模型；补充 Chunk/图片/表格 ID 唯一性、
  引用完整性和进程内 BinaryPayload 大小/Hash 校验。`IngestDispatcher` 是唯一分派入口，统一计算源 Hash
  并验证 Parser 输出格式、Provenance 与输入一致。
- 路由：九格式注册表联合检查扩展名、MIME、PDF/PNG/JPEG/ZIP Magic Bytes 和 DOCX/XLSX/PPTX
  必需成员；无扩展名只在信号唯一时路由。只有 PDF Provider 为 MinerU，不存在跨格式默认回退。
- 安全：增加 100 MiB 源文件门禁，以及 OOXML Entry 数量、总展开大小、压缩比、路径穿越、Windows
  Drive Path、重复路径、加密标志和冲突 Office 家族检查；错误映射与 4.4.1 的 16 项目录一致。
- 依赖与许可：没有增加解析依赖、系统包、服务、端口或模型，也没有复制 Knowhere 源码；
  `THIRD_PARTY_NOTICES.md` 无需新增 Vendored 路径。
- 构建：基线 Wheel 178,298 bytes，当前 190,277 bytes，增加 11,979 bytes；83 个 Entry 中 15 个属于
  `semikb_ingest`。依赖和 Dockerfile 未变化，因此本阶段未重建 API/Worker 镜像或测空载内存。
- 测试：定向 `32 passed`，后端全量 `266 passed, 1 warning`，Ruff、运行时输出对冻结 JSON Schema
  验证、Wheel 内容检查和凭据扫描通过；唯一警告为既有 Starlette/httpx 弃用提示。
- 影响与状态：未修改现有入库发布链路、API、存储 Schema、ECS 或数据。T9-4.4.2 已完成，T9-4/T9
  仍未完成；下一步只能在用户明确确认后进入 T9-4.4.3 首期专用适配器，不得提前接入发布或部署。

## 2026-08-15 - T9-4.4.3 首期专用格式适配器

- 公共解析层：新增结构感知 Chunker、文本解码、表格 HTML/Markdown、图片尺寸/解压门禁、统一
  Provenance 和 Parser 工厂；表格/图片保持原子 Chunk，资产双向引用由统一完成器生成。
- 文本格式：Markdown 支持标题、列表、表格和代码块；TXT 支持 UTF-8/GB18030；HTML 清除脚本、
  样式和导航噪声，恢复标题、正文、列表、代码和表格，不抓取外部资源。
- Office/表格：DOCX 按 OOXML 块顺序恢复标题、段落、列表、表格和图片；XLSX/CSV 保留 Sheet、表头、
  单位、合并单元格、连续表格区域和公式缓存，隐藏 Sheet 跳过且不执行公式；PPTX 保留 Slide、标题、
  文本框、表格、图片、讲者备注和 bbox。
- Provider：新增隔离的 MinerU PDF API/结果 ZIP 客户端和 `qwen3.7-plus` 图片理解客户端；页码、表格、
  图片、图注、OCR 和检测摘要映射到统一契约。Provider 缺失、超时、429/5xx 和非法输出使用稳定错误码，
  不跨格式兜底。
- 资源：复用现有 Qwen Endpoint/Key，`.env` 与 `.env.example` 增加独立视觉模型名和超时；真实合成晶圆
  图在线冒烟通过，得到 1 个 `image_text` Chunk 和 1 个原图资产，字节往返一致。
- 依赖与许可：新增并哈希锁定 `python-docx`、`openpyxl`、`python-pptx`、`Pillow`、
  `beautifulsoup4`、`markdownify`、`lxml`；更新 `THIRD_PARTY_NOTICES.md`。没有复制 Knowhere 源码，
  没有新增服务、本地模型或 GPU。
- 构建与影响：Wheel 219,489 bytes，较 4.4.2 增加 29,212 bytes，共 96 个 Entry；Linux Python 3.12
  `requirements.lock --require-hashes` dry-run 通过。本机 Docker Linux daemon 未运行，本阶段也未改 ECS，
  因此 Docker 镜像实测留待 4.4.4 接线后、部署前完成。
- 测试：适配器定向 `17 passed`，4.4.1~3 组合 `53 passed`，后端全量 `283 passed, 1 warning`；Ruff、
  冻结 Schema、Wheel、凭据扫描及 Qwen 在线冒烟通过。唯一警告为既有 Starlette/httpx 弃用提示。
- 影响与状态：未修改现有 `rag_ingestion`、API、存储 Schema、ECS 或数据。T9-4.4.3 已完成，T9-4/T9
  仍未完成；下一步只能在用户明确确认后进入 T9-4.4.4 受控入库集成。

## 2026-08-15 - T9-4.4.4 受控入库集成

- 接线：新增 `SemikbIngestAdapter` 作为 `rag_ingestion` 唯一解析组合边界；九格式不再走旧的
  Markdown/通用 MinerU 二分支，统一消费 `semikb-ingest-v1`，同时保持原上传、Hash、Job/Event、
  Embedding、暂存、发布和补偿流程。
- 数据：新增纯映射层，将 Draft 转为 `DocumentRevision`、`Chunk`、`ImageAsset`、`TableAsset`；保存
  Parser/Provider/上游 Commit、Chunker、解析警告、指标和源位置。图片原字节与表格 JSON 使用稳定
  MinIO 路径，Milvus 仍只保存 Chunk 检索投影。
- 存储：MongoDB 新增 `table_assets` 契约及生命周期传播；提供独立
  `t9444_table_assets_migration` dry-run/apply/rollback，旧模型字段均使用向后兼容默认值，不改跨存储
  主键或 REST 路径。
- 稳定性：API 映射冻结 IngestError 到安全 HTTP 错误；Worker 把 Parser timeout/unavailable 纳入有界
  重试。失败补偿覆盖表格资产，Provider 恢复后可复用原件和同一 Job，重复提交不重复发布。
- 测试：定向 `61 passed`，后端全量 `302 passed, 1 warning`；Ruff、compileall、前端 lint/build、Wheel
  和凭据扫描通过。Wheel 228,166 bytes，共 96 个条目；没有新增运行依赖或服务。
- 限制：本机 Docker Linux daemon 未运行，未测镜像体积；本阶段未部署 ECS、未运行逐格式黄金文件、
  真实三存储、检索或浏览器验收，不能表述为线上多格式闭环。
- 状态：T9-4.4.4 已完成，T9-4/T9 仍未完成。下一步只能在用户明确确认后进入 T9-4.4.5 逐格式真实
  闭环，不得跳到 T9-4.5 或后续阶段。

## 2026-08-15 - T9-4.4.5 多格式真实闭环验收

- 黄金文件：新增九种确定性 CC0 合成文件和 manifest，冻结 SHA-256、Parser、最少 Chunk/图片/表格、
  必需/禁止语义和检索问题；PDF 根据真实 MinerU 表现发布 V2，而不是降低质量门禁。
- 真服务闭环：九格式通过真实 ECS API、Celery Worker、在线 MinerU/Qwen VLM/Embedding/Reranker 发布；
  MongoDB 9 Document/26 Chunk/5 Image/9 Table、MinIO 32 对象和 Milvus v4 26 行一致。9/9 检索命中，
  ACL 0 泄露，R1 隐藏/R2 命中，伪装 PDF 返回稳定 422。
- 浏览器：工作台文件选择器逐一上传九格式，任务中心均为发布 100%，阶段历史完整；代表性问答逐类
  命中，首图自动预览、多图切换、无图清空、Trace 和刷新恢复通过。桌面 `1280 x 720` 与移动
  `390 x 844` 无横向溢出，Console 0 error/0 warning，截图归档到 `docs/evidence/t9-4-4-5/`。
- 缺陷修复：上传幂等键冲突改为稳定 409，前端保留失败文件；Chamber 解析只接受短标识，避免把
  `chamber recovery/pressure/cleaning` 当槽位；结构化消息持久化受限证据账本，刷新后证据详情恢复，
  气泡路由仍清空调查数据。
- 部署：只重建 API/Worker/Web；六个受保护数据容器 ID 不变。最终 API/Web healthy、Worker pong，
  没有新增服务、本地模型、GPU、端口或存储迁移。
- 测试：后端全量 `313 passed, 1 warning`；Ruff、compileall、源码包/Wheel、前端 lint/build、
  `git diff --check` 和 40 个变更文本文件凭据扫描通过。唯一警告为既有 Starlette/httpx 弃用提示。
- 状态：T9-4.4.5 已完成，在当时只解除 T9-4.5 Provider 韧性门禁；T9-4/T9 仍未完成。后续新增的
  T9-4.4.6 计划已在下一条记录中接管当前门禁。

## 2026-08-15 - T9-4.4.6 真实公开语料与受控下架计划确认

- 目标：在扩充真实公开半导体语料前，先补齐可审计、可恢复的知识文档下架能力，避免误入库内容持续
  污染检索；同时建立面试演示问题集，明确每个问题展示什么能力。
- 计划：新增 `T9-4.4.6a` 来源/生命周期契约、`4.4.6b` 下架与恢复、`4.4.6c` 真实语料获取与标准化、
  `4.4.6d` 批量入库/黄金集/浏览器验收，完成后才允许进入 T9-4.5 Provider 韧性。
- 数据：首批候选包括 UCI SECOM、ST-AWFD、Carinthia SEM Defect、WM-811K 抽样、DeepPCB、
  SemiKong 论文/仓库/ontology 和 Etch/FDC/Yield/Wafer Map/SEM 检测论文。大型表格不逐行向量化，
  图片不虚构 Recipe 或根因，现有合成 SOP/Recipe/Case 继续明确标注 synthetic。
- 治理：许可证不作为面试 Demo 的唯一筛除条件，但 Source Manifest 必须保留来源、Hash、许可状态和
  再分发限制；受限原件只进私有 MinIO，不提交公开 GitHub。误入库默认 `withdrawn`，不做一键硬删除。
- 文档：同步更新分阶段计划、T9-4 验收计划、测试计划、架构设计、产品方案和样例数据说明；新增
  `docs/半导体Agent智库演示问题集.md`。
- 影响与状态：本次仅修改文档，没有修改业务代码、ECS、MongoDB、Milvus、MinIO 或现有知识数据。
  T9-4.4.6 尚未开发；下一步只有在用户明确发出开发指令后才进入 T9-4.4.6a。

## 2026-08-15 - T9-4.4.6a 来源与生命周期契约及迁移

- 契约：新增不可变 `SourceManifest`、来源许可/再分发/入库策略、预期资产、知识文档/revision 清单、
  下架/恢复请求、跨存储影响计数、补偿状态和生命周期操作记录；Python/OpenAPI/TypeScript 同步，旧
  `DocumentRevision` 与上传请求通过可选来源字段继续兼容。
- 治理：`DocumentLifecycle` 增加 `withdrawn`；大型表格策略强制
  `raw_row_vectorization=false`，只允许数据字典、数据画像和只读分析工具，图片语料必须建立
  `image_text`，仅参考来源不得生成检索投影。
- 来源登记：新增不可变 MongoDB `source_manifests` Registry，同版本同内容幂等、不同内容拒绝覆盖；
  发布现有合成语料基线 Manifest 和 6 份冻结 Schema，校验本地语料 SHA-256。
- 迁移：新增 `t9-4.4.6a-source-governance-v1`。历史 T2-G4/T6 迁移冻结各自索引范围，避免旧迁移版本
  意外吸收新索引。ECS 预演只含 6 个 `create`，正式应用成功，第二次预演为 0；35 条文档目录和 141
  条审计事件未改写，新来源集合保持 0 条。
- 部署与资源：只重建 API/Worker/Web；MongoDB、Milvus、双 MinIO、etcd、Redis 容器 ID 前后相同。
  API/Web healthy，Worker pong，公网健康接口 200；完整 storage verifier 全部通过。
- 测试：定向/存储回归 `52 passed`，后端全量 `325 passed, 1 warning`；Ruff、Manifest/Schema Hash、
  235,985-byte Wheel、前端 lint/build 和凭据扫描通过。唯一警告为既有 Starlette/httpx 弃用提示。
- 边界：本阶段没有开放知识文档 REST API，没有执行 revision 下架/恢复、Milvus 删除、MinIO 恢复、
  补偿任务或前端页面，也没有下载和入库真实公开语料。
- 状态：T9-4.4.6a 已完成，T9-4/T9 仍未完成。下一步只能在用户明确发出开发指令后进入
  T9-4.4.6b 受控下架与恢复，不得跳到 4.4.6c、4.4.6d 或 T9-4.5。

## 2026-08-16 - T9-4.4.6b 受控下架、恢复与知识文档管理

- 目标：在真实公开语料导入前提供可审计、可恢复的 revision 下架能力，避免误入库内容继续污染检索，
  同时保留 MinIO 原件用于复核和恢复。
- 后端：新增 MongoDB/Demo 知识文档 Repository 和 `KnowledgeDocumentLifecycleService`；下架先阻断
  Document/Chunk/图片/表格，再按稳定 Chunk ID 清理 Milvus；恢复重新校验审批、有效期、Scope、
  Source Manifest、原件/资产 Hash 和活动索引版本，并按 staged/published 重建向量。检索 Repository
  增加 MongoDB 防御性生命周期过滤，向量清理失败也不会重新暴露内容。
- API/Worker：新增文档清单、revision 清单、下架、恢复、操作查询和补偿重试 REST API；只允许
  `knowledge_admin/admin`，相同请求幂等复用、冲突请求返回 409。Celery 新增
  `semikb.knowledge_document.lifecycle`，MongoDB 操作记录是业务状态真相。操作执行由 MongoDB 原子
  `execution_id` 占用，阻止重复 Worker 并发发布；该字段只在 Repository 内部使用，读取公共契约前剥离。
- 前端：入库任务中心新增“知识文档”页签，展示 revision、生命周期、来源、Chunk/图片/表格数和最近
  操作；管理员填写原因后下架/恢复，真实轮询操作终态，需要补偿时提供重试入口。
- 迁移：新增 `t9-4.4.6b-lifecycle-operations-v1`，只创建 4 个批准索引。ECS 首次 dry-run 只有
  4 个 `create`，应用后再次 dry-run 为 0；回滚发现业务记录时拒绝删除 Collection。
- ECS：真实 `CASE-FA-2026-004 R1` 的 5 Chunk/2 图片完成下架和恢复。下架后 Milvus 无对应 Chunk、
  RAG 不命中、图片 HTTP 403；恢复后重新命中、图片 HTTP 200，MinIO 原件 Hash 不变，最终为
  `published`。普通工程师访问清单为 403，相同请求复用、同 ID 改原因返回 409。
- 部署：只重建 API/Worker/Web；MongoDB、Milvus、双 MinIO、etcd、Redis 六个受保护容器 ID 前后
  一致。API/Web healthy，Worker pong 且新任务已注册，Storage Verifier 与公网健康通过。
- 浏览器：本地工作台实际完成 `published -> withdrawn -> published` 点击；活动向量 `5 -> 0 -> 5`，
  桌面和 `390 x 844` 移动视口无页面横向溢出，Console 0 error/0 warning。公网 Web 已服务新构建；
  Codex 隔离浏览器无法使用安全访问码能力，因此未绕过登录，ECS 业务链路由真实 API/Worker/存储验收。
- 真机修复：ECS 首轮并发保护复验发现 MongoDB 私有 `execution_id` 被严格 Pydantic 契约拒绝；已在
  Repository 反序列化边界过滤 `_id/execution_id`，补充回归测试并安全恢复验收 revision。修复后的
  全新随机请求再次完成下架/恢复闭环，应用日志无错误标记。
- 测试：后端全量 `337 passed, 1 warning`；Ruff、前端 ESLint/TypeScript/Vite build、迁移幂等、
  重复 Worker 投递、Mongo 私有字段边界、Storage Verifier、日志检查和凭据边界通过。唯一警告为既有
  Starlette/httpx 弃用提示。
- 状态：T9-4.4.6b 已完成，T9-4/T9 仍未完成。下一步只有在用户明确发出开发指令后才进入
  T9-4.4.6c 真实公开语料获取与标准化；不得跳到批量入库、4.4.6d 或 T9-4.5。

## 2026-08-16 - T9-4.4.6c 通用性与数据泄漏审计编排

- 目标：按“未知未来上传文件、未知未来问题”重新审计 4.4.6c 和前序方案，禁止为了选定公开来源或面试
  问题制作专用解析、Chunk、路由、分数、召回结果和答案。
- 发现：公共上传/Actor/Document/Chunk 等契约仍含 `demo_engineering/FAB-01/P-ALPHA/approved/published`
  默认值；首次发布没有完整复用 Source Manifest 恢复门禁；受保护证据仍依赖 `SOP-` ID 前缀；现有
  EvaluationDataset 未区分开发、校准、回归与 holdout，且既有 Rerank 阈值曾用冻结黄金集校准。
- 编排：不新增脱离主线的大阶段。`T9-4.4.6c-1` 先完成通用契约、Manifest 首发门禁、治理字段检索策略、
  上传 UI 和评测契约加固；`T9-4.4.6c-2` 再建立通用 Corpus 标准化服务，并让候选公开来源通过同一
  API/Worker、声明式 Sidecar/Profile 和暂存复核流程。`T9-4.4.6d` 负责批量发布、四类评测数据、密封
  盲测、演示回归和浏览器验收。
- 评测边界：九格式黄金文件、`demo-v1/v2`、意图 v1/v2/v3 和未来 `demo-public-v1` 保留为集成、演示或
  回归证据，不再解释成无偏泛化结果。参数冻结后才允许打开 holdout；查看结果后修改实现必须发布新
  holdout 版本。
- 影响：本次只更新计划、验收、测试、样例数据和开发记录，没有修改业务代码、依赖、数据、ECS 或三类
  存储。下一步仍需用户明确发出开发指令，才能开始 `T9-4.4.6c-1`，不得先下载并特化候选语料。

## 2026-08-16 - T9-4.4.6c-1 通用化基线实现与验收

- 目标：在接触候选公开语料前消除演示默认值、来源命名启发式、首次发布门禁缺口和评测数据混用，确保
  未知未来文件和问题不会被 Demo 元数据或预置规则静默污染。
- 契约：公共 Actor 默认匿名无权限；上传、Document、Chunk、Image/Table 使用中性治理默认值，新上传
  默认为 `draft + staged`。新增 `retrieval_policy`、`retrieval_mode` 和评测用途/密封/打开/来源快照/
  泄漏字段；旧冻结 Demo 和意图数据 Hash 保持兼容。
- 入库：新增显式 Demo Factory；首次发布和恢复共享 Source Manifest 门禁，统一校验审批、Scope、许可、
  再分发、数据集版本、原件/快照 Hash 及允许的索引产物。未通过门禁的发布请求关闭失败，普通暂存不会
  进入活动 Milvus 投影。
- 检索与评测：受保护证据改由治理字段决定，不再依赖 `SOP-` ID 前缀；HyDE 和图片加权使用结构化任务
  模式。holdout 必须在密封和泄漏复核通过后运行，并且首次打开时间只写一次，运行保存治理快照。
- 前端：上传页默认展示“待复核”及空 Scope/Fab/Product/来源字段；只有点击“加载演示配置”才显式填入
 合成 Demo 治理值。离线评估页展示数据集用途和运行治理快照。
- ECS：仅重建 API/Worker/Web，六个 MongoDB/Milvus/MinIO/etcd/Redis 受保护容器 ID 前后相同；未知
  元数据文件通过真实 API/Worker 后停留在 MongoDB/MinIO 暂存，最终 Milvus 0 行。API/Web healthy、
  Worker pong、完整 Storage Verifier 通过。
- 浏览器：公网 Chrome 实际检查任务中心、上传默认值、显式 Demo 配置和离线评测治理；桌面与精确
  `390 x 844` 截图无横向溢出或内容遮挡，Console 0 error/0 warning，截图归档到
  `docs/evidence/t9-4-4-6c-1/`。
- 测试：后端全量 `345 passed, 1 warning`，Ruff、compileall、11 份版本化 Schema 校验、前端 lint/build、
  差异检查和凭据扫描全部通过；唯一警告为既有 Starlette/httpx TestClient 弃用提示；
  详细计数、浏览器步骤和证据见 `docs/T9-4.4.6c-1通用化基线验收.md`。
- 边界：没有下载或入库公开语料，没有实现 Corpus 标准化服务、批量发布或密封盲测，也没有进入
  `T9-4.4.6d` 或 `T9-4.5`。下一步需用户明确指令后才允许开始 `T9-4.4.6c-2`。

## 2026-08-16 - T9-4.4.6c-2 通用 Corpus 标准化与真实资料暂存

- 目标：让未知未来多文件、ZIP、文档、表格、标签和图片使用同一套通用标准化能力，不针对首批公开
  来源或演示问题编写特殊处理，并在人工审核前彻底隔离知识发布。
- 契约与存储：新增严格 Corpus/Sidecar/Profile/Job/Report/File/Relation 模型、MongoDB
  `corpus_standardization_jobs`、MinIO `corpora/{corpus_id}/{snapshot_hash}/{category}` 稳定前缀和只增
  四索引迁移。报告只保存有界画像和安全摘要，不保存表格原始行、Prompt 或密钥。
- 处理：实现不可变快照、ZIP 路径/大小/压缩比/加密/嵌套/脚本门禁、稳定 MIME、统一 Parser
  Dispatcher、Markdown 标准化、表格/标签画像、图片客观 `image_text`、显式跨文件关系、警告和幂等
  重试。`.data` 只通过通用声明式分隔符/表头规则处理。
- API/Worker/UI：新增管理员上传、列表、详情、重试 API 和 `semikb.corpus.standardize`；任务中心新增
  “语料标准化”页签，可上传多文件/ZIP、填写中性元数据和 Sidecar/Profile，并查看文件清单和阶段事件。
- 真实资料：UCI SECOM ZIP 被识别为表格、标签和安全保留文件；arXiv 2411.11029 PDF 被识别为文档；
  浏览器普通 Markdown+CSV 被识别为 mixed。三者全部走同一入口，没有来源专用分支。
- 隔离证明：两条公开资料任务前后 `document_catalog=36`、`chunk_catalog=87`、Milvus 活动行 `123`
  不变；只增加 2 条标准化任务、5 个 raw 和 5 个 derived MinIO 对象。所有任务为 `review_required`。
- ECS：迁移首次只有四个批准索引，应用后再次 dry-run 为 0；只重建 API/Worker/Web，六个受保护数据
  容器 ID 不变，API/Web healthy，Worker 注册新任务。
- 浏览器：公网实际上传、Worker 完成、刷新恢复、逐文件审核清单均通过；桌面 `1440 x 900` 和精确
  `390 x 844` 无横向溢出，Console 0 error，截图归档到 `docs/evidence/t9-4-4-6c-2/`。
- 测试：后端全量 `360 passed, 1 warning`；Ruff、compileall、前端 lint/build、差异检查和凭据扫描
  通过。唯一警告为既有 Starlette/httpx TestClient 弃用提示。
- 状态：T9-4.4.6c-2 已完成，T9-4/T9 仍未完成。下一步只有在用户明确发出开发指令后才允许开始
  `T9-4.4.6d` 批量发布、密封盲测与演示验收；不得提前发布公开语料或进入 T9-4.5。

## 2026-08-16 - T9-4.4.6d 批量发布、密封盲测与演示验收

- 目标：把已标准化且审核通过的未知 Corpus 通过同一通用 API/Worker 发布，冻结代码和检索配置后打开
  私有 holdout，并完成任务中心、评估、公开语料问答、图片和 Trace 浏览器闭环。
- 契约与实现：新增 Corpus Publication Batch、发布 Item/事件/对账、Evaluation Release Freeze 契约，
  MongoDB Repository、只增索引迁移、Celery `semikb.corpus.publish`、管理员 API 和前端发布复核界面。
  发布服务复用标准 Document/Chunk/Image/Table 和现有受治理入库，不接受预制 Chunk 或客户端指定结果。
- 真实发布：UCI SECOM 2 项/18 Chunk、arXiv 2411.11029 1 项/37 Chunk、私有混合 holdout 4 项/
  11 Chunk 全部完成 MongoDB/MinIO/Milvus 对账。表格仍只发布说明和有界画像，不逐行向量化。
- 盲测：发布冻结 Hash 为
  `0dd1c8cd55215e9a7a1ba3c1fd3bc7a27f9ac4af6a6679c53faddd599acd2c61`。有效 v2 holdout 首次打开后
  5/6 通过，唯一失败为无证据问题误命中表格画像；没有在已打开 v2 上继续调参。字符编码损坏且 Actor
  Scope 不匹配的首次 v1 标记为无效运行，不计入指标。
- 浏览器发现与修复：旧卡把公开资料查询过度引向 Web，且未理解“不要使用 Web”。新增通用
  `semikb-intent-catalog-v4` 受治理语料卡和 Web 否定规则，没有出现 `SECOM`、论文标题或演示问法专用
  分支。v1/v2/v3 冻结文件保持原 Hash，通过版本化覆盖层兼容。
- ECS：公网任务中心、离线评估、内部公开语料问答、证据抽屉、Trace、首图自动预览、刷新恢复和桌面/
  移动点击通过；最终干净页面 Console 0 error。只替换 API/Worker，必要时重建 Web；六个受保护数据
  容器 ID 不变，最终服务健康且日志无错误标记。
- 测试：后端全量 `374 passed, 1 warning`；Ruff、前端 lint/build、冻结意图 v1/v2/v3、在线 v4 探针、
  差异检查与凭据扫描通过。唯一警告为既有 Starlette/httpx TestClient 弃用提示。
- 证据：`docs/T9-4.4.6d批量发布密封盲测与演示验收.md` 和 `docs/evidence/t9-4-4-6d/`。
- 状态：T9-4.4 已完成，T9-4/T9 仍未完成。下一步只有在用户明确发出开发指令后才可进入
  `T9-4.5` Provider 韧性，不得跳到容量、冷备或最终验收。

## 2026-08-16 - T9-4.5 Provider 韧性

- 目标：统一 Embedding、Reranker、LLM、MinerU、OCR/VLM 和 Web MCP 的超时、429、5xx、非法响应、
  重试、降级、失败关闭和审计语义，不增加新基础设施或本地模型。
- 实现：新增独立 `semikb_provider_resilience` 包、稳定失败分类、有界同步/异步重试和
  `semikb-provider-attempt-v1`；接入检索 Trace、入库 Job/Event、流式/直接回答审计和前端 Trace 页面。
- 边界：Embedding 失败关闭；Reranker 降级 RRF；LLM 仅在首 Delta 前切换 Provider；MinerU 创建批次
  不重放；VLM 非法结构失败不发布；Web 失败只用内部证据。审计不保存 URL、Key、Prompt 或正文。
- 自动测试：后端全量 `382 passed, 1 warning`；定向故障矩阵 10 项、Provider/入库/检索 58 项、
  Direct Reply 14 项、冻结意图 46 项通过；Ruff、前端 lint/build、凭据扫描和差异检查通过。
- ECS：六类真实 Provider 烟雾全部成功；API/Web healthy、Worker running、应用日志无错误标记；只替换
  API/Worker/Web，六个受保护数据容器 ID 前后完全一致。
- 浏览器：公网工作台新建调查并完成真实问答、SSE、结构化证据、Trace 和 Provider 尝试区块验收，
  Console 0 error/warning。内置截图命令对任意页面均超时，因此不伪造截图，留到 T9-4.9 补录。
- 证据：`docs/T9-4.5Provider韧性验收.md` 和 `docs/evidence/t9-4-5/`。
- 状态：T9-4.5 已完成，T9-4/T9 仍未完成。本记录形成时下一步为 `T9-4.6`；同日后续发现通用澄清
  闭环缺陷，现已由下一条记录新增 `T9-4.5.1` 接管 T9-4.6 前置门禁。

## 2026-08-16 - 编排 T9-4.5.1 通用澄清状态机与多轮槽位闭环修复

- 触发：公网对话中，用户对目标消歧问题给出合法回答后，系统仍重复追问同一临时字段并在两轮后停止。
- 补充触发：只读代码审计和内存服务复现确认，线程进入 `waiting_for_clarification` 后，完整的新 SOP 问题、
  能力咨询和“别查了”都会被无条件送入旧 Checkpoint，分别继续第 2 轮澄清；现有取消测试未覆盖中断恢复态。
- 结论：这是任务级澄清、候选路由、真实缺失槽位和 LangGraph 恢复之间的通用状态迁移缺陷，不按单句
  或关键词修补。
- 编排：新增版本化 `ClarificationFrame`、目标消歧与槽位补充分类、结构化回答解析、进展约束、旧
  Checkpoint 兼容、脱敏审计和版本化回滚开关。原问题与补充回答分别保留，不再依赖文本拼接作为状态真相。
- 退出机制：恢复前新增受控关系判定，区分继续当前、取消当前、替换为新任务、普通插话和歧义；取消立即
  结束旧 Frame，新任务独立路由并重置轮次，插话不自动重复追问，终态 Frame 不得被重放或迟到恢复激活。
- 复杂度边界：明确控制表达优先复用 L0，其余自然输入由现有一次结构化理解产出关系候选并由服务端校验；
  不增加第二次意图 LLM、不穷举未来问题，也不引入新基础设施。
- 非回归：开发前先冻结现有行为和调用计数；`chat_direct`、历史、反馈、取消、拒绝、RAG、Tool、Web、
  证据复用、图片、SSE、Provider 韧性、消息展示、旧 Mongo/Trace/Checkpoint 全部进入回归矩阵。
- 验收：不得产生非法槽位或连续相同澄清；部分补充只追问剩余项；未补齐时全部下游调用为 0；只更新
  ECS 应用容器，受保护数据容器 ID 不变，并完成桌面/移动浏览器与回滚验收。
- 文档：`docs/T9-4.5.1通用澄清状态机与多轮槽位闭环修复计划.md`。
- 状态：T9-4.5.1 已编排但未开始。只有用户明确发出开发指令后才能实施；T9-4.5.1 验收前不得进入
  T9-4.6 容量与运行韧性。

## 2026-08-21 - T9-4.5.1 通用澄清状态机与多轮槽位闭环修复

- 目标：把无条件恢复旧 Checkpoint 的澄清流程改为版本化、可验证的状态机，使部分补充、乱序补充、
  取消、换题、普通插话、继续原任务和旧数据恢复都具有明确迁移语义。
- 契约：新增 `semikb-clarification-frame-v1`、待解决/已解决项、Frame 生命周期、恢复关系枚举和脱敏
  Transition Audit；旧 MongoDB、Trace 和 Checkpoint 通过兼容适配器读取，无破坏性数据迁移。
- 编排：结构化理解在同一次 LLM 调用中给出关系候选，服务端校验取消 Grounding、合法槽位、实体来源和
  缺失条件；LangGraph 增加恢复闸门、继续/取消/替换/插话/歧义分支以及轮次和进展约束。
- 通用修复：线上发现模型把领域词“清腔”误判为取消，现限制只有完整匹配明确取消表达才能建立取消
  迁移；另发现插话分支仍携带旧请求和任务结果，现统一重置当前执行态但保留暂停 Frame。两项均增加
  特征测试，没有为固定问题、来源或预期答案建立生产特判。
- 展示：澄清、取消和普通插话继续使用自然气泡，RAG/Tool 完成结果继续使用结构化卡片；前端只显示
  产品名称、时间范围、机台/腔室等用户字段，不泄露 `request_goal` 一类内部名。
- 自动测试：后端全量 `399 passed, 1 warning`；独立澄清评估 8 场景/12 轮全部通过，完成率、关系准确率、
  路由准确率均为 `1.0`，新任务误吸收、取消失败、跨 Frame 污染、重复澄清、内部字段泄露和未补齐
  下游误调用均为 `0`。Ruff、前端 lint/build、差异检查和凭据扫描通过。
- 冻结回归：`semikb-intent-v1/v2/v3` 分别以原 Hash 完成 96/99/108 Case 回归，路由准确率均为
  `1.0`，危险执行漏拒和新产生 `history_direct` 均为 `0`。
- ECS：只替换 API/Worker/Web；API/Web healthy、Worker ping 返回 `pong`，版本化开关启用和关闭回滚
  均实测通过。MongoDB、Milvus、项目 MinIO、Milvus MinIO、etcd、Redis 六个数据容器 ID 前后不变。
- 浏览器：亲自完成完整新问题替换旧澄清、两轮槽位积累、普通插话、继续刚才、明确取消、歧义换题、
  刷新恢复、结构化卡片/自然气泡区分和真实 SSE 检查；桌面与精确 `390 x 844` 视口无横向溢出，
  Console 0 error/warning。截图归档到 `docs/evidence/t9-4-5-1/`。
- 证据：`docs/T9-4.5.1通用澄清状态机验收.md`。
- 状态：T9-4.5.1 已完成，T9-4/T9 仍未完成。下一步为 T9-4.6 容量与运行韧性，但只有用户明确
  发出开始开发指令后才允许实施。

## 2026-08-23 - T9-4.6 容量与运行韧性

- 目标：在不引入新基础设施、不制造无界远程 Provider 负载的前提下，验证 4 vCPU/8 GiB 测试 ECS
  同时承载 3 个在线会话、1 个入库和 1 个 Full 评估的能力，并完成断连、日志和逐服务重启闭环。
- 实现：新增有界容量/SSE 时序工具、宿主机与容器采样及摘要工具、跨存储状态快照和逐服务重启脚本；
  Worker 固定 `--concurrency=1`。生产入库存储补齐 Provider 尝试审计转发，失败重试保持幂等。
- 负载结果：按 `1 -> 2 -> 3` 完成 6/6 在线请求，错误率、超时和 5xx 均为 0；入库发布 1 个 Chunk，
  Full 评估完成 8 个 Case。accepted P50/P95 为 `21.38/40.05 ms`，首 Delta 为
  `14.68/18.55 s`，端到端为 `17.32/21.38 s`，检索为 `3.06/15.39 s`。
- 资源结果：31 个采样点中最低可用内存约 5.06 GiB、最大 Load 0.57、根盘变化约 -1.4 MiB；无 OOM
  或意外重启。所有容器使用 `json-file` 的 `20m x 3` 日志轮转，当前最大单日志约 10.2 MiB。
- 韧性：SSE 断连后请求进入 `cancelled`，同一线程恢复成功。九个服务完成两轮受控逐项重启；修复验收
  脚本对无 Health 字段、Worker ping 和 `pipefail + grep -q` 的假失败后，重启流程连续通过。
- 数据一致性：MongoDB 12 个关键集合、Milvus Active Alias 与 Strong 逻辑行数 149、MinIO 56/91 个
  对象和 Redis 空队列在稳定重启前后完全一致。Milvus 物理统计的暂时变化经 Strong `count(*)` 证明为
  Compaction/Tombstone 时序，不作为业务数据差异。
- 浏览器：公网工作台观察到真实 Live/Delta 状态，结构化 RAG 结果完成后刷新内容一致；桌面与精确
  `390 x 844` 视口通过，Console 0 error/warning。
- 自动测试：后端全量 `411 passed, 1 warning`；T9-4.6/部署脚本定向测试、Ruff、compileall、前端
  lint/build、差异检查、`.env` 忽略和凭据扫描通过。唯一警告为既有 TestClient 弃用提示。
- 证据：`docs/T9-4.6容量与运行韧性验收.md` 和 `docs/evidence/t9-4-6/`。
- 风险与状态：当前 8 GiB 满足面试 Demo 档位，但不代表生产容量；首 Delta P95 18.55 秒仍需后续优化，
  测试机无 Swap。T9-4.6 已完成，T9-4/T9 仍未完成；下一步只能在用户明确发出开始开发指令后进入
  T9-4.7 冷备恢复。

## 2026-08-23 - T9-4.7 冷备恢复

- 目标：验证单机 Compose 的真实停写冷备可以恢复到独立空目录和独立 Project，并以内容级证据证明
  Checkpoint、任务、向量索引、对象资产和检索能力可恢复，而不是只证明归档文件存在。
- 实现：加固 `backup_cold.sh`、重写 `restore_cold.sh` 为只准备独立副本，新增 Marker 保护的清理脚本、
  Compose 恢复环境注入和 `verify_t947_restore.py` 跨存储指纹工具；线上默认 `.env` 行为不变。
- 验证修正：最初烟雾使用旧 `t5-live-v1` 匿名 Case，被 ACL 正确关闭且旧期望 Chunk 已不是当前回归
  证据。验证器改用当前冻结 `demo-v2` 首个 Case 及其显式 Actor Scope，没有修改黄金集、权限策略或
  硬编码召回结果；该 Case 在线和恢复副本均命中 `SOP-ETCH-03-R2-001`。
- 备份结果：521 个文件/633,361,069 bytes 生成 190,703,528 bytes 归档，SHA-256 与同期环境副本校验
  通过；从停写到主栈全健康 67 秒。环境副本、Manifest 和恢复环境权限均为 `0600`。
- 恢复结果：独立副本准备 6 秒、启动 33 秒；MongoDB 17 个业务集合共 21,326 条记录 BSON Hash、
  Milvus Strong 149 行与元数据 Hash、MinIO 56/91 个对象的全内容 Hash、Redis 空队列均与备份点一致。
- 安全门禁：篡改校验和、非空目标和线上数据根三类恢复请求均被拒绝且未生成恢复环境；恢复服务无宿主机
  端口，所有可写挂载位于独立恢复根。双栈并存时约 4.8 GiB 可用内存，无 OOM。
- 清理与非回归：清理后恢复容器、网络均为 0，恢复数据根和环境不存在；线上六个数据容器 ID、项目
  `.env` Hash 和四类存储指纹与备份点一致，API/Web 健康、Worker ping 为 `pong`。
- 自动质量：后端全量 `416 passed, 1 warning`，T9-4.7/部署契约定向 `16 passed`；Ruff、compileall、
  前端 lint/build、生产 Compose 解析、JSON 证据解析、差异检查、`.env` 忽略和凭据扫描通过。唯一警告
  为既有 Starlette TestClient/httpx 弃用提示。
- 证据：`docs/T9-4.7冷备恢复验收.md` 和 `docs/evidence/t9-4-7/`。真实环境副本和密钥未进入 Git。
- 状态：T9-4.7 已完成，T9-4/T9 仍未完成。下一步只能在用户明确发出开始开发指令后进入 T9-4.8
  安全与可迁移部署。

## 2026-08-23 - T9-4.8 安全与可迁移部署

- 目标：完成测试 ECS 的公网面、身份权限、访问码、短时资产、日志和应用容器最小权限复核，并生成
  不依赖 Docker Hub 的固定镜像离线迁移产物。
- 身份与权限：生产 Demo Token 不再信任客户端自报用户、角色或 Scope，固定签发服务端 Scope；生产
  seed 入口关闭。无效/过期 JWT、工程师越权和跨用户线程访问分别被 401/403/404 拒绝。
- 入口与资产：公网仅监听 22/80，所有数据端口未发布；访问码缺失/错误返回 401，连续错误触发 429。
  图片短时地址约 300 秒，授权时 200、未授权和过期后均 403。
- 日志与运行权限：验收发现 Nginx 默认 `$request` 会记录图片签名查询串，改为仅记录 `$uri` 并移除
  Referer 后，真实密钥对 Git、应用日志、镜像配置和前端静态文件精确扫描为 0 命中。API/Worker/Web
  均为非 root、只读根文件系统、`cap_drop=ALL`、`no-new-privileges=true`。
- 离线迁移：修复 Compose 本地构建服务没有显式 `image` 字段时的镜像名解析；以提交
  `0f4b57806fc584867229edbe116d0e5b4bdc8e0f` 生成 10 镜像、3,813,758,976 bytes 的离线包。
  删除专用 offline alias 后仅凭 `images.tar` 重载，文件 Hash 和所有镜像 ID 均一致。
- 非回归：只替换 API/Worker/Web，六个数据容器 ID 不变；MongoDB 17 个业务集合、Milvus Strong
  149 行、MinIO 56/91 个对象、Redis 状态与部署前指纹完全一致，`demo-v2` 检索烟雾通过。
- 自动质量：后端全量 `427 passed, 1 warning`；Ruff、前端 lint/build、生产 Compose、部署脚本语法、
  安全黑盒、离线重载、凭据扫描和公网健康检查通过。唯一告警为既有 TestClient 弃用提示。证据见
  `docs/T9-4.8安全与可迁移部署验收.md` 和
  `docs/evidence/t9-4-8/`。
- 风险与状态：测试期仍保留 HTTP、公网 22 密码登录和 MongoDB 单节点三项已批准例外；正式发布前
  必须关闭。T9-4.8 已完成，T9-4/T9 仍未完成；下一步只能在用户明确发出开始开发指令后进入
  T9-4.9 最终演示验收。

## 2026-08-23 - T9-4.9 最终演示验收

- 目标：固化一键最终验收、8~10 分钟演示顺序、故障回退和迁移交接，并以自动化与真实浏览器证据
  判断一期是否达到可重复面试 Demo 标准。
- 实现：新增 9 场景最终 Demo 验证器、六类结论汇总器和受控 Shell 入口；操作手册覆盖启动前检查、
  页面判断、故障处置、回退和迁移。离线打包器新增 Git Commit 前置验证，避免无效引用产生大型半成品。
- 通用 Bad Case 修复：公开语料有界表格画像必须保留 `observed/preview/truncated` 限定；证据追问走
  L0 时继承上一轮可执行任务类型和有效槽位，SOP 追问不再退化成 `general/explain`。没有修改冻结数据、
  Hash、来源名路由或预期答案。
- 本地质量：后端全量 `445 passed, 1 warning`，Ruff 全库通过；前端 ESLint、TypeScript、Vite build
  通过；意图 v1/v2/v3 和离线包契约回归通过。唯一警告为既有 TestClient/httpx 弃用提示。
- ECS 自动验收：9 个对话场景全部通过；`final_demo/security/offline_bundle/storage_invariants/worker/health`
  六类总门禁全部为真，存储差异为空。公网只监听 22/80，短时图片授权/未授权/过期分别为
  200/403/403，Worker 返回 `pong`。
- 浏览器：亲自完成自然聊天、结构化 RAG、证据复用、历史、澄清、图片、多任务、Trace、任务中心和
  评估页点击；桌面与 `390 x 844` 移动布局通过，自动打开首张 320 x 320 图片，Console 0 error/warning。
- 发布与迁移：只替换 API/Worker/Web，六个数据容器 ID 和跨存储指纹不变。最终离线包固定提交
  `1d1d1b604b926709c865cea54465faa58e93cd3b`，含 10 个唯一镜像；`images.tar` 为
  3,825,176,576 bytes，SHA-256 为
  `42ec100007c5770f70dc87ecf499661e2f5280848f2333ec7a237c9e62bad5c4`。
- 证据：`docs/T9-4端到端验收报告.md`、`docs/T9-4.9最终演示操作手册.md` 和
  `docs/evidence/t9-4/`。
- 状态：T9-4 与 T9 已按可重复面试 Demo 标准完成。有效 v2 holdout 仍为 5/6；HTTP、公网 SSH
  密码登录、MongoDB 单节点及当前容量边界仍是正式生产阻塞项，不能描述为已生产上线。
