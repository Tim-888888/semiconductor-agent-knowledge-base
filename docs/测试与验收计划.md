# 测试与验收计划

## 自动化验证

| 范围 | 测试 | 通过标准 |
| --- | --- | --- |
| 入库 | 哈希幂等、可重放对象、质量门禁、失败补偿、队列投递失败 | 重试不重复写入，失败 Revision 和 staged 向量不可检索 |
| 检索 | SOP 版本、权限、图文召回 | 命中现行 SOP；无权图片返回 403 |
| T6 澄清恢复 | 真实 `interrupt/resume`、最多两轮澄清、跨服务实例恢复 | 关键约束不足时不调用下游；同一线程从 MongoDB Checkpoint 恢复 |
| T9-4.3 通用连续会话 | 历史回顾、上下文承接、摘要边界、刷新恢复、线程隔离、同线程并发 | 精确回忆最近消息；指代来源可追踪；新线程/其他用户不可见；消息和上下文不乱序 |
| T9-4.3 按需路由 | L0、任务/非任务/混合、受控多任务、情绪解耦、槽位纠正/取消、三层识别、确定性路由和下游调用计数 | 历史/普通聊天不检索；混合输入不漏任务；最新知识重新检索；纠正使依赖状态失效；缺槽位先澄清；越权项单独拒绝 |
| Agent 证据 | 账本闭合、外部来源降级、只读工具参数 | 事实引用均存在；外部资料不能覆盖内部证据；任意额外工具参数被拒绝 |
| 长期记忆 | 显式批准、用户隔离、TTL | 不自动沉淀对话；未批准或过期内容不进入上下文 |
| 流式对话 | SSE 事件顺序、UTF-8 分片、心跳、取消、断连、错误与最终持久化 | 页面立即显示用户消息和阶段状态；答案按真实 Token Delta 增量显示；无重复消息、残缺最终答案或隐藏推理泄漏 |
| Trace | 用户隔离 | 非管理员不能读取其他用户 Trace |
| 评估 | 开发/校准/回归/密封盲测分层、异步运行、基线差异、权限 | 每个 Case 保存 Trace；输出 Recall@5/MRR/nDCG@5、负例准确率、图片召回和延迟；记录密封/首次打开/泄漏状态；非管理员返回 403 |
| 前端 | 构建 | TypeScript/Vite 无构建错误 |
| 部署制品 | 配置生成、Compose、网络、端口、镜像、资源和恢复门禁 | 不复制 SSH/旧地址；只有 80 发布；空目标才能恢复 |

执行：`conda run -n dl python -m pytest -q`、`npm run build`、`docker compose config`。

T9-4.1 还需执行 `conda run -n dl python scripts/verify_llm_streaming.py --provider all`。两家 Provider
必须分别返回至少一个可见 `content` Delta 和明确终止信号；组合命令禁止 fallback，并且任何一家失败
都返回非零。单元测试必须覆盖网络分片切开 UTF-8 字符、`[DONE]`、`finish_reason + EOF`、无终止残流、
隐藏 reasoning 不进入可见内容、网络错误脱敏和 SSE 事件判别/顺序。

T7 真实验收还需启动 `solo` Celery Worker，执行 `scripts/verify_t7_evaluation.py` 和
`scripts/verify_t7_api.py`。前者验证四档真实检索、baseline、MongoDB 重启可见性和 Trace 闭合；
后者验证 JWT 权限、`202 queued`、Worker 消费以及 API 持久读取。详见 `docs/T7离线评估验收.md`。

## T4 真实存储验收

前置检查：

```powershell
conda run -n dl python -m semikb.storage.verifier
conda run -n dl python scripts/verify_t4_ingestion.py
```

验收脚本只使用 `T4-CASE-ETCH-03` 和 `T4-RETRY-SOP-ETCH-03` 两条 CC0 合成资料。它验证
当时的真实 BGE-M3 Dense/Sparse 编码、MinIO 原件/解析件/图片回读、MongoDB Job/Event/Catalog、
Milvus staged/published 生命周期、活动 Alias、失败后跨服务实例重试和重复提交幂等性。

2026-08-11 实测：2 个 Document、5 个 Chunk、1 个 Image、2 个 Job；一个 Job 在注入
Embedding 失败后第 2 次成功；20 条镜像事件与 Job 内事件一致；5 个 MinIO 对象可读取；
5 条 Milvus `v2` 记录均为 `published`；Alias 指向 `semikb_chunks_v2`。脚本连续执行两次，
Job ID、数据数量和尝试次数不变。

另以 `DEMO_MODE=false` 启动独立 Celery `solo` Worker，通过真实 Redis 投递并消费一条临时
合成 SOP，Worker 完成当时的 BGE-M3 和三存储发布。该测试同时发现并修复了 Celery App 未显式加载
任务模块的问题；自动测试会检查 `semikb.ingestion.process` 和 `semikb.ingestion.retry` 已注册。
临时验收数据和 Worker 进程在验证后已精确清理。

2026-08-12 当前基线已迁移为在线 `qwen3.7-text-embedding` 原生 Dense/Sparse，活动索引为
`semikb_chunks_v4`。迁移验收覆盖在线 API、候选构建、行读回、自检索、Alias 回滚保护、
发布前后 T5/T7 黄金集、跨 Worker 新文档入库和 T8 浏览器复验，详见
`docs/Qwen原生Sparse迁移验收.md`。

## T6 真实 Agent 验收

```powershell
conda run -n dl python -m semikb.storage.verifier
conda run -n dl python scripts/verify_t6_agent.py
```

脚本先提交缺少 Product、时间和 Tool 的良率问题，确认图经 `interrupt()` 暂停并写入 MongoDB；
然后重建 ConversationService，用同一 `thread_id` 和 `Command(resume=...)` 补充条件。完成路径必须
包含 T5 内部 Chunk、证据账本、引用、模拟只读制造工具、审计和显式批准长期记忆。脚本拒绝无内部
受控证据或声明“根因是”的结果，并在 `finally` 中按临时 ID 清理线程、Checkpoint、Trace、审计和记忆。

该验收证明澄清中断/恢复和受控证据路径，不证明每轮都会读取完整历史，也不证明普通会话能够跳过 RAG。
后两项由 T9-4.3 单独开发和验收，不修改 T6 的已完成结论。

## T9-4.3 通用会话与按需路由验收（T9-4.3.4 已完成）

T9-4.3.1 已完成上下文、恢复、隔离、长对话和并发验收；T9-4.3.2 已完成 L0、结构化理解、确定性
路由、槽位纠正/取消、拒绝和冻结意图集验收；T9-4.3.3a~c 已完成意图治理、v3 指标、容量观测、
四臂 Few-shot 影子实验、ECS B 默认臂和自然直接回答复验。下列第 1~2 项已经由 T9-4.3.3c 完成；
T9-4.3.3d 的组合执行、逐任务终态和前端路由标识已经通过；T9-4.3.4 又完成 Markdown/PDF、跨存储、
连续对话、图片、Trace、Full 评估和意图回归的完整业务闭环：

1. 先问业务问题，再问“我刚才问什么了”，由服务端选中上一条实质性用户消息，并通过
   `chat_direct` 自然复述；Embedding、Reranker 和 Tool 调用均为 0，连续历史元问题不得自引用。
2. “把刚才回答简单解释一下”只基于服务端选中的历史回答自然改写，不触发检索；Provider 不可用或
   生成校验失败时返回确定性精确内容。
3. “它可能和 Recipe 有关吗”正确继承上一轮对象，并保存槽位来源和独立化查询。
4. “查询最新 SOP”重新执行权限过滤和 T5 检索，不复用可能过期的旧证据。
5. 刷新页面、重启服务后同一线程仍可继续；新线程和其他用户不能读取当前线程短期状态。
6. 长对话压缩后，最近精确消息可回忆，`summary_upto_message_id` 与摘要边界一致。
7. 同一线程并发请求被串行化或返回稳定冲突，消息、`turn_seq`、摘要和活动上下文不乱序。
8. 旧证据已过期、被替代或无权时拒绝 `reuse_evidence`，改为重新检索或澄清。
9. 聊天、历史约束聊天、RAG、工具、混合、Web、澄清和拒绝路由分别满足专用答案校验规则；旧
   `history_direct` 记录仍可读取，但新请求迁移后不再产生。
   每条助手消息还必须保存服务端决定的展示模式：RAG/证据复用/工具路由显示结构化卡片，
   `chat_direct/clarify/refuse` 只显示气泡。后续直接回复、SSE 线程替换、刷新和旧消息兼容补齐均不得
   删除历史卡片或给气泡路由误加卡片。
10. 桌面和 `390 x 844` 移动浏览器亲自点击，核对“基于会话上下文”“已检索知识库”“已调用制造数据”等
    真实路由标签、证据抽屉、SSE 阶段、控制台和横向溢出。
11. “你好，顺便查一下 ETCH-03 报警”识别为混合输入并执行任务；“回答太复杂了”只改变表达，不检索。
12. “查 FDC，再对照当前 SOP 给排查建议”拆分并合并为受控 `rag_and_tool`，所有任务都有最终状态。
13. 同句包含安全查询和 Recipe 写操作时，安全任务可继续，写操作单独拒绝或转审批，不静默丢单。
14. Tool 从 ETCH-03 纠正为 ETCH-04 后，旧 Chamber、Recipe 和证据引用失效，无关 Product 保留。
15. “别查了”“停止生成”“放弃本轮追问”和指定子任务取消的范围正确，不删除会话历史。
16. 固定命令与稳定实体格式走 L0；复杂异常调查不得被正则或固定短语强行路由。
17. 当前检索结果的最终授权证据包含图片时，证据抽屉自动显示最终证据顺序中的第一张；多图可手动切换，
    同一结果不覆盖用户选择。新结果无图、图片无权、版本失效或短时链接加载失败时不显示旧图、不绕过
    `/api/assets/{image_id}/access`，文本答案仍可正常完成；桌面和移动端均通过。

现有冻结 `semikb-intent-v1/v2` 已保留内容和 Hash。T9-4.3.3a 已新增意图定义卡、独立
`intent-example-bank-v1` 与冻结 `semikb-intent-v3`，示例库和评估集交叉数为 0。除现有指标外，已新增逐类混淆矩阵、Precision、
Recall、F1、Macro/Micro-F1、高风险 F-beta、多任务集合完全匹配率、漏任务率、多识别率、
`target_type + action` 联合准确率、槽位操作准确率、依赖关系准确率和最终路由计划准确率。

一期每个非 L0 复杂 Case 必须证明同一活动目录的全部意图卡均进入结构化理解 Prompt，不允许关键词或
Embedding 先缩小卡片集合。记录目录版本/Hash、活动卡数量、输入 Token、P50/P95 延迟；`draft` 和
`deprecated` 卡必须排除。超过容量门禁只告警，不得自动切换候选卡算法。

当前 Prompt、全量卡、全量卡+固定 Few-shot、全量卡+动态 Few-shot 已完成同集影子评估并记录质量、
卡数、Token、P50/P95 延迟和调用成本；C/D 均未获得上线资格，生产保持 B。动态检索未使用评估样本，
且未来仍必须在多任务、关键混淆对和危险任务门禁全部通过后另行确认。执行顺序固定为
T9-4.3.1、T9-4.3.2、T9-4.3.3a~d、T9-4.3.4，前一项未验收不得开始后一项。详细设计见
`docs/T9-4.3通用会话记忆与按需路由设计.md`。

## 人工验收顺序

1. 从空 Demo 启动 API 和 Web，提交现行 SOP 问题。
2. 提问缺少 Product/时间/设备的良率问题，确认最多两次追问且未检索。
3. 检索边缘环状缺陷，打开证据抽屉中的合成晶圆图。
4. 查看 Trace 的过滤、Dense/Sparse、RRF、Rerank 和截断结果。
5. 新建训练文档，检查入库任务阶段事件和幂等重试。
6. 运行 `demo-v2`，确认正向证据与无权限、过期、无答案等负向 Case 可以追溯；`demo-v1` 只保留为历史基线。
7. 运行 `python scripts/verify_t5_retrieval.py`，比较 Dense、Dense+Sparse、Reranker、HyDE 四组真实基线，并验证无证据 Case。

填好 `.env` 后先运行 `python -m semikb.storage.preflight`。只有四个数据存储均显示
`connection verified`，才能开始真实资源的集成验收。T4、T5、T6 当前真实验收均已完成；
指标和回答只代表合成知识、公开资料及模拟制造数据场景。

## T9 部署验收

T9-2 本地执行 `tests/test_deployment_artifacts.py`、全量后端测试、Ruff、ESLint、Vite build 和使用临时
服务器 `.env` 的 `docker compose config --quiet`。本机 Docker Desktop Linux Engine 未运行时，
只能证明 Compose 可解析，不能宣称镜像可构建或服务已部署。

T9-3/T9-4 必须在新 ECS 继续完成：安全组复核、Moby/Compose 版本冻结、镜像拉取与构建、10 个容器
健康状态、只发布 80、空存储初始化、`--seed-demo` 幂等发布、浏览器四页点击、真实图片、连续线程、
Trace、异步评估、资源峰值和一次冷备恢复演练。任一项缺证据时 T9 保持未完成。

T9-4 新增流式对话门禁。后端使用 POST SSE，前端使用 `fetch` 读取 `ReadableStream`，不得用只能发起
GET 的原生 `EventSource` 替代当前带鉴权和请求体的消息接口。验收必须同时覆盖：

- `accepted -> stage -> evidence -> answer_delta -> completed` 正常路径，以及 `error`、`heartbeat`、
  `AbortController` 取消和网络断连后的线程状态对账。
- Nginx 关闭该路由的代理缓冲，使用 `curl -N` 和浏览器时间戳证明事件分批到达，不能把完整答案一次性吐出。
- SSE 建连后 1 秒内出现首个可见状态；超过 10 秒没有业务事件时发送心跳，避免页面无反馈。
- 完成事件返回数据库中的规范线程结果；失败时不把半截助手答案保存为最终消息；同一 `request_id`
  重试不得产生重复用户消息或助手消息。
- 桌面与 390 x 844 移动视口均亲自点击，检查长答案滚动、停止生成、切页/返回、控制台和无横向溢出。

2026-08-13，T9-4.2 已按上述门禁通过：公网首事件 94 ms，4 个真实答案 Delta 分批到达，
`completed` 发生在最终持久化之后；停止、同 ID 立即重试、页面刷新取消、心跳、失败不保存半截回答、
桌面和移动浏览器均已验收。详细证据见 `docs/T9-4.2 SSE全链路验收.md`。T9-4.3 的方案已经确认，
其中 T9-4.3.1、T9-4.3.2、T9-4.3.3a~d、T9-4.3.4 和 T9-4.4.1~5 已完成开发与 ECS 验收；
真实公开语料治理与受控下架、Provider 韧性、容量、冷备、安全迁移和最终演示仍未完成，因此 T9-4
和 T9 不能标记完成。

T9-4.4 新增多格式入库门禁。每种声明支持的格式必须使用真实文件独立验收，不能以 MinerU 接口返回
成功或统一 Markdown 输出代替结构和资产正确性证明：

- PDF：标题层级、页码、跨页段落、表格、内嵌图片和页面资产可追溯。
- DOCX：标题/段落顺序、列表、表格、图片及图注正确，不能简单转成无结构纯文本。
- XLSX/CSV：Sheet、表头、单位、合并单元格、公式结果和表格区域保留；Chunk 必须携带列语义。
- PPTX：页序、标题、文本框、表格、图片及其所属页面正确。
- JPG/JPEG/PNG：原图进入 MinIO，OCR/VLM 结果标注来源与置信度，并产生可检索的 `image_text`。
- Markdown/TXT/HTML：编码、标题树、代码/表格和正文净化正确；HTML 导航噪声不得进入正文 Chunk。
- 所有格式：验证 MIME/扩展名/文件签名冲突、损坏文件、超限文件、未知格式、重复上传、失败重试、
  隔离/不发布、版本与权限过滤、`parser_version/upstream_commit` 审计和跨存储主键一致。
- 每种格式至少完成一次工作台真实上传、任务中心阶段检查、检索问答、Trace 下钻和原始/派生资产访问；
  有图片时验证自动预览第一张及多图切换，无图片时验证旧预览清空。

2026-08-15，T9-4.4.1 已通过来源与契约门禁：Knowhere Commit、License/NOTICE 和关键 Git Blob 已
冻结；九格式矩阵不存在通用 MinerU 兜底；四类输出模型及 Parse Provenance 已写入 JSON Schema；
16 个错误码唯一且绑定处理阶段。固定源码工作树的 Commit、License/NOTICE SHA-256 和 17 个 Git Blob
已逐项复算一致；`tests/test_t9441_contract_artifacts.py` 结果为 `4 passed`，后端全量测试为
`234 passed, 1 warning`，Ruff 通过。唯一警告是既有 Starlette/httpx 弃用提示。这只证明审计产物，
不证明 T9-4.4.2 的包边界或 T9-4.4.3~T9-4.4.5 的 Parser、入库和浏览器闭环已经实现。

2026-08-15，T9-4.4.2 已通过包边界与格式路由门禁：`semikb_ingest` Wheel 同时包含 models、
parsers、providers、structure、assets、chunking、routing 和 Dispatcher；九格式路由联合检查扩展名、
MIME、Magic Bytes 与 OOXML 成员，只有 PDF 声明 MinerU，未知格式和未注册适配器无默认回退。测试覆盖
格式伪装、损坏 Office、ZIP Bomb、路径穿越、无扩展名文件、引用完整性和跨层依赖，定向结果
`32 passed`，后端全量 `266 passed, 1 warning`，Ruff、Schema 输出验证、Wheel 和凭据扫描通过。这只
证明统一边界和选择逻辑，不证明 T9-4.4.3 专用适配器、T9-4.4.4 发布集成或 T9-4.4.5 ECS 闭环完成。

2026-08-15，T9-4.4.3 已通过首期专用适配器门禁：九格式均返回冻结统一契约；DOCX/PPTX 保留文档
顺序、页/Slide、表格和图片，XLSX/CSV 保留 Sheet、表头、单位、合并单元格和表格区域，PDF 解析
MinerU content-list 页码与资产，图片使用真实 `qwen3.7-plus` 生成图注/OCR/检测摘要和 `image_text`。
定向结果 `17 passed`，后端全量 `283 passed, 1 warning`，Ruff、冻结 JSON Schema、Linux 哈希依赖锁、
Wheel、凭据扫描和 Qwen 在线冒烟通过。该结论不证明 T9-4.4.4 Worker/跨存储发布或 T9-4.4.5 ECS、
检索和浏览器闭环已经完成。

2026-08-15，T9-4.4.4 已通过受控入库集成门禁：统一 Draft 映射为受治理业务模型并复用原有
上传、Hash、Job/Event、MinIO/MongoDB/Milvus 暂存发布、补偿与幂等路径；Parser/Provider/上游
Commit、Chunker、警告、指标和资产来源可追溯。Markdown/PDF 兼容、九格式映射、表格/图片对象、
稳定 API 错误、Provider 超时不发布、重试不重复和 `table_assets` 可逆迁移均有自动测试。定向结果
`61 passed`，后端全量 `302 passed, 1 warning`，Ruff、前端 lint/build、Wheel 和凭据扫描通过。
本结论不证明逐格式真实黄金文件、ECS 三存储对账、真实检索、图片自动预览或浏览器验收；这些属于
T9-4.4.5。

2026-08-15，T9-4.4.5 已通过逐格式真实闭环门禁：九种确定性 CC0 黄金文件均通过 API/Celery Worker
发布；MongoDB 9 Document/26 Chunk/5 Image/9 Table、MinIO 32 对象和 Milvus v4 26 行对账一致；
9/9 检索命中、ACL 0 泄露、R1/R2 过滤和伪装 PDF 拒绝通过。公网任务中心逐一显示九任务发布及完整
阶段历史；工作台真实点击验证结构化回答、Trace、首图预览、多图切换、无图清空和刷新恢复。桌面
`1280 x 720` 与移动 `390 x 844` 无横向溢出，Console 0 error/0 warning。最终后端全量
`313 passed, 1 warning`，Ruff、compileall、源码包/Wheel、前端 lint/build、`git diff --check` 和
凭据扫描通过。证据见 `docs/T9-4.4.5多格式真实闭环验收.md` 与 `docs/evidence/t9-4-4-5/`。

T9-4.4.6 新增真实公开语料治理与受控下架门禁，严格按以下顺序验收：

1. 来源与契约：Source Manifest 必须包含来源 URL、抓取时间、SHA-256、真实/合成标识、许可状态、
   再分发限制、Parser 提示、预期资产和数据集版本；`withdrawn` 生命周期、API、RBAC、审计、dry-run、
   迁移与回滚方案先冻结。
2. 下架与恢复：仅管理员可操作，必须填写原因；MongoDB 先阻断检索和资产访问，Milvus 再精确清理；
   向量清理失败时保持不可检索并进入补偿。重复下架/恢复幂等，恢复前重新检查审批、有效期、权限、Hash
   和索引版本。MinIO 一期保留原件，不提供误触式硬删除。
3. 通用化基线：公共契约和上传 UI 不得默认 `FAB-01/P-ALPHA/approved/published`；未知元数据进入暂存/
   复核。首次发布必须验证批准的 Source Manifest。受保护证据使用治理字段，不使用 `SOP-` 等 ID 前缀；
   来源更名、未见问法和未见结构不得改变处理结果。
4. 通用标准化：大型表格原始行不向量化；数据字典/画像/受控分析报告可检索；真实图片只生成或保存
   客观标签和 `image_text`，不得虚构 Recipe 或根因；ZIP、目录、标签文件和多文件关系通过统一批次/
   Sidecar 契约处理。候选来源不得拥有专用运行时分支，许可证不明确的原始文件不得提交到公开 GitHub。
5. 批量入库：每个来源经同一 API/Worker、质量门禁和版本/权限发布；MongoDB、MinIO、Milvus 数量与稳定
   ID 对账，重复任务不重复写入，失败、低质量或待复核文档不发布。
6. 评测隔离：开发集可用于实现调试，校准集可用于阈值和参数选择，回归集验证旧能力，密封盲测集只在
   代码、Prompt、Chunk 和检索参数冻结后打开。查看盲测结果后发生修改必须创建新 holdout 版本。
7. 检索与演示：`demo-public-v1` 和 `docs/半导体Agent智库演示问题集.md` 只用于演示/回归；密封盲测单独
   验证未见文档、问题改写、无证据、权限、下架/恢复、Trace 和离线评估，不混报指标。
8. 浏览器：在 ECS 亲自操作任务中心、知识文档清单、工作台、证据抽屉、Trace 和评估页；检查刷新恢复、
   桌面和 `390 x 844` 移动截图及控制台错误。

2026-08-15，T9-4.4.6a 已通过来源与生命周期契约门禁：冻结 `semikb-source-manifest-v1`、许可/
再分发/入库策略、`withdrawn`、知识文档 revision 清单、下架/恢复请求、影响计数和补偿状态；大型表格
在契约层禁止逐行向量化。ECS MongoDB 迁移前 dry-run 为 6 个纯 `create`，应用后再次 dry-run 为 0；
35 条文档目录和 141 条审计事件未改写，六个受保护数据容器 ID 不变。定向 `52 passed`，后端全量
`325 passed, 1 warning`，Ruff、前端 lint/build、Schema/Manifest Hash、存储 preflight/verifier 和
Worker ping 通过。该结果不覆盖 4.4.6b 的运行时下架/恢复、4.4.6c 的真实语料或 4.4.6d 的浏览器验收。

2026-08-16，T9-4.4.6b 已通过受控下架/恢复门禁：知识文档清单、管理员 RBAC、幂等请求、MongoDB
先阻断、Milvus 精确清理和读回、MinIO Hash 保留、补偿重试、恢复重校验及前端知识文档视图均已实现。
ECS 对 5 Chunk/2 图片 revision 完成 `published -> withdrawn -> published`，下架期间检索不命中且图片
HTTP 403，恢复后重新命中且图片 HTTP 200；普通工程师清单访问为 403，同 ID 改原因返回 409。迁移
首次 dry-run 只有 4 个 `create`，第二次为 0，六个受保护数据容器 ID 不变。后端全量
`337 passed, 1 warning`，并已覆盖重复 Worker 投递与 Mongo 私有执行字段边界；Ruff、前端 lint/build、
本地桌面/移动浏览器、Worker、Storage Verifier 和
公网健康均通过。证据见 `docs/T9-4.4.6b受控下架恢复与知识文档管理验收.md`。

2026-08-16，T9-4.4.6c-1 已通过通用化基线门禁：公共契约默认匿名、未知、`draft + staged`，Demo
元数据只由显式 Factory 注入；首次发布和恢复共享 Manifest 校验，受保护检索改用治理字段，HyDE/图片
加权改用结构化检索模式。来源和 Document ID 任意更名不会改变发布/保护行为，缺失 Manifest 的请求
不能进入索引。评测契约已区分四类用途，holdout 必须密封且通过泄漏复核，并只记录一次首次打开时间；
旧 `demo-v1` 和意图冻结数据 Hash 保持不变。

ECS 未知元数据文件经真实 API/Worker 进入 `staged`，MongoDB/MinIO 记录存在而最终 Milvus 行数为 0；
API/Web healthy、Worker pong、Storage Verifier 全绿，六个受保护数据容器 ID 不变。浏览器任务中心、
上传默认值、显式 Demo 配置和评测治理字段完成真实点击检查。完整证据见
`docs/T9-4.4.6c-1通用化基线验收.md`。

桌面与精确 `390 x 844` 公网 Chrome 截图经视觉检查无横向溢出、文本遮挡或异常裁切，移动页面宽度
375 小于视口宽度 390，Console 为 0 error/0 warning。截图保存在 `docs/evidence/t9-4-4-6c-1/`。

最终后端全量为 `345 passed, 1 warning`；Ruff、compileall、11 份版本化 Schema 校验和前端
ESLint/TypeScript/Vite build 通过。唯一警告为既有 Starlette/httpx TestClient 弃用提示。

该结果只覆盖 4.4.6c-1，不覆盖 4.4.6c-2 的真实公开语料标准化、4.4.6d 的批量入库/密封盲测，或
T9-4.5 Provider 韧性。

其余 T9-4 验收包括真实公开语料治理与受控下架、Provider 超时/429/降级、面试 Demo 负载下的 CPU/内存/磁盘/延迟与容器重启、
日志轮转、冷备恢复到独立空目录、端口/RBAC/短时图片链接/日志脱敏复核，以及可离线迁移的固定镜像清单
和 SHA-256。执行顺序、测试边界和证据清单见 `docs/T9-4端到端验收计划.md`。
